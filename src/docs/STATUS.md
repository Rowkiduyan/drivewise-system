# Current Development Status

Project Stage

Early Development

Completed

- Initial project planning
- Drowsiness detection prototype (the detection logic itself runs and works — see caveat under In Progress)
- Login authentication (Supabase Auth, role-based routing to each portal: Admin, Supervisor, Driver, Customer)
- Logout (confirmation modal, single-click guard, wired into all four portals: Admin, Supervisor, Driver, Customer)
- Admin account management (create, update role/name/email, deactivate, reset password — via the `admin-users` Edge Function and Admin panel; supports Supervisor/Admin/Driver/Helper/Customer roles)
- Start Trip backend (`driver-trip` Edge Function, `action: "start-trip"`) — driver-authenticated, creates the `sessions` row, tested end-to-end 2026-08-08.
- `admin-users`' `get-driver-deliveries` and `update-driver-delivery` actions — Driver-gated, back `DriverDeliveries.jsx`'s "Start Pickup"/"Confirm Pickup"/"Complete Delivery" buttons.
- Start Trip UI wiring (2026-08-08): `DriverDeliveries.jsx`'s "Start Pickup" button now calls `update-driver-delivery` (status → `OUT_FOR_PICKUP`) then `driver-trip`'s `start-trip`, in that order. Tested end-to-end: delivery status advances and a `sessions` row (`status = Active`) is created. Guardrails verified directly against the Edge Function via curl (the UI can't retrigger these once a session is Active): calling `start-trip` again on the same delivery (`DR-0011`) while its session was still Active returned `409 "You already have an active trip in progress"`; calling it on a second delivery (`DR-0016`, a test row assigned to the same driver, since deleted) while the first was still Active returned the same 409 — confirming the check is keyed on the driver, not the delivery.
- Fixed a bug found during that testing: `start-trip` originally required `delivery_requests.status === "ASSIGNED"`, but by the time it runs the paired `update-driver-delivery` call has already advanced status to `OUT_FOR_PICKUP`, so every call failed. Now accepts either `ASSIGNED` or `OUT_FOR_PICKUP` (see the Edge Function's inline comment for why the session-uniqueness checks, not this status check, are what actually guard against duplicate/late calls).
- Also verified via curl: calling `start-trip` on a delivery whose status is neither `ASSIGNED` nor `OUT_FOR_PICKUP` (tested with `PENDING_REQUEST`) returns `400` with a clear message. (The remaining guardrail — a delivery assigned to a different driver — wasn't tested; deemed low-risk since it's the same simple equality check pattern as the ones already verified, and the effort wasn't worth it against implementing actual unbuilt functionality next.)
- `service_role` grants confirmed 2026-08-08, ahead of implementing Pause Trip: `update on public.trucks` (previously only `select`) and `select on public.gps_logs` (never previously tested by any Edge Function, since Phase 5/GPS upload isn't implemented yet). See `DATABASE.md`'s `trucks`/`gps_logs` entries and `SUPABASE_GOTCHAS.md` #2/#7 for why these needed checking explicitly rather than assuming `service_role` already had them.
- Pause Trip and Resume Trip backend (`driver-trip` Edge Function, `action: "pause-trip"`/`"resume-trip"`), per `IMPLEMENTATION/03B_PAUSE_AND_RESUME_TRIP.md` — written and deployed 2026-08-08, tested end-to-end against `DR-0011` via the browser console (fetch calls using the driver's session token pulled from `sessionStorage`, since this app stores the Supabase session there unless "Remember Me" was checked at login — see `rememberMeStorage` in `src/lib/supabaseClient.js`). `pause-trip` closes the delivery's open Session (`end_time`, `session_duration`), sums `gps_logs` distances for that Session via Haversine and adds to `trucks.current_mileage` (confirmed `0` and no-op on `trucks`, as expected — no GPS telemetry exists yet), marks the Session `Completed`; confirmed `delivery_requests.status` untouched. `resume-trip` resolves the truck (defaults to `assigned_truck_plate`, accepts an optional `truckPlate` override — no picker UI wired to it yet, see Next Steps), resolves that truck's device fresh via `devices.plate_number`, inserts a new Active Session. Verified: pause on an Active session → `200`; pause with no open session → `400 "No active session found for this delivery"`; resume after pause → `200` with a new session row; resume again while already Active → `409 "This delivery already has an active session"`. Not separately tested: the own-delivery-403 check (same simple equality pattern already verified for Start Trip) and the device-already-active-elsewhere reject on Resume (would need a second truck/device fixture) — deemed low-risk, same call made for Start Trip's remaining guardrail.

- Pause/Resume Trip UI (`DriverDeliveries.jsx`, 2026-08-08): sticky action bar now shows "Pause Trip" (secondary, outlined) next to the normal stage-advance button while a Session is Active, and a full-width "Resume Trip" button (replacing the stage-advance button entirely) when paused. A dedicated amber banner replaces the normal stage banner while paused. Both actions go through the same compact-modal confirmation pattern as the existing stage-advance confirm. Extended `admin-users`' `get-driver-deliveries` to return `hasOpenSession` per delivery (queries `sessions` for a `status = "Active"` row) so the UI can tell "never started" (`ASSIGNED`, no session yet) apart from "paused" (past `ASSIGNED`, no open session) — deployed 2026-08-08. Click-tested end-to-end in the browser 2026-08-08 (Pause/Resume buttons, banner swap, confirm modals) — confirmed working.
- Mobile viewport-height fix (`DriverLayout.jsx`, 2026-08-08): the outer shell and mobile nav drawer used `h-screen` (`100vh`), which on mobile browsers is taller than the actually-visible area whenever the address bar is showing — this made the sticky action bar (and anything else near the bottom) unreachable without first scrolling the whole page. Swapped both to `h-dvh` (dynamic viewport height, supported natively by the installed Tailwind 3.4.4). At the time, Driver was believed to be the only portal used on mobile, so the same `h-screen` pattern in `SupLayout.jsx`/`CustomerLayout.jsx`/`AdminLayout.jsx`/`HelperLayout.jsx` was deliberately left alone — **corrected 2026-08-09: Helper is also used on mobile** (rides along with the Driver on the same delivery, see `UI/LAYOUT.md`'s Mobile section), so `HelperLayout.jsx`'s `h-screen` should get the same `h-dvh` fix; `SupLayout.jsx`/`CustomerLayout.jsx`/`AdminLayout.jsx` remain low-priority as desktop-only.
- Loading states on confirm-modal buttons (`DriverDeliveries.jsx`, 2026-08-08): the stage-advance/Pause/Resume confirm buttons now show a spinner + "Please wait…" and disable both themselves and Cancel while their backend call is in flight, guarding against double-tap double-submission. One shared `isSubmittingTripAction` flag plus a `runTripAction(action, closeModal)` helper, since only one confirm modal is ever open at a time. See `UI/DESIGNS.md`'s new "Loading States" section — this is now the standard pattern for every confirm-modal button, not just these four.
- End Trip (Phase 7, `driver-trip` Edge Function, `action: "end-trip"`), per `IMPLEMENTATION/07_END_TRIP.md` — written and deployed 2026-08-09, same shape as `pause-trip` (closes the delivery's open Session: `end_time`, `session_duration`, mileage sum via `gps_logs`/Haversine added to `trucks.current_mileage`, marks Session `Completed`), plus the one deliberate exception: also sets `delivery_requests.status` to `DELIVERED`. `DriverDeliveries.jsx`'s "Complete Delivery" button now calls `update-driver-delivery` (status → `DELIVERED`) then `driver-trip`'s `end-trip`, same call-order pattern as Start Pickup. Tested end-to-end via curl against `DR-0011` 2026-08-09: `end-trip` closed the Session (`status: "Completed"`, `session_duration: 808`) and `delivery_requests.status` read back as `DELIVERED` with `hasOpenSession: false` afterward; calling `end-trip` again on the same (now-closed) delivery returned `400 "No active session found for this delivery"` as expected. Not separately tested: the own-delivery-403 check — same simple equality pattern already verified for Start/Pause/Resume Trip, same low-risk call made each of those times. Click-tested end-to-end in the browser 2026-08-09 ("Complete Delivery" button, "Delivery Completed" confirmation toast) — confirmed working. Phase 7 is now fully done, backend and UI both.
- Helper visibility into Start/Pause/Resume/End Trip (2026-08-09), per `IMPLEMENTATION/03_START_TRIP_AND_SESSION.md`, `03B_PAUSE_AND_RESUME_TRIP.md`, and `07_END_TRIP.md`'s Helper visibility notes — read-only, no new Helper-triggered actions. Added `admin-users`' `get-helper-deliveries` action (Helper-role-gated, resolves deliveries via `assigned_helper_ids` containment on `delivery_requests`, same shape as `get-driver-deliveries` including `hasOpenSession`). Rewrote `HelperDeliveries.jsx` to fetch this real data instead of its old mock (`buildInitialState`) and removed its old local "Start Pickup"/"Confirm Pickup"/"Complete Delivery" stage-advance buttons and confirm modal (those wrote to local mock state only, never the backend) — the page is now fully read-only, showing the real `delivery_requests.status` and an Active/Paused banner (mirroring `DriverDeliveries.jsx`'s `isPausedTrip`) driven by `hasOpenSession`. Deployed and build-verified (`npm run build`, `eslint`); not live-tested against Supabase by an agent. Not realtime — fetches once per page load, same limitation `DriverDeliveries.jsx` already has; deferred to `08_REALTIME_DASHBOARD.md`.
- Self-service profile picture fix (`admin-users`, 2026-08-09): `upload-profile-picture`/`remove-profile-picture` were gated behind the blanket Admin-only check, so every non-Admin role's own Profile page (Driver/Supervisor/Customer/Helper — all call these same two actions to manage their own picture) got a `403` trying to change their photo; only Admin could actually use them. Fixed by moving both actions above the Admin-only gate with their own check: allowed when `userId` is the caller's own auth id, or when the caller is Admin (managing any user's picture). Found while testing Helper's profile page, but the bug affected all four non-Admin roles equally — not Helper-specific. Deployed.
- Phase 4 backend (`04_DEVICE_BOOT_AND_HEARTBEAT.md`, 2026-08-11): new `device-heartbeat` Edge Function authenticates `device_id`/`device_secret` against `devices.device_secret_hash`, updates `last_ping`, and returns `{ session_active }` resolved from `sessions`. Also fixed a gap found while building this: `RegisterDeviceModal.jsx` previously inserted a `devices` row directly from the browser with no `device_secret` at all, so any admin-registered device could never authenticate — moved into a new `admin-users` action (`register-device`) that generates the secret and its hash server-side and returns the plaintext once for the Admin to copy onto the physical Pi. Alongside this, tightened `devices`/`sessions` grants (previously `anon`/`authenticated` had full unrestricted access to `devices`, including `device_secret_hash` — see `DATABASE.md`'s `devices` "Grants" note and `RLS.md`). Not deployed yet — written and click-tested against the local grants only; run `npx supabase functions deploy admin-users` and `npx supabase functions deploy device-heartbeat` before relying on this in production. Not implemented: the Pi-side heartbeat sender itself (lives outside this repo, on the physical device) and the `session_active`-driven gating of detection/vibration/alerts (depends on that Pi-side code).
- Phase 5 backend (`05_GPS_PIPELINE.md`, 2026-08-11): new `gps-upload` Edge Function, same device-secret auth pattern as `device-heartbeat`. Resolves the most recent `sessions` row for the device, sets `session_id` when Active, leaves it null when Paused-but-Trip-not-ended (attributing the reading via `delivery_request_id` instead), and rejects with `400` once the Trip's `delivery_requests.status` reaches `DELIVERED`/`COMPLETED`/`CANCELLED` or there's no session at all. Deployed and curl-verified end-to-end against a real device (`DV-0020`, truck `FUS 7171`) through a real Start→Pause→Resume→Complete cycle: Active-session insert (`session_id` set), Paused insert (`session_id` null), no-session reject, and ended-Trip reject all confirmed working.
  - **Schema gap found and fixed during this testing:** despite DATABASE.md/`05_GPS_PIPELINE.md` describing the GPS-during-Pause schema change (`gps_logs.session_id` nullable, `delivery_request_id` added) as resolved 2026-08-10, the live `gps_logs` table still had `session_id` as `NOT NULL` and had no `delivery_request_id` column at all as of 2026-08-11 — the design was documented but the actual `ALTER TABLE` had never been run. Confirmed via `information_schema.columns` (table was empty, 0 rows, so no backfill needed) and fixed by running: `alter table public.gps_logs alter column session_id drop not null;` then `alter table public.gps_logs add column delivery_request_id text not null references public.delivery_requests(id);`. DATABASE.md's `gps_logs` entry now matches the live schema.
  - **Also found, not yet fixed:** the driver-level "one Active session" guardrail in `start-trip` can get silently stuck. `DriverDeliveries.jsx` only ever shows the Active-trip banner/Pause-Trip-End-Trip controls for a delivery whose `pickup_date === today` (`activeDelivery = nonArchived.find((d) => d.pickupDate === today)`, ~line 1299) — a delivery with a genuinely open Session but a different `pickup_date` never renders as "Active," so there's no UI path to close it. Hit this live: a stale session on driver `D002`/delivery `DR-0015` (dated 2026-08-08) sat open ~64 hours, silently blocking that driver from starting any new trip, discoverable only by noticing the stale delivery sitting in the Upcoming tab. Closed manually via `driver-trip`'s `end-trip` action over curl (using the driver's own session token). Root-cause fix: pick `active` by `hasOpenSession === true` first, falling back to `pickupDate === today` only when no delivery has an open session — not yet implemented, tracked here so it isn't lost. Out of Phase 5's scope.
- Phase 6 backend + UI (`06_DROWSINESS_ALERT_PIPELINE.md`, 2026-08-11): new `alert-upload` Edge Function, same device-secret auth pattern as `gps-upload`/`device-heartbeat`. Finds the device's most recent `status = "Active"` session (per the doc's "only active sessions may generate alerts" rule), rejects with `400` if none, otherwise inserts `event_type`+`duration`+`session_id` into `alerts`. `service_role`'s missing `select`/`insert` grant on `alerts` (flagged ahead of time, see the Phase 5 entry above) was run before deploying. Deployed and curl-verified: missing-fields `400`, wrong-secret `401`, invalid `event_type` `400` all confirmed against a real device (`DV-0020`) — not verified against a real Active session, since that needs the device's plaintext secret (only shown once at `register-device` time, not recoverable now) paired with a live trip in progress.
  - `face_not_detected` added to `ALERT_TYPE_LABELS`/`ALERT_TYPE_ICONS` (`CameraOff` icon) everywhere the other three event types were enumerated: `SupAnalysisIndiv.jsx`, `AdminAnalysis.jsx`, `SupCrewProfile.jsx`, `SupDeliveries.jsx`, `DriverPerformance.jsx`, `DriverDeliveries.jsx`.
  - `DriverDeliveries.jsx`'s live-alerts card (`LiveMonitoringCard`) was mocked — a `setInterval` with a random chance of fabricating an alert every 20s, per that file's own comment ("no real camera/detection pipeline wired into this frontend yet"). Replaced with a real Supabase Realtime subscription to `alerts` `INSERT`s filtered by `session_id=eq.<active session>`, gated on `isMonitoring` (Active session) the same way the mock was. Needed a small `admin-users` addition: `get-driver-deliveries` only ever returned `hasOpenSession` (a boolean), not the session's actual `session_id`, so there was nothing to filter the subscription on — added `sessionId`, resolved the same way `hasOpenSession` already was, plus captured fresh from `start-trip`/`resume-trip`'s existing `session` response on those two actions.
  - **Not done, deliberately deferred:** revoking `anon`'s unrestricted `select`/`insert` on `alerts` (leftover from the `drowsines.py`/`renz_test.py` prototypes) — recommended in the Phase 6 pre-check but held back pending confirmation, since it's a live privilege change like the `service_role` grant above. Still open.
  - **End-to-end tested 2026-08-11** using disposable fixtures (a throwaway `devices` row + `Active` `sessions` row, both deleted afterward — no real driver/trip data touched): called the deployed `alert-upload` with a real device secret against the test session, confirmed the row landed in `alerts` with the correct `session_id`, and confirmed a Node script subscribed the same way `DriverDeliveries.jsx` does (`postgres_changes` on `alerts` `INSERT`, filtered by `session_id`) actually received it. **First attempt failed** — no event delivered even though the insert succeeded — root cause found: `alerts` (and `delivery_requests`, which `SupDeliveries.jsx` already depends on for its own Realtime subscription) was never added to Supabase's `supabase_realtime` publication; only `delivery_messages`/`devices`/`trucks` were in it. Fixed by running `alter publication supabase_realtime add table public.alerts;` and, separately, `alter publication supabase_realtime add table public.delivery_requests;` (the latter fixes a pre-existing, previously-unnoticed gap in `SupDeliveries.jsx`'s own Realtime subscription — out of Phase 6's scope but the same root cause, confirmed via its RLS policy that an authenticated Supervisor session should now receive it, not live-verified the same way since no Supervisor login was available).
  - **Audio replaced the initial Web Audio API beep with two real clips the user provided**, `public/Usual Alert.mp3` and `public/5+ Multiple Alert.mp3` (both committed to the repo). Escalation rule: alerts 1-4 within any rolling 30-minute window play the Usual clip; the moment a 5th alert lands within that same window, it (and any further alert until the window clears) plays the Multiple clip instead — each clip plays twice per alert (`ALERT_CLUSTER_WINDOW_MS`/`ALERT_CLUSTER_THRESHOLD` in `DriverDeliveries.jsx`). Two bugs found and fixed while live-testing this in the browser against real driver session `D002`/`DR-0018`, both now fixed and re-verified working end-to-end (audio audible, correct clip per tier):
    - **Alert count silently reset to 0 on page reload** — `liveAlerts` was pure client state with no fetch-from-DB fallback, so a remount (e.g. this session's own code-edit-triggered HMR reload) lost all history even though the rows were safely in `alerts`. Fixed: the Realtime-subscribe effect now also fetches existing `alerts` rows for the active `session_id` on mount and seeds `liveAlerts` before subscribing.
    - **Autoplay silently blocked after a page reload mid-trip** (`NotAllowedError`) — the existing unlock only fired on the Start Trip button click, so a reload after the trip was already Active left the freshly-mounted `<audio>` elements never unlocked, and `.play()` was failing silently (the `.catch(() => {})` swallowed it with no visibility). Fixed two ways: added `console.warn` logging on playback failure so this is diagnosable going forward, and added a fallback unlock on the first tap/click anywhere on the page while `isMonitoring` is true, not just the Start Trip button specifically.
  - Confirmed live in the browser (not just build/lint-verified): alert card updates, both audio clips playing at the correct tier, each playing twice, across a real session change mid-testing (a pause/resume produced a new `session_id`, and the subscription/seed-fetch correctly followed it).
- Pi-side script rewrite (2026-08-11): new `pi/drowsiness_monitor.py` (+ `pi/requirements.txt`, `pi/README.md`), replacing the repo-root prototypes' architecture. Based on `renz_test.py`'s structure (its serial-GPS reading was closest to what `gps-upload` needs). Authenticates as a device (`device_id`/`device_secret` via env vars, never hardcoded) against the real deployed Edge Functions instead of writing to Postgres directly with a hardcoded anon key: calls `device-heartbeat` every 10s and gates detection/vibration/alert-upload locally on its `session_active` response (the one documented exception in `00_IMPLEMENTATION_RULES.md`), calls `gps-upload` every 1s unconditionally (never gated on `session_active`, per the GPS-during-Pause exception), calls `alert-upload` immediately per detection threshold. Also now uploads `face_not_detected` as a real alert (the dlib prototypes only ever pulsed the vibration motor locally for "no face," never logged the fourth `06_DROWSINESS_ALERT_PIPELINE.md` event type to `alerts` at all).
  - **Detection library: MediaPipe tried first, reverted to dlib after live hardware testing (2026-08-11).** Initial version used MediaPipe Face Mesh per user decision (resolving the previously-undecided choice). Deployed to a real Raspberry Pi 4 (Python 3.13) and hit two real blockers in sequence: (1) the official `mediapipe` PyPI wheel is 1.0.0 on this platform, which dropped the legacy `mp.solutions` API entirely for a Tasks API requiring a separately-downloaded model file — worked around by auto-downloading the model and rewriting to the Tasks API; (2) once that was fixed, `mediapipe` 1.0.0's native binary crashed with `SIGILL` on this Pi 4's CPU specifically (compiled assuming ARM AES extensions this SoC doesn't implement). The community fallback build (`mediapipe-rpi4` 0.8.8, from ~2021) has no wheel for Python 3.13, and no other official mediapipe version has a matching wheel for this Python/architecture combo either (confirmed via `pip install "mediapipe==0.10.14"` → "No matching distribution"). Given the choice between compiling an older Python from source (20-40+ min on Pi 4 hardware, plus a second interpreter to maintain) or reverting to dlib, the user chose to revert. `drowsiness_monitor.py` now uses dlib with EAR/MAR thresholds/state-machine logic ported unchanged from `drowsines.py` — same library the original prototypes already ran successfully on this class of hardware, so no MediaPipe-placement recalibration is expected this time. The 68-point predictor file (`shape_predictor_68_face_landmarks.dat`) isn't bundled with dlib or this repo either — auto-downloaded on first run, same pattern as the model file was for the abandoned MediaPipe path.
  - **Hardware test completed 2026-08-11, full pipeline confirmed working end-to-end on a real Pi 4** (Wi-Fi model, device `DV-PITEST1`, a disposable test device registered directly via SQL for this session — not through `register-device`). Confirmed against the live deployed backend, not mocked: `device-heartbeat` (real `last_ping` updates and `session_active` reads confirmed via direct DB query), the venv/dependency setup (needs `--system-site-packages`, PEP 668 blocks system-wide `pip install` on this Pi's OS; `dlib` was already present system-wide, no compile needed), camera + dlib detection, and `alert-upload` (`prolonged_eye_closure` and `face_not_detected` alerts both fired and were confirmed landing in the `alerts` table with correct `session_id`/`event_type`/`duration`).
  - **Two more bugs found and fixed during this hardware test, beyond the MediaPipe/dlib story above:**
    - **`device-heartbeat`/`gps-upload`/`alert-upload` calls were missing the Supabase platform's required `Authorization`/`apikey` header** (separate from, and in addition to, the app's own `device_id`/`device_secret` body check) — every heartbeat was failing with `401 UNAUTHORIZED_NO_AUTH_HEADER` until this was added (`SUPABASE_AUTH_HEADERS`, using the public anon/publishable key, same one embedded in the web app's client bundle). This wasn't caught by curl-testing earlier in the session because those curl commands always included the header manually; the Pi script itself never did.
    - **GPS serial permission denied** (`/dev/serial0` → `/dev/ttyS0`, `crw-------` root-only) — this specific Pi's "Serial Port" was configured as a login console (`serial-getty@ttyS0.service`, itself crash-looping since a GPS module was sending NMEA data instead of a login prompt), which reserves the UART and blocks non-root access regardless of `dialout` group membership. Fixed via `raspi-config nonint do_serial_hw 0` (enable UART) + `do_serial_cons 1` (disable login-console use) + disabling the getty service + reboot. Not a code fix — a one-time Pi configuration step, documented here in case a second unit hits the same thing.
  - **Vibration motor GPIO pin: two wrong guesses before landing on the right one, resolved by direct hardware testing.** `drowsines.py` wires the motor to GPIO 17; `renz_test.py` (this rewrite's structural basis) wires it to GPIO 4 — the two prototypes disagree with each other, and neither documented which was real. First guess (pin 4, from `renz_test.py`) produced no vibration. Assumed pin 17 was correct instead and switched to it, plus wrote a byte-for-byte headless copy of `drowsines.py` itself (no rewrite code involved at all, `pi/drowsines_headless_test.py`, deleted after use) to rule out a software bug — pin 17 toggled exactly as expected in that isolated test too, but still no physical vibration. The user then separately tried running the real, unmodified `renz_test.py` directly (pin 4) and **it did vibrate** — meaning pin 4 was right all along, and pin 17 (despite toggling correctly at the GPIO level) isn't connected to anything on this unit's actual wiring. Reverted to pin 4 (now overridable via `DRIVEWISE_VIBE_MOTOR_PIN` env var for other units). Takeaway: GPIO toggling correctly is not proof a pin is the right one — only a physical vibration (or a multimeter) confirms that, and the two prototypes' disagreement with each other should have been flagged as an open question before guessing, not resolved by picking one via documentation-reading alone.
  - Boot automation (systemd unit) not written — separate, still-open item, see `pi/README.md`.
  - **GPS upload verification started, not yet confirmed complete as of this writing (still true as of 2026-08-12, see the GSM entry below — indoor testing that day still had no satellite fix available).** `alert-upload`/`device-heartbeat` are confirmed working, but no real coordinate had yet landed in `gps_logs` — the script only sends once it has a non-zero lat/lon, and no valid satellite fix had been confirmed either way (indoors GPS often can't get one at all, which isn't a bug). Added three one-time console prints to `drowsiness_monitor.py` for this diagnosis, since the script was otherwise silent about GPS state: `[GPS] receiving NMEA data from module` (module is talking to the Pi at all), `[GPS] satellite fix acquired: <lat>, <lon>` (got a real position), `[GPS] upload confirmed working (...)` (a reading reached the backend). A second disposable test session (`500dc7c9-4693-4e2e-9220-238c49e9fdfc`, device `DV-PITEST1`) was created for this — **left Active, not cleaned up**, since the test was still in progress when this session ended. Clean it up (`update sessions set status='Completed', end_time=now() where session_id='500dc7c9-4693-4e2e-9220-238c49e9fdfc'`) once the GPS test is actually done, or reuse it to continue testing.
- GSM/cellular connectivity for the Pi, retested and confirmed end-to-end 2026-08-12 with WiFi hard-disabled (`sudo nmcli radio wifi off`, rather than just deprioritized) to eliminate the dual-default-route/DNS conflict found and worked around earlier the same day — this time `alert-upload` and the full `session_active` Start/Pause/Resume gating cycle were run over the cellular link itself (previously only `device-heartbeat` had real GSM confirmation): heartbeat, alerts, and pause/resume gating all confirmed working over GSM. One transient hiccup during this retest — `ping 8.8.8.8` briefly returned "Network is unreachable" / 100% loss right after an `systemctl restart gprs-ppp.service`, resolved itself once given more time (~15s) for the PPP link to fully renegotiate; not a bug, just PPP's normal connect sequence taking longer than a 5s wait accounted for.
  - **Real bug found and fixed during this retest** (`drowsiness_monitor.py`'s pause-reset block, `~line 392`): the vibration motor could be left physically on after a Pause. The reset block only called `set_vibration(False)` when `ALARM_ON` (the eye-closure alarm) was set — but the no-face-detected pulse (a separate code path, `pulse_mode`/`pulse_on`) also drives the motor directly via `set_vibration(pulse_on)`, independent of `ALARM_ON`. A Pause landing mid-pulse reset `pulse_mode`/`pulse_on` back to `False` in software but never told the GPIO pin itself to turn off, since the `if ALARM_ON:` guard was `False` for that path — motor kept vibrating after Pause until something else eventually changed its state. Reproduced live during GSM testing (a `face_not_detected` alert "leaked" past a Pause, motor kept running). Fixed by making the reset unconditional (`set_vibration(False)` runs every time regardless of which path had the motor on), copied to the Pi via `scp`, and confirmed fixed live — pausing mid-pulse now stops the motor immediately.
- `face_not_detected` alerts no longer play the driver-facing audio clip (`DriverDeliveries.jsx`, 2026-08-12, user decision) — the Realtime alert handler now skips `playAlertClip` specifically for this `event_type`, while still recording it in `liveAlerts`/history same as the other three types. Reasoning: the Pi's own vibration motor already gives physical feedback for this case, and `face_not_detected` fires easily on ordinary driving behavior (checking mirrors, glancing at the dashboard) unlike the other three alert types, which are all genuine drowsiness signals — audio for it was judged more noisy than useful. Build/lint-verified; not yet click-tested live (no real `face_not_detected` alert triggered against this change yet).
- GSM/cellular connectivity for the Pi (2026-08-12): a USB-to-serial SIM800C module (2G/GPRS, Smart SIM) confirmed as a working alternative internet path for units without WiFi — see `pi/README.md`'s new "GSM/cellular connectivity" section for the full PPP/chat-script/systemd/DNS-routing setup. Confirmed end-to-end with WiFi fully disabled: DNS resolution, TLS handshake, and `device-heartbeat` all working over the cellular link alone (`devices.last_ping` observed advancing on schedule for `DV-1114`). Found and fixed two application-level issues surfaced only once real (non-curl) traffic ran over this link: `drowsiness_monitor.py`'s `HTTP_TIMEOUT_SEC` was too tight (`3.0`→`8.0`) for GPRS's measured 340-826ms round-trip latency, and heartbeat/GPS/alert calls were each opening a fresh connection instead of sharing one `requests.Session()` — the latter was likely starving the heartbeat of bandwidth once GPS's 1s upload cadence (`05_GPS_PIPELINE.md`) was also active on the same low-throughput link. **Not yet tested:** `gps-upload` itself over this link — no satellite fix was available during this session (indoors). Also fixed in passing: `RegisterDeviceModal.jsx`'s status dropdown defaulted to lowercase `"active"` while its own `<option>` values were capitalized (`"Active"`/etc.), so a device registered without touching the dropdown got sent an unmatched value — resolved by removing the dropdown entirely and hardcoding new devices to `"Active"` (status changes now belong to a post-registration device-management flow, not creation time; matches `admin-users`' own `register-device` default).
- **`session_active` heartbeat gating bench-tested and confirmed 2026-08-12** (see `04_DEVICE_BOOT_AND_HEARTBEAT.md`'s "Active Session" section — this was the one specific behavior left "implemented-not-confirmed" after the 2026-08-11 hardware test): started a real trip (temporarily reassigned `DR-0001` from `D004` to `D002` since `D002`'s login was the one available — reverted back to `D004` immediately after), watched the Pi's console print `[HEARTBEAT] session_active -> True` right after Start Pickup, confirmed detection/alerts running, then hit Pause Trip and confirmed `[HEARTBEAT] session_active -> False` printed and detection/vibration/alert-upload all stopped immediately — well inside the ~10s heartbeat interval. Resume also correctly flipped it back to `True` and resumed detection. Added a one-line diagnostic (`[HEARTBEAT] session_active -> <bool>`, only printed on change) to `call_heartbeat()` in `drowsiness_monitor.py` to make this transition directly observable in the console instead of inferred — no other behavior changed.
  - **Separate observation surfaced during this test, not yet resolved:** the vibration motor (GPIO 4) missed physically actuating on one `prolonged_eye_closure` alarm out of two closure attempts, despite both attempts correctly detecting the closure and calling `call_alert_upload`/`set_vibration(True)` (confirmed identical code path both times — this is not a detection-software bug). Leading theory is a hardware-level intermittent issue (loose connection, or an ERM vibration motor's start-torque vs. run-torque margin — cheap eccentric-mass motors can occasionally fail to kick-start from a dead stop even with a sound connection) rather than a code defect, since both activations ran through the exact same `IN → OUT → output LOW` sequence with no code-level difference between them. One miss out of two attempts isn't enough data to call this a real pattern yet — next step, not yet done, is a repeat test (~5 consecutive closures) to establish an actual hit rate before investigating further (e.g. checking the driver transistor/MOSFET specifically).
- **Raspberry Pi boot automation done and confirmed via a real cold-boot test, 2026-08-12** (see `pi/README.md`'s new "Boot automation" section for full detail — this was the last open gap from the 2026-08-11 hardware verification): two `systemd` units, both `enable`d to persist across reboots — `gprs-ppp.service` (GSM/PPP link, already existed) and a new `drivewise-monitor.service` running the script itself, with device credentials supplied via `EnvironmentFile=/etc/drivewise/monitor.env` (mode `600`) instead of manual `export`s. `sudo reboot`, then reconnecting: both services came up `active (running)` on their own — GSM negotiated, camera initialized, monitor reached its normal running state, heartbeats succeeded — zero manual steps. WiFi deliberately kept enabled alongside GSM (not disabled) so the Pi stays reachable over SSH for maintenance; confirmed GSM still wins the default route regardless (pppd's route has no explicit metric, beats WiFi's DHCP `metric 600`). Pinned DNS to public resolvers (`8.8.8.8`/`1.1.1.1`) via an immutable `/etc/resolv.conf` (`chattr +i`) to stop NetworkManager (WiFi) and pppd (GSM) from fighting over which nameserver wins — the original version of this exact conflict was diagnosed and worked around earlier in the same session (see the GSM entries below).
  - Two things found and fixed along the way, both operational rather than app bugs: (1) `ExecStart` needed a `-u` flag added — under `systemd`, Python fully-buffers `print()` to a pipe by default (unlike a TTY), so `journalctl` showed nothing past the camera-init lines for a while even though the script was running fine; (2) a stray `y` character (`y[Unit]`) ended up on line 1 of the service file from mishandling `nano`'s file-lock prompt, which broke the `[Unit]` section header and caused `systemd`'s `Assignment outside of section. Ignoring.` warnings — fixed by deleting the stray character.
- Revoked `anon`'s unrestricted `select`/`insert` access to `alerts` (2026-08-12) — flagged since Phase 6 as a leftover privilege from the `drowsines.py`/`renz_test.py` prototypes' direct-anon-key write pattern, held back pending confirmation that `alert-upload` was the sole real write path. Executed now that's been confirmed via repeated bench testing (Phase 6 completion, GSM retest, boot automation test — all through `alert-upload`, none through direct anon writes). `revoke all on public.alerts from anon;`, verified via `information_schema.role_table_grants`: `anon` no longer appears in the table's grants at all, while `authenticated`'s `select` (Driver app's Realtime subscription) and `service_role`'s `select`/`insert` (`alert-upload`) are untouched. See `DATABASE.md`'s `alerts` entry.
- **Live in-app navigation built (`DriverDeliveries.jsx`, 2026-08-12)** — resolves the map-library contradiction in `01_SYSTEM_ARCHITECTURE.md`'s Route Comparison section, driven by the project's capstone documentation requiring in-app navigation built on the Google Maps JavaScript API (a fixed external requirement, not something resolvable by keeping the existing Leaflet UI as-is — see that doc for the full reopened-then-scoped history). New `LiveNavigationMap` component, added `@react-google-maps/api` (first real use of the Maps JS SDK client-side; `VITE_GOOGLE_MAPS_API_KEY` had been declared but unused until now). Shown only during the driving stage (`isDrivingStage`); before that, the original static Google Maps iframe embed is unchanged. Route computed client-side via `DirectionsService` off the existing pickup/dropoff coordinates — no schema change needed. Live position sourced from the Pi's `gps_logs` uploads via a new Realtime subscription mirroring the existing `alerts` subscription pattern exactly (seed-fetch + `postgres_changes` `INSERT`), gated on `isMonitoring` so it correctly freezes during Pause rather than tracking a closed session. Rerouting on deviation via `google.maps.geometry.poly.isLocationOnEdge` against the rendered route polyline, debounced to ~12s between reroutes. Turn-by-turn step advance via `computeDistanceBetween` against each step's end point (~35m threshold). Live-position marker is a rotating directional arrow (`SymbolPath.FORWARD_CLOSED_ARROW`), Waze/Google-Maps-navigation-style, not a plain dot — `gps_logs` has no heading/bearing column, so heading is derived client-side from consecutive position ticks via `computeHeading`, skipping updates under ~2m of movement to avoid jitter from GPS drift while stationary. Camera also tilts (45°) and rotates to match direction of travel, via a single atomic `map.moveCamera({ center, zoom, tilt, heading })` call — this is the actual Waze/Google-Nav "Start" look, not just a flat top-down view with a rotating arrow on it. (Note: Google's dedicated turn-by-turn Navigation SDK, with the full native-app-quality puck/lane-guidance experience, only exists for native Android/iOS — there is no web/JavaScript equivalent, so this hand-built arrow-plus-DirectionsService approach is the actual ceiling of what's available in-browser, not a simplified stand-in.) Voice guidance added (Web Speech API `SpeechSynthesisUtterance`, one announcement per step index, mute toggle) — matches the app's existing drowsiness-alert audio precedent. The existing `toGoogleMapsDirections()`/"Navigate Now" deep-link to the driver's native Google Maps app is kept as a fallback inside the new component, not removed, per explicit scope decision. Required one new grant: `authenticated: select` on `gps_logs` (previously `service_role`-only) — see `DATABASE.md`'s `gps_logs` entry.
  - **Four real issues found and fixed during live testing 2026-08-12, all now resolved:**
    1. `DirectionsService` initially failed with `REQUEST_DENIED` — it bills against the legacy **Directions API**, a separate API from the **Routes API** already enabled on this project. Fixed by enabling Directions API in Google Cloud Console (same key, no new credential).
    2. Tilt/rotation requires a **vector map**, which needs a `VITE_GOOGLE_MAPS_MAP_ID` (`.env`/`.env.example`, passed via `GoogleMap`'s `options.mapId`) — created in Google Cloud Console (Maps Platform → Map Management → Create Map ID, type JavaScript, style Vector) and set to a real value.
    3. Vector map still fell back to raster on the dev machine — traced to Chrome having hardware GPU acceleration disabled (`chrome://gpu` showed `WebGL: Software only`); fixed by enabling "Use graphics acceleration when available" in `chrome://settings/system` and restarting Chrome. Real driver phones are expected to have hardware WebGL by default, so this was a dev-machine-only issue, not expected to recur in production use.
    4. **Camera-reset bug**: tilt showed briefly on load, then flattened back to 0 the moment a live position update landed. Root cause — `<GoogleMap center={livePosition}>`'s own reactive `center` prop was making `@react-google-maps/api` call `map.setCenter()` on every GPS tick, competing with a separate manual `panTo()`/`setTilt()`/`setHeading()` sequence; `setCenter()`-style calls reset tilt/heading to 0 as an internal side effect. Fixed by capturing a stable, never-updated `initialCenter` for the `center` prop and moving all subsequent camera changes into one atomic `moveCamera()` call instead of three separate ones.
    5. **Second, separate camera override found after fixing #4**: even with the reset bug fixed, the map still rendered fully zoomed out and flat, showing the entire Manila-to-Antipolo route span instead of a tilted street-level view. Root cause — `DirectionsRenderer` calls `map.fitBounds()` on every render by default, to fit the whole route on screen, which silently overrides whatever `zoom`/`tilt` `moveCamera()` had just set. First attempted fix — adding `preserveViewport: true` to `DirectionsRenderer`'s `options` — was built and build/lint-verified 2026-08-12 but **not re-verified live before that session ended**; re-tested live 2026-08-12 (new session) and the override was still recurring even with `preserveViewport: true` set. Root-caused via `node_modules/@react-google-maps/api`'s own source: not conclusively pinned to a specific line, but `DirectionsRenderer` is now a deprecated Maps JS API (Feb 2026) whose console warning states existing bugs won't be addressed going forward, and it's the only thing in the render tree touching `fitBounds` at all. **Actually fixed** by removing `DirectionsRenderer` entirely and rendering the route as a plain `<Polyline>` (`directions.routes[0].overview_path`, purple `#7C3AED` stroke, Waze-style) instead — a `Polyline` never calls any camera method, so there's nothing left to override `moveCamera()`. `DirectionsService` still computes the route/steps client-side, unchanged; only the rendering path changed. Confirmed live in the browser 2026-08-12: map now stays tilted and street-level, following the GPS-tracked position, instead of flattening/zooming out.
  - **Also found, unrelated to the map itself**: testing on a phone via the dev server's LAN URL (`http://<ip>:5173`, needed for real GPS/touch testing) hit `RefererNotAllowedMapError` — the Maps API key's HTTP referrer restriction only allowed `localhost`. Fixed by adding the LAN URL to the key's allowed referrers in Google Cloud Console. Worth revisiting later since a LAN IP can change (DHCP) — a broader pattern (e.g. the whole local subnet) may be more durable than the exact IP added here.
  - Temporarily added `eruda` (dev dependency, `main.jsx`, gated behind `import.meta.env.DEV`) as an on-screen console overlay for phone debugging without a USB/remote-debugging setup — **not yet removed**, should be stripped out once mobile testing of this feature is fully done.
  - Map height increased (`h-36 sm:h-44` → `h-64 sm:h-80`) since this is now the page's primary interactive nav view, not a small at-a-glance preview — user feedback mid-session, the original size felt too short once tilt/rotation made it a real navigation surface rather than a glance-and-go embed.
  - Build/lint-verified throughout (`npm run build`, `eslint`, one `react-hooks/set-state-in-effect` false-positive suppressed with justification — the step-advance effect is a legitimate external-system sync off GPS ticks, not a derived-state anti-pattern).

  **Bug #5 fix re-verified 2026-08-12 (continued session), but the first attempt (`preserveViewport: true`) turned out insufficient** — still recurring live even with that option set, root-caused to `DirectionsRenderer` itself being a deprecated Maps JS API (Feb 2026) whose remaining bugs "will not be addressed" per its own console warning. **Actually fixed** by removing `DirectionsRenderer` entirely and rendering the route as a plain `<Polyline>` (`directions.routes[0].overview_path`, purple `#7C3AED`, 7px stroke) instead — a `Polyline` never calls `fitBounds`/any camera method, so there's nothing left to override `moveCamera()`. `DirectionsService` still computes the route/steps client-side, unchanged; only the rendering path changed. Confirmed live: map now stays tilted, street-level, and follows the GPS-tracked position correctly.

  **Two more real bugs found and fixed during this same live-testing pass (2026-08-12, continued session), both now confirmed working:**
  - **`gps_logs` had zero RLS policies despite RLS being enabled on the table.** The 2026-08-12 grant (`authenticated: select`, see `DATABASE.md`'s `gps_logs` entry) only set table-level privilege — with RLS enabled and no policy, Postgres silently returns 0 rows to every query regardless of the grant. This meant `livePosition` could never populate and the live-position arrow could never render, for _any_ GPS data (real Pi fix or synthetic test rows alike) — not actually a "no satellite fix yet" limitation as first assumed when this gap was investigated. Fixed: `create policy "authenticated select gps_logs" on public.gps_logs for select to authenticated using (true);`, same permissive pattern already used for `alerts`' equivalent Realtime-read policy. `DATABASE.md`'s `gps_logs` entry should be updated to mention this policy alongside the grant it was found next to.
  - **Live-position arrow rotation was wrong** — set to the absolute compass `heading` computed from consecutive GPS ticks, on the theory that a legacy `google.maps.Marker` Symbol's `icon.rotation` rotates in the same absolute space the camera's `heading` rotates, canceling out to "pointing up on screen." Confirmed wrong via live testing (arrow didn't track direction of travel): a legacy `Marker`'s Symbol icon is a screen-space billboard, not part of the rotating map plane, so setting its rotation to match camera heading does not cancel out. Fixed by hardcoding the marker's `rotation: 0` (always points straight up) and leaving `heading` state to drive only the camera via `moveCamera()` — the correct Waze/Google-Nav model: the arrow never turns, the world rotates under it so direction-of-travel stays at the top of the screen. Confirmed live: arrow now stays fixed pointing up while the map visibly rotates under it as simulated GPS points moved in different directions.

  **Third bug found the same pass: `gps_logs` was also missing from the `supabase_realtime` publication.** Even with the RLS policy above fixed, the live-position arrow only ever updated once (the seed-fetch on mount) and then never moved again — a symptom distinct from the RLS gap, since the seed-fetch (a plain REST `select`) worked fine but the `postgres_changes` `INSERT` subscription received nothing. Root cause: the table was never added to the `supabase_realtime` publication (the exact same class of gap hit and fixed for `alerts`/`delivery_requests` back in Phase 6, see that entry above) — `postgres_changes` events are only broadcast for tables in that publication, regardless of grants/RLS. Fixed: `alter publication supabase_realtime add table public.gps_logs;`. Confirmed live afterward with a scripted GPS simulation (repeated `gps_logs` inserts a couple seconds apart against the live test session/fixture below): the arrow now visibly moves per tick instead of only reflecting the one seed-fetched position.

  **Further live-testing/polish on this same feature, later the same day (2026-08-12), all in `DriverDeliveries.jsx` unless noted, confirmed working live:**
  - **Arrow rotation was still wrong even after the RLS/publication fixes** — set to the absolute compass `heading` computed from consecutive GPS ticks, on the theory that a legacy `google.maps.Marker` Symbol's `icon.rotation` rotates in the same absolute space the camera's `heading` rotates, canceling out to "pointing up on screen." Confirmed wrong via live testing (arrow didn't track direction of travel): a legacy `Marker`'s Symbol icon is a screen-space billboard, not part of the rotating map plane, so setting its rotation to match camera heading does not cancel out. Fixed by hardcoding the marker's `rotation: 0` (always points straight up) and leaving `heading` state to drive only the camera via `moveCamera()` — the correct Waze/Google-Nav model: the arrow never turns, the world rotates under it so direction-of-travel stays at the top of the screen.
  - **Movement simulation methodology**: rather than hand-picked straight-line test coordinates (which cut across blocks/parks, not real roads), a temporary `console.log` was added to `computeRoute`'s success callback to dump the live `DirectionsService` result's `overview_path` as JSON, copied out of the browser console, sampled to ~40 points, and walked via repeated `gps_logs` inserts a couple seconds apart (a disposable shell script, not committed) — this made the simulated dot trace the actual rendered purple route instead of an approximation. The temporary `console.log` was removed once no longer needed.
  - **Zoom**: raised in steps over several rounds of feedback, `16 → 18 → 19 → 23 → 21` (a `NAV_ZOOM` constant), settling at `21` — close to the real ceiling Google's imagery supports most places anyway (it silently clamps lower where a location's imagery doesn't support 21).
  - **Recenter-on-refresh bug**: a page refresh mid-trip could leave the map stuck on its `initialCenter` fallback (the pickup/dropoff default) instead of the driver's actual last-known position. Root cause — the camera-recenter effect only ran when `livePosition` changed, but on a fresh page load the seed-fetch (which sets `livePosition`) could resolve _before_ the Google Map itself finished loading (`mapRef.current` not yet set), and since a plain ref isn't a reactive dependency, the effect never got a second chance to fire once the map became ready. Fixed by tracking map-readiness as state (`isMapReady`, set in `onLoad`) and adding it to that effect's dependency list.
  - **Map height** raised twice more after the initial `h-64 sm:h-80` → `h-96 sm:h-[28rem]` bump (recorded above): once to `h-[28rem] sm:h-[32rem]`, per repeated user feedback that it still felt short once this became the page's primary nav surface rather than a glance-and-go embed.
  - **Fullscreen toggle added**: a `Maximize2`/`Minimize2` button in the map's header toggles a local `isFullscreen` state, which switches the whole `<section>` to `fixed inset-0` covering the viewport. Purely a CSS/layout change — doesn't unmount or remount anything, so the drowsiness-alert `<audio>` elements and their Realtime subscription (both in the parent `DriverDeliveries` component, entirely outside this component's tree) keep running unaffected while fullscreen.
  - **Recenter button added**: a floating `LocateFixed`-icon button (bottom-right of the map) calls the same `moveCamera({ center: livePosition, zoom: NAV_ZOOM, tilt: 45, heading })` the auto-follow effect uses, letting the driver jump back after manually panning the map around (`gestureHandling: 'greedy'` lets them drag it away freely, and nothing else snaps it back on its own).
  - **Phone-only page-scroll control, several iterations to get right:**
    1. First attempt: an invisible `touch-action: pan-y` swipe pad overlapping the left edge of the map. Didn't visibly work — nothing on screen moved, which read as broken even though the intent was for it to scroll transparently.
    2. Moved out from overlapping the map to a sibling column beside it (still inside the Live Navigation card), per feedback that it shouldn't visually be "inside the map" at all.
    3. Moved again, per further feedback, to a `fixed`-position element pinned to the actual page edge (`left-0 top-1/2`, independent of the card's scroll position) — rendered directly from `DriverDeliveries.jsx`, not from inside `LiveNavigationMap`, gated on `isDrivingStage`. `z-20`, deliberately below `DriverLayout.jsx`'s mobile nav drawer/backdrop (`z-40`/`z-30`) so it sits behind those if opened.
    4. Still didn't scroll — traced to two compounding causes: (a) `touch-action: pan-y` doesn't reliably hand off scrolling from a `fixed` element to a _nested_ `overflow-y-auto` container the way it does for `window`/`document` — and this app's actual scrollable region is exactly that nested case (`DriverLayout.jsx`'s outer shell is `h-dvh overflow-hidden`, with a separate inner `<div id="driver-scroll-container" className="overflow-y-auto">` doing the real scrolling — added that `id` specifically so this control could target it directly); (b) the handlers were Touch Events only (`onTouchStart`/etc.), which never fire for a mouse drag when testing via a resized desktop browser rather than a real touchscreen — switched to Pointer Events, which cover both.
    5. **Final form, per explicit request**: a real scrollbar-style track + thumb, not an invisible drag pad — the thumb visibly moves as the page scrolls (a `scroll` listener on `#driver-scroll-container` recomputes its height/position, matching native scrollbar proportions) and can itself be dragged (Pointer Events, mapping drag distance to a proportional `scrollTop` change) to scroll the page. Confirmed working live.
  - **Fullscreen trip-action buttons**: fullscreen (`fixed inset-0 z-50`) covers the page's own sticky bottom action bar entirely, so there was no way to Pause Trip or advance the delivery stage without backing out of fullscreen first. `LiveNavigationMap` now accepts `isMonitoring`/`nextLabel`/`NextIcon`/`nextColor`/`onPause`/`onResume`/`onStageAdvance` props (mirroring `statusCfg` and the three confirm-modal triggers exactly) and, while fullscreen, its footer shows the same Pause Trip/stage-advance (or Resume Trip, if paused) buttons instead of "Navigate Now." Outside fullscreen, "Navigate Now" is unchanged.
    - **z-index collision found while testing this**: the confirm modals these buttons open (`confirmingStageAdvance`/`confirmingPause`/`confirmingResume`) and the fullscreen map both used `fixed inset-0 z-50` — same layer, so the modal could end up visually behind/tied with the fullscreen overlay, with taps silently reaching the map underneath instead of the modal's buttons. A tap on "Confirm Pickup" appeared to do nothing as a result. Fixed by raising the three confirm modals to `z-[60]` and the "Delivery Completed" success toast (previously `z-40`, same problem) to `z-[70]`, both explicitly above the fullscreen map.
    - **Real data bug found via this same test, unrelated to the z-index fix**: once the modal was actually reachable, confirming still failed with a genuine backend `400`. Root cause was the live test fixture itself (below) — `DR-0001`'s raw `delivery_requests.status` had been set to `'FOR_PICKUP'`, which is not a real value in that column at all (only coincidentally displayed correctly, since `DriverDeliveries.jsx`'s `DB_TO_DRIVER_STATUS` map falls back to passing an unrecognized raw value straight through unchanged, and `'FOR_PICKUP'` also happens to be the frontend's own internal stage-vocabulary name — see `DriverDeliveries.jsx` ~line 1192-1203). `admin-users`' `DRIVER_STATUS_TRANSITIONS` guard only recognizes `ASSIGNED`/`OUT_FOR_PICKUP`/`ARRIVED_PICKUP`/`OUT_FOR_DROPOFF`/`ARRIVED_DROPOFF`/`DELIVERED`, so it correctly rejected the transition. Fixed by correcting the live row to the real raw value, `status = 'OUT_FOR_PICKUP'` — the UI's displayed stage was unaffected (still reads as `FOR_PICKUP`, now via the real mapping entry instead of the fallback). **Confirm Pickup then succeeded for real** — see the fixture note below for the resulting state change.
  - Build/lint-verified after every change in this list (`npm run build`, `eslint`), and each interactive behavior (movement, rotation, recenter, fullscreen, slider, trip-action buttons) confirmed live in the browser, not just build-clean.

  **Live test fixture, current state as of this writing (2026-08-12) — needs cleanup once testing is fully done:**
  - `delivery_requests` row `DR-0001`: originally `assigned_driver_id: D004`, `status: ASSIGNED`, human-readable addresses, `assigned_helper_ids` not including `H003`. Now `assigned_driver_id: D002`, `pickup_location: '14.5906, 120.9822'`, `dropoff_location: '14.5995, 121.0026'` (coordinate-format text, needed for `parseCoords`), `assigned_helper_ids` includes `H003` (added for the Helper-portal Realtime testing above) — **and**, across two separate rounds of live click-testing this same day (the fullscreen navigation test, then the Helper-portal catch-up test), `status` has for-real advanced twice: first to `OUT_FOR_DROPOFF`, then all the way to `DELIVERED` via a genuine "Complete Delivery" press. **Must be reverted** to `D004`/`ASSIGNED`/original addresses/original `assigned_helper_ids` (remove `H003`) when done — reverting the status this time undoes two real transitions, not just a fixture field.
  - The test `sessions` rows created/closed across both rounds of testing on this delivery (several — Start/Pause/Resume/End Trip and repeated Pause/Resume cycles each create or close one) — none should be left `Active`; delete or set `Completed` during cleanup along with `DR-0001` itself.
  - `gps_logs` rows against those sessions: 45 rows as of the navigation-testing round (ids 3 through 65, non-contiguous) — delete all of them during cleanup.
  - Two disposable `alerts` test rows inserted directly via SQL during the Helper-portal Realtime testing (`id 104`, `id 105`, both `event_type` test values, not real detections) — delete during cleanup.
  - `eruda` (temporary phone-debugging console overlay, `main.jsx`) — remove the `import('eruda')` block and `npm uninstall eruda` once mobile testing is fully done.
  - The Maps API key's HTTP referrer allowlist includes the dev machine's specific LAN IP (`http://192.168.254.176:5173/*`) — kept intentionally, needed for phone-based testing of the mobile-only Driver/Helper portals (`localhost:5173/*` alone doesn't cover a phone hitting the dev server over LAN). Will go stale if the LAN IP changes (new network/router); re-add the new IP the same way if the map stops loading on-device. A tunnel tool (e.g. `ngrok`) would give a stable hostname instead if this becomes a recurring annoyance — not done yet, low priority since the entry is scoped to a private IP unreachable from the public internet either way. `localhost:5173/*` also added, for desktop browser dev testing (2026-08-12).
  - Billing safety net added (2026-08-12): a Cloud Run function (`disable-billing-on-budget-exceeded`) subscribed to the budget's Pub/Sub notifications (`budget-alert-topic`) now auto-disables project billing if actual cost exceeds the configured budget — the earlier email-only budget alert (`02_BOOKING_AND_TRIP_CREATION.md`) was notification-only and did not cap spend. Function's service account (`884327379182-compute@developer.gserviceaccount.com`) granted `roles/billing.projectManager`. Setup verified correct (IAM roles, Pub/Sub linkage, Eventarc trigger, deployed source) but not live-fire tested against a real overspend — real cost was $0.00 at verification time, so a forced-low-budget test would have been inconclusive.
  - Explicitly not touched, per plan: `RouteDeviationMap`/`COMPLETED_REPORT_DATA` (the archived-delivery post-trip report's Leaflet map, fed by hardcoded mock data — separate, already-flagged issue) and `DeliveryDetailView`'s own separate "Route Overview" iframe (a different view, no live GPS context wired to it).
  - Still open: whether the Supervisor Dashboard's (Phase 8, unbuilt) map follows the same Google Maps JS pattern or something else — not decided, the capstone requirement was specifically about driver navigation. See `08_REALTIME_DASHBOARD.md`'s Map Display section.

- **Helper portal catch-up with Driver, done and live-verified (2026-08-12)** — the two gaps flagged in `03_START_TRIP_AND_SESSION.md`'s and `06_DROWSINESS_ALERT_PIPELINE.md`'s Helper visibility notes: `HelperDeliveries.jsx` now subscribes to Realtime updates on `delivery_requests` (any change refreshes the list) and `sessions` (filtered by `delivery_request_id`, keeps Active/Paused live), and shows a read-only `LiveAlertsCard` (seed-fetch + `alerts` INSERT subscription filtered by `session_id`, no audio — mirrors `DriverDeliveries.jsx`'s `LiveMonitoringCard` minus the `<audio>` element) while `isMonitoring`. `admin-users`' `get-helper-deliveries` now also returns `sessionId` per delivery (same as `get-driver-deliveries` already did), needed to filter the alerts subscription.
  - **Required RLS work beyond the original plan, two real bugs found along the way:** Driver/Helper had no direct read access to `delivery_requests` or `sessions` at all before this (only Supervisor/Admin did — `RLS.md`'s prior claim that `sessions` had a public-read policy was stale/wrong, corrected as part of this work, see that file). Added `assigned crew can read own deliveries`/`assigned crew can read own sessions` policies. First attempt at both used an inline `exists (select ... from other_table ...)` join in the `USING` clause — this passed ordinary REST queries fine but **silently dropped every Realtime event**, no error anywhere; confirmed via raw websocket frame inspection that `alerts` (unaffected, no join in its policy) delivered live while `sessions`/`delivery_requests` never did despite correct publication membership, replica identity, and a successful channel join. Root cause and fix — rewriting both policies as a single `SECURITY DEFINER` function call (matching the codebase's existing `current_user_role()`/`is_admin()` pattern on `users`) rather than an inline join — written up in full as `SUPABASE_GOTCHAS.md` #9, since this is a non-obvious Realtime+RLS interaction likely to recur on the next table. `sessions` also needed adding to the `supabase_realtime` publication (never added before — same recurring gap as `alerts`/`delivery_requests`/`gps_logs`).
  - **Live-verified end-to-end in the browser** using real Driver/Helper accounts (`D002`/`H003`, already paired crew from past deliveries) on the `DR-0001` live test fixture, two browser contexts side by side: Pause Trip and Resume Trip on Driver's screen both updated Helper's Active/Paused banner within seconds with no reload; a manually-inserted `alerts` row appeared in Helper's Live Alerts count/history live; Driver pressing "Complete Delivery" moved the delivery out of Helper's Today tab live. Build/lint-verified throughout (`npm run build`, `eslint`, two justified `react-hooks/set-state-in-effect` suppressions added following the existing precedent in `DriverDeliveries.jsx`).
  - **Fixture consequences, folded into the existing DR-0001 cleanup note below:** `H003` was added to `DR-0001`'s `assigned_helper_ids` to enable this test (needs removing), and the live "Complete Delivery" test genuinely advanced `DR-0001` to `DELIVERED` for real (superseding the already-real `OUT_FOR_DROPOFF` transition noted below — this is now a second real, non-fixture transition on the same row). Two disposable `alerts` test rows (`id 104`, `id 105`) were also inserted directly via SQL against a real session on this delivery and need deleting.
- Fixed the stale-open-session UI bug (`DriverDeliveries.jsx`, 2026-08-12): the "Active" delivery was selected purely by `pickupDate === today`, so a delivery with a genuinely open Session but a different `pickup_date` never surfaced as Active and its Pause/End Trip controls were unreachable (root cause of the `D002`/`DR-0015` incident noted above, found 2026-08-11 while testing Phase 5). Two-part fix, since the render path had the same date check in two places: (1) `loadDeliveries`'s `activeDelivery` selection now checks `hasOpenSession` first, falling back to `pickupDate === today` only when no delivery has an open session; (2) the "Today" tab's own render gate and tab-count badge were separately checking `active.pickupDate === todayISO` (`isActiveToday`) before showing the workspace at all — even with (1) fixed, a stale-dated open session still wouldn't have rendered. Replaced with a plain `Boolean(active)` check (`hasActiveDelivery`), since `active` itself is now the correctly-prioritized source of truth. Build/lint-verified (`npm run build`, `eslint`), then **click-tested end-to-end in the browser 2026-08-12** against a disposable fixture: inserted a real `Active` `sessions` row (`1516424c-45c6-47c4-92f0-9caf6a97604f`) on `DR-0018` (driver `D002`, `pickup_date` `2026-08-11`, status `OUT_FOR_PICKUP` — i.e. genuinely stale-dated, not today) directly via SQL, since the normal Start Trip flow can't produce a stale-dated open session on demand. Logged in as `D002`: the delivery correctly rendered as Active in the workspace tab despite its date, and Complete Pickup → Complete Delivery both worked, closing the session (`status: Completed`, `session_duration: 151s`) and advancing `delivery_requests.status` to `DELIVERED` — same End Trip codepath as always, no manual fixture cleanup needed. Bug confirmed fixed, not just built.
- **Phase 8 — Supervisor Realtime Dashboard, built and live-verified (2026-08-12)** (`IMPLEMENTATION/08_REALTIME_DASHBOARD.md`). Per that doc's already-approved plan: `SupDashboard.jsx`'s `ActiveDeliveries` (mock `ETA` column dropped — no real backing data existed for it — replaced with real `Trip`/`Device` badge columns and a per-row locate/recenter icon) and `DriverSafetyList` wired to real data; net-new `LiveFleetMap` panel (`@react-google-maps/api`, one marker per truck, `fitBounds` across multiple trucks, zoom 17 on a single truck) and a paused-but-moving anomaly banner. KPI strip/`LiveFleet`/`RecentActivity`/`WeeklySafetySummary` deliberately left mock, per the plan's explicit scope.
  - **Two schema/DB fixes needed before the build, done via user-run SQL (no service-role/DB credentials available to run directly):** the two `gps_logs` indexes design had called for since 2026-08-08 but never created (`create index on gps_logs (session_id, timestamp)` / `(delivery_request_id, timestamp)` — see `DATABASE.md`); and a `devices` RLS gap found while re-verifying this phase's own schema/access check — `devices`' SELECT policy had, at some undocumented point, already been tightened to Admin-only (not the wide-open policy `RLS.md`/this phase's plan both still described), which meant Supervisor got zero rows. Fixed by adding `Admins and Supervisors can read devices` (bare `current_user_role() = ANY(...)` check, Realtime-safe per `SUPABASE_GOTCHAS.md` #9) — see `RLS.md`'s corrected `devices` section.
  - **Three real bugs found and fixed during live click-testing**, all confirmed working afterward, none caught by `npm run build`/`eslint` alone:
    1. `select('*')` against `devices` returned `403`, not a filtered/empty result — traced to `devices`' `authenticated` grant being column-scoped (excludes `device_secret_hash`); selecting `*` tries to read that column too and Postgres rejects the whole request. Fixed by selecting an explicit column list instead. Written up as `SUPABASE_GOTCHAS.md` #10 since this is a distinct mechanism from the more common missing-grant/missing-RLS gaps already documented there.
    2. The new `LiveFleetMap` panel rendered completely empty — no map, no spinner, no empty-state text, despite correct data/logic. Root cause: `Panel`'s own outer div already sets `h-full`; passing `className="h-80"` to override it created two same-specificity `height` utility classes on the same element, and `h-full` won — which then resolved against an auto-height ancestor (this panel isn't in a grid row with a sibling to stretch against) and collapsed to ~0. Fixed by giving the panel's _content_ an explicit `h-80` from inside, instead of fighting `Panel`'s own height class.
    3. The paused-but-moving anomaly detector missed a GPS reading taken immediately after a Pause, during rapid manual SQL testing (session closed, then a GPS row inserted right after, back-to-back). Root cause: the original implementation decided "did this reading happen during the Pause" by comparing the reading's timestamp against a locally-cached pause-start value, kept in sync via a `sessions`-Realtime-event → refetch → re-render chain — and a fast-arriving GPS insert could beat that chain, silently dropping its distance from the cumulative total (the marker's position still updated correctly, just not the movement tally). Fixed by keying off `gps_logs.session_id is null` directly instead — the real backend's own signal for "this reading landed during a Pause" (per the GPS-during-Pause design), decided at insert time and carried on the row itself, so it can't race against anything client-side. Re-tested against the same rapid-fire scenario that broke it, confirmed fixed.
  - **Locate/recenter icon** (per-truck button in `ActiveDeliveries`' Device column, pans+zooms the map to that specific truck) wasn't in the original plan — added per user request during testing. Needed its own small fix: clicking the _same_ truck twice in a row (after manually panning away) didn't recenter the second time, since React saw no state change on the repeated id — fixed with a separate click-counter dependency so the pan/zoom effect always re-fires regardless of whether the id repeats.
  - **Live-verified in the browser** against a disposable fixture on `DR-0015` (driver `D002`, truck `AAA 1111`, device `DV-1114`, all pre-existing/reusable — no new fixture rows needed): Waiting for Device → Monitoring Unavailable → Online → Offline device-state transitions, Active → Paused trip-state transition, live map marker appearing and updating via Realtime (not just the seed-fetch), the locate button (including repeat-click), and the paused-but-moving anomaly banner turning the marker red and showing the banner text. Fixture fully cleaned up afterward (`gps_logs`/`alerts`/`sessions` deleted, `delivery_requests.status` reverted to `DELIVERED`, `devices.last_ping` restored to its pre-test value).

- **Phase 2B — Multi-Stop Deliveries, built and verified 2026-08-12** (`IMPLEMENTATION/02B_MULTI_STOP_DELIVERIES.md`, scoped and built as a new lettered sub-phase, following `03B_PAUSE_AND_RESUME_TRIP.md`'s precedent). Schema: `delivery_requests.stops` (`jsonb not null default '[]'`) deployed via `supabase/migrations/20260812134525_delivery_requests_stops.sql`, verified live via REST before building against it. Customer-only entry at booking (`CustomerRequestDelivery.jsx`, new repeatable `LocationInput` rows, capped at 5, no existing add/remove-row pattern in this codebase to reuse — built net-new); read-only ordered display in `SupDeliveries.jsx`'s request detail panel (`mapDbRequest` now carries `stops` through); driver navigation (`DriverDeliveries.jsx`'s `LiveNavigationMap`) extended with `DirectionsService` `waypoints` on the dropoff leg only, a client-side-only `currentLegIndex` tracking which stop is current (mirroring the existing `currentStepIndex` turn-by-turn pattern), a "Stop X of Y" indicator, leg-aware voice guidance, and reroute-on-deviation now re-routing only through stops not yet reached. `admin-users`' `get-driver-deliveries` now returns `stops` per delivery (deployed). No per-stop status is ever written anywhere — reference-only, per the original schema decision.
  - **Real contradiction found and corrected during scoping:** `02_BOOKING_AND_TRIP_CREATION.md` had assumed route generation would use the Routes API (`computeRoutes`/`intermediates`), but the driver navigation that actually shipped 2026-08-12 uses the legacy `DirectionsService` (`waypoints`) instead — already an open, previously-reverted contradiction per `01_SYSTEM_ARCHITECTURE.md`'s Route Comparison section. Built against what's actually shipped; both docs corrected to stop citing the Routes API for this.
  - Build/lint-verified throughout (`npm run build`, `eslint`) on all three modified frontend files plus the Edge Function change. **Browser click-tested end-to-end 2026-08-12** via Playwright against the real dev server and real test accounts (customer + Supervisor), disposable fixture `DR-0019` (deleted afterward): booking form's add-stop UI confirmed working via screenshot, submitted row verified via REST to have the exact designed `stops` shape, and Supervisor's request detail panel confirmed showing "STOPS (2)" with numbered badges read-only, above Pick-up/Drop-off Location. No console errors either flow. **Driver navigation "Stop X of Y" indicator also verified live 2026-08-12**, same fixture pattern. Straight-line interpolated GPS points failed first (never advanced past "Stop 1 of 2" — the 35m step-advance threshold needs the _real_ road-following route's step points, not a coarse straight line; sequential per-step advance means an early miss blocks everything after it). Fixed by adding `scripts/gps-route-capture.mjs` (headless Playwright, captures the real `DirectionsService` result by replacing the whole class — patching just `.prototype.route` is silently ignored by the SDK) and `scripts/gps-route-simulate.py` (replays those exact points into `gps_logs`), kept in the repo as reusable test tooling (`playwright` added as a devDependency; see `scripts/README.md`). Also fixed a fixture bug hit along the way: `DR-0011`'s `dropoff_location` was still a plain address, not `"lat, lng"` text, so `parseCoords` failed silently and the map hung on "Computing route..." — not a feature bug, a stale fixture from earlier testing. Confirmed live: indicator correctly advanced "Stop 1 of 2" → "Stop 2 of 2" → "Heading to Drop-off". Fixture fully reverted.
- **Photo-required chain completion, reassigned to the Helper — built and live-verified 2026-08-12** (`IMPLEMENTATION/02B_MULTI_STOP_DELIVERIES.md`'s "Photo-Required Chain Completion"). Reopened two previously-deliberate decisions on purpose, per explicit user direction after several rounds of clarification: (1) the real completion order is Pickup → Dropoff → Stop 1 → ... → Stop N, not "stops between a fixed pickup and dropoff" — `dropoff_location` is now the _second_ item in the chain, not always last; (2) the Helper now completes Confirm Pickup, the dropoff, and every stop (each requiring a proof photo), not the Driver — Driver keeps only Start Pickup and Pause/Resume Trip.
  - **Schema**: `pickup_photo_url`/`dropoff_photo_url`/`dropoff_completed_at` added to `delivery_requests` (`supabase/migrations/20260812151605_delivery_requests_proof_photos.sql`); `stops` entries gain `completed`/`completedAt`/`photoUrl` once the Helper completes them.
  - **New Storage bucket `delivery-proof-photos`, created via the Storage REST API** (`POST {SUPABASE_URL}/storage/v1/bucket`, service-role key) — unlike raw table DDL, this went through without needing the Dashboard/SQL Editor, a first for this project's bucket-creation history (`driver-profile-pics` needed manual Dashboard setup back in Phase whatever-it-was).
  - **Backend**: `admin-users`' `update-driver-delivery` trimmed to Driver-only `ASSIGNED->OUT_FOR_PICKUP`; the same action name reused inside the Helper block for `OUT_FOR_PICKUP->OUT_FOR_DROPOFF` (Confirm Pickup, photo required) — safe since role blocks are mutually-exclusive `if`s. New Helper-gated `complete-dropoff`/`complete-stop` actions, each computing "is this the final item in the chain" dynamically (`stops.length === 0` for dropoff; `stopIndex === stops.length - 1` for a stop) and setting `DELIVERED` only when true, returning `isFinal` so the frontend knows whether to call `end-trip` next. Shared `uploadProofPhoto()` helper factors out the upload/validate logic across all three. `driver-trip`'s `end-trip` — previously blanket Driver-only — now also accepts an assigned Helper caller specifically for that one action; `start-trip`/`pause-trip`/`resume-trip` unaffected.
  - **Frontend**: `HelperDeliveries.jsx` (previously fully read-only) gained a "Delivery Chain" list with per-item Complete buttons and a required-photo confirm-modal (`src/lib/proofPhoto.js`, new, resizes without forcing a square crop unlike the profile-picture helper it's modeled on). `DriverDeliveries.jsx` lost the Confirm Pickup/Complete Delivery buttons entirely (dead `DELIVERED`/`OUT_FOR_DELIVERY` branches removed from `advanceStage`, not just hidden), gained a Realtime `delivery_requests` subscription so it notices Helper-driven transitions live (it previously had none at all — the old Complete Delivery button used to update local state directly, which no longer happens on this page), and `LiveNavigationMap`'s destination/waypoints reworked so `dropoff_location` becomes a waypoint (not the final target) once stops exist, falling back to the old dropoff-is-final behavior if the last stop's address doesn't parse to coordinates.
  - **Real bug found and fixed while testing, not by design intent**: `HelperDeliveries.jsx` had the exact same stale-open-session bug `DriverDeliveries.jsx` was fixed for back on 2026-08-11 (`STATUS.md`'s `D002`/`DR-0015` incident) — Helper's "Active" delivery was selected purely by `pickupDate === today`, so a delivery with a genuinely open Session but a different `pickup_date` never rendered as Active at all, making the brand-new chain-completion UI completely unreachable for any such delivery. This had apparently never been ported over when the Driver-side fix shipped. Same two-part fix applied: `loadDeliveries`'s `activeDelivery` selection now checks `hasOpenSession` first; the Today tab's render gate replaced with a plain `Boolean(active)` check (`hasActiveDelivery`).
  - **Build/lint clean throughout** (`npm run build`, `eslint`) on every touched file.
  - **Live-verified end-to-end 2026-08-12**, both finality cases, reusing `DR-0015`/`DR-0016` (driver `D002`, helper `H003`, truck `AAA 1111`/device `DV-1114`, both temporarily reassigned/reset then fully reverted afterward) as disposable fixtures:
    - **No-stops case** (`DR-0015`): curl-verified the full chain — Driver rejected from calling Confirm Pickup directly (`"Cannot move a delivery from OUT_FOR_PICKUP to OUT_FOR_DROPOFF"`), Helper rejected without a photo (`"A proof-of-pickup photo is required"`), Helper succeeded with one (`pickup_photo_url` set); `complete-dropoff` returned `isFinal: true` immediately (no stops), setting `DELIVERED`; Helper's `end-trip` call closed the Session; a Helper attempt at `start-trip` still correctly `403`'d. Uploaded photo confirmed publicly fetchable (`200`, real bytes) at its stored URL.
    - **With-stops case** (`DR-0016`, 2 stops): same Confirm Pickup flow, then `complete-dropoff` correctly returned `isFinal: false` (stops remain) with status still `OUT_FOR_DROPOFF`; `complete-stop` on index 0 returned `isFinal: false`; `complete-stop` on index 1 (the last one) returned `isFinal: true` and set `DELIVERED`; `end-trip` closed it out. `dropoff_completed_at` was set even though dropoff wasn't the item that finalized it — confirmed as-designed, not a bug.
    - **Browser click-through** (Playwright, real accounts, `DR-0015` reset a third time): confirmed via the actual rendered UI, not just API responses — Driver's page showed zero Confirm Pickup/Complete Delivery buttons and the new read-only "Waiting for the helper to confirm pickup" banner; Helper's page showed the Complete button, the confirm-modal's submit button was disabled before a photo was chosen and enabled immediately after, and clicking Complete genuinely advanced the delivery (`OUT_FOR_DROPOFF`, `pickup_photo_url` set) — confirmed via REST, not just a screenshot, since the screenshot itself landed mid-transition ("Please wait…" spinner) rather than the settled state. Zero console errors.
    - All fixtures and uploaded test photos fully cleaned up afterward — both deliveries back to `DELIVERED` with their original locations/truck/stops, no dangling Active sessions, all 6 test photo objects deleted from Storage.
- **Phase 2C — Route Styling & Proof-of-Delivery Visibility, all three parts built and live-verified 2026-08-13** (`IMPLEMENTATION/02C_ROUTE_STYLING_AND_PROOF_VISIBILITY.md`). Resolves the "Distinguishable pickup vs. dropoff markers/route color" item that had sat in Planned since 2026-08-12 (see that entry, now superseded) — scoped up during design from the originally-discussed two-color (amber/purple) scheme to a full per-leg palette, per user request mid-session.
  - **Part 1 (route colors/pins, `DriverDeliveries.jsx`'s `LiveNavigationMap`)**: the single `overview_path` `<Polyline>` replaced with one `<Polyline>` per `directions.routes[0].legs[]` entry, each colored from a 6-color `NAV_LEG_COLORS` palette (red/blue/green/purple/orange/pink, cycling) — the to-pickup leg (its own single-leg `DirectionsService` call) is pinned to red; the post-pickup chain (pickup→dropoff→stops) starts one color over so it's never red. Added `<Marker>` pins for Pickup (sky "P"), Dropoff (emerald "D"), and each Stop (amber numbered) — previously the map showed no markers at all for the actual chain points, only the live-position arrow. Whichever pin matches the current leg's `end_location` (loose-tolerance match, `isCurrentNavTarget`) renders larger/full-opacity; the rest are smaller/muted.
  - **Part 2 (proof-of-delivery visibility)**: `admin-users`' `get-driver-deliveries` gained `pickupPhotoUrl`/`dropoffPhotoUrl`/`dropoffCompletedAt` (mirroring `get-helper-deliveries`, which already had them from the prior entry above); `SupDeliveries.jsx`'s `mapDbRequest`, `CustomerDeliveries.jsx`'s `mapDeliveryRow` (+`stops`, which it hadn't carried before), and `DriverDeliveries.jsx`'s `mapDelivery` all carry the fields through. Each of the four portal files (Supervisor/Customer/Driver/Helper) gained its own `ProofOfDeliverySection` component — deliberately _not_ a shared component, matching this codebase's existing no-cross-portal-sharing convention (e.g. `LiveAlertsCard` already exists separately in Driver/Helper) — a thumbnail grid (Pickup/Dropoff/each completed Stop) linking out to the full-size photo, gated on `DELIVERED`/`COMPLETED`.
  - **Part 3 (location-gated completion, added after Parts 1-2 shipped, per follow-up user request)**: Confirm Pickup/Complete Dropoff/Complete Stop (all three Helper-owned actions in `admin-users`) now reject with a distance-included error unless the crew's most recent `gps_logs` reading is within 200m of the target. Since there's no Helper-owned GPS device (only the truck's Raspberry Pi reports position), location is read from the delivery's own `gps_logs` rather than adding browser geolocation to the Helper portal. Fails open (skips the check, same as before) when the target address isn't in parseable `"lat, lng"` form, or when no GPS reading exists yet for the delivery — deliberate, matching `09_EDGE_CASES.md`'s existing "Trip functionality must not block on Pi absence" principle. New `checkProofLocation`/`parseCoords`/`haversineMeters` helpers in `admin-users/index.ts`; no frontend changes needed since the Helper's chain-completion modal already surfaces any `{error}` string the Edge Function returns. Deployed (`npx supabase functions deploy admin-users`) and curl-verified live: Confirm Pickup correctly rejected from ~2.5km away with `"You're too far... about 2496m away, must be within 200m"`, then succeeded once a `gps_logs` row was inserted at the pickup coordinates.
  - **Two unrelated small fixes made along the way, same session:** (1) the Live Navigation mute button (`isMuted`) previously only blocked _future_ voice-guidance announcements — an already-speaking utterance kept talking until it finished on its own; fixed by calling `window.speechSynthesis.cancel()` immediately when muted is toggled on. (2) `HelperDeliveries.jsx`'s proof-photo `<input type="file">` had `capture="environment"`, which forces mobile browsers straight into the camera app, skipping the gallery — removed, so mobile now shows the normal camera-or-gallery picker.
  - **New reusable test tooling, kept in the repo** (`scripts/README.md` documents all of it): a permanent fixture delivery `DR-0020` (driver D002, Metro Manila coordinates, 2 stops) with its captured route geometry saved at `scripts/fixtures/DR-0020-legs.json` (reusable indefinitely — the fixture's coordinates never change); `scripts/simulate-dr0020.sh` (idempotent GPS-movement replay for Part 1); `scripts/prep-proof-photos-test.sh` (resets DR-0020 and assigns Helper H003 for a _manual_ Part 2/3 walkthrough — deliberately does not upload anything itself, since exercising the real file-picker/upload path is the point); `scripts/set-dr0020-location.sh <pickup|dropoff|stop1|stop2>` (nudges the crew's simulated position for the Part 3 location gate during a manual walkthrough, since position doesn't move on its own outside the GPS simulation); `scripts/demo-dr0020.sh` (runs Part 1 then preps Part 2/3, one command for a full live demo).
  - Build/lint-verified throughout (`npm run build` not re-run this session, `eslint` clean on every touched file — one pre-existing, unrelated warning/error each in `SupDeliveries.jsx`/`HelperDeliveries.jsx`, left untouched). `deno check` on `admin-users/index.ts` shows the same pre-existing systemic `never`-type noise the whole file already has (no typed Database generics used anywhere in this file) — not something this change introduced.
  - **Follow-up round, same day (2026-08-13): "Stop N" renamed to "Dropoff N" everywhere it's used as an item label**, per user request — every point after Pickup is conceptually another dropoff, so the chain reads Pickup → Dropoff → Dropoff 2 → Dropoff 3 → ... instead of mixing "Dropoff"/"Stop" vocabulary. Touched: `HelperDeliveries.jsx`'s Delivery Chain list + confirm-modal title; the `ProofOfDeliverySection` thumbnail labels in all four portal files; `SupDeliveries.jsx`'s read-only "Stops (N)" section header → "Additional Dropoffs (N)" (numbered badges now start at 2 to match); `CustomerRequestDelivery.jsx`'s booking-form stop-input labels. Also found and fixed a stale/wrong label while touching that last file: the "+ Add a stop" button said "between Pick Up and Drop Off," which contradicted `02B_MULTI_STOP_DELIVERIES.md`'s already-documented correction that these locations come _after_ Drop Off, not between — left over from before that correction shipped. Fixed to "+ Add another dropoff after Drop Off."
  - **Live-navigation UX revised twice more this same day, per direct user feedback while watching it live:**
    1. Dropoff/Stop markers switched from a plain filled circle to a proper map-pin teardrop SVG shape (`dropoffPinIcon`, Material "place" glyph, explicit `anchor`/`labelOrigin`) — a circle alone didn't read as "you're supposed to drop off here." Pickup deliberately kept the plain circle (conceptually different).
    2. **"Current leg only" rendering** — originally every leg's polyline and every Pickup/Dropoff/Stop pin rendered simultaneously (muted/smaller for non-current ones); user feedback was that showing the whole future route/every pin at once read as cluttered for a turn-by-turn view. Changed to render only `legs[currentLegIndex]`'s polyline and only the one pin matching the current leg's target — still colored/labeled per its position in the chain (so it still changes leg to leg), just no longer previewing the whole remaining route at once.
  - **New browser-based demo tool, `scripts/demo-control-panel.html`** — Play/Stop/Next buttons instead of terminal commands, for self-serve control during a live demo. Deliberately a standalone local HTML file, never part of the deployed app: the actions it triggers need the `service_role` key (only `service_role` can insert into `gps_logs`; `authenticated` only has `select`), which must never exist in code that ships to users — an in-app version would need a new server-side Edge Function action just to gate test-only functionality, adding permanent test scaffolding to the graded app's backend. The service_role key is pasted into a password field once and saved only to that browser's `localStorage`, never written into the file or committed.
  - **Two real bugs found and fixed while building/using that tool, both also present in the underlying `simulate-dr0020.sh`:**
    1. GPS-tick inserts weren't setting `session_id` (only `delivery_request_id`) — `DriverDeliveries.jsx`'s Live Navigation subscribes to `gps_logs` `INSERT`s filtered by `session_id=eq.<active session>`, so rows with a null `session_id` saved fine but were invisible to that subscription, silently leaving the map's position stuck. Fixed by resolving and passing the real session id on every insert.
    2. Reusing an existing Active session skipped re-advancing `delivery_requests.status` to `OUT_FOR_DROPOFF` — if the delivery had been left at `OUT_FOR_PICKUP` by a prior `prep-proof-photos-test.sh`/Reset-Chain run, `LiveNavigationMap` stayed in single-leg "heading to pickup" mode (one color only) instead of showing the full post-pickup chain. Fixed in both the shell script and the HTML tool to always force `OUT_FOR_DROPOFF` regardless of whether the session was reused or freshly started.
  - **Route fixture recaptured with far denser points**: `gps-route-capture.mjs` originally captured only each turn's `end_location` (one point per maneuver, 21 points total for DR-0020) — replaying that made the marker visibly jump between distant points on long straight segments instead of gliding along the road. Changed to capture every point along each step's actual road-following path (`step.path`), yielding 559 points for the same route. Recapturing also surfaced a real bug: `computeRoute` prefers a stale `livePosition` (leftover `gps_logs` rows from earlier testing) over the true origin when one exists, so the first recapture attempt silently started mid-route near the last stop instead of at Pickup — fixed by clearing `DR-0020`'s `gps_logs` before recapturing. `simulate-dr0020.sh`'s default replay delay shortened `1.5s → 0.1s` per point to keep total playback time reasonable with ~27x more points.
- **Real hardware GPS pipeline confirmed end-to-end on a physical Raspberry Pi + Live Navigation map, 2026-08-13** — closes out GPS-upload-over-real-hardware verification that had been sitting incomplete since 2026-08-11/12 (see those entries above; only heartbeat/alerts/vibration had real hardware confirmation before this — GPS itself never had a confirmed coordinate reach `gps_logs` from a genuinely real, uncontrolled outdoor test until today).
  - **New disposable fixture, `DR-0021`** (driver `D002`, helper `H003`, truck `AAA 1111`/device `DV-1114` — same reusable combo Phase 2B/8 already used), created for this test after retiring `DR-0020` (the Phase 2C demo fixture, see above): its `gps_logs` had accumulated 1,112 simulated rows from `simulate-dr0020.sh`/`gps-route-simulate.py` runs, which needed clearing before trusting real-hardware data, and its truck (`WAS 1234`) had no `devices` row linked to it at all — `driver-trip`'s `start-trip` resolves `sessions.device_id` by looking up `devices.plate_number == assigned_truck_plate`, so a real session on it would always resolve `device_id: null`, same failure mode as gap noted in `05_GPS_PIPELINE.md`. `DR-0020` is now `CANCELLED`, not deleted: `service_role` has no `DELETE` grant on `sessions`/`alerts` at all, and `delivery_requests` has a blocking FK from `sessions` besides — hard deletion isn't available through the app's normal permission model. Cancelling (`status`/`cancelled_by`/`cancel_reason`/`cancelled_at`/`cancelled_from_status`) plus manually closing its stale open session and moving its `pickup_date` off "today" was needed to fully retire it as "today's active delivery" (see the `DriverDeliveries.jsx` bug below, found in the process). `scripts/simulate-dr0020.sh`/`demo-dr0020.sh`/`set-dr0020-location.sh`/`demo-control-panel.html` all still reference `DR-0020` by id and are now stale against a cancelled delivery — left as-is, since they're for the _simulated_ Phase 2C demo specifically, not this real-hardware track.
  - **Real bug found and fixed (worked around, not code-fixed) during this fixture swap, `DriverDeliveries.jsx`**: `DB_TO_DRIVER_STATUS` (line 1349-1357) has no `CANCELLED` entry, so a `CANCELLED` delivery's `active.status` resolves to `undefined`, and `statusConfig[undefined]` is also `undefined` — `statusCfg.banner` (line 2467) then throws `Cannot read properties of undefined`, crashing the whole page. Reachable whenever a same-day `CANCELLED` delivery still has (or recently had) an open session, since `loadDeliveries`'s `activeDelivery` selection (line 2144) checks `hasOpenSession` first, `pickupDate === today` second, and neither fallback excludes `CANCELLED` the way `nonArchived` (line 2137-2138) already excludes `DELIVERED`/`COMPLETED`. Hit live when `DR-0020` was cancelled without also closing its stale open session. Worked around at the time by closing that session directly and moving `DR-0020`'s `pickup_date` off "today" — **fixed for real 2026-08-13**: added `CANCELLED` to `nonArchived`'s filter (line 2161, alongside the existing `DELIVERED`/`COMPLETED` exclusion). Build/lint-verified, then **live-verified** via a disposable Playwright check (`scripts/repro-cancelled-crash-fix.mjs`): temporarily set the already-cancelled `DR-0021`'s `pickup_date` back to today (recreating the exact crash scenario) and loaded the Driver portal — zero page errors, correctly showed "No deliveries for today" instead of crashing or surfacing the cancelled delivery. `DR-0021` reverted back to its retired `pickup_date` afterward.
  - **Pi camera crash-loop found on physical hardware**: `Picamera2()` (`drowsiness_monitor.py` line 366) has no try/except around it, so a camera-detection fault (`IndexError: list index out of range` from `global_camera_info()` returning empty) crashed the whole process before the heartbeat/GPS/alert threads ever started (those only start after camera init succeeds, line 374-377) — 84+ silent `systemd` restart cycles observed, zero heartbeat or GPS during any of them. This is exactly Phase 9 gap #3, already flagged below in Next Steps (camera fault taking down GPS instead of degrading in isolation) — confirmed today as a real, reproduced failure mode, not just a hypothetical. The fix applied this session was on the physical/wiring side (camera reseated), not the code-level try/except Phase 9 still calls for — **that code fix is also still open.**
  - **`/dev/serial0` UART fix**: this unit's `/dev/serial0` resolved to `ttyS0` (the mini-UART, clock-coupled to the GPU/core clock and unreliable for continuous serial use) instead of `ttyAMA0` (the full hardware UART) because Bluetooth still held the good UART — `enable_uart=1` was already set in `/boot/firmware/config.txt` but `dtoverlay=disable-bt` was missing entirely. Added it (`[all]` section), confirmed `/dev/serial0 -> ttyAMA0` after reboot. `hciuart.service` (the older mechanism for freeing the UART from Bluetooth) doesn't exist on this Debian 13/trixie image — the `dtoverlay` alone was sufficient, no separate service to disable. Not yet folded into `pi/README.md`'s setup instructions.
  - **Wrong baud rate + wrong NMEA sentence prefix, found by comparing against a working reference script**: even after the UART fix, `gps_reader_thread` received zero NMEA sentences. A separate diagnostic script (`gps_test.py`, not part of this repo) successfully locked a GPS fix on the same wiring using `BAUD_RATE = 38400` and matching `$GNGGA`/`$GPGGA`, versus `drowsiness_monitor.py`'s hardcoded `baudrate=9600` and `line.startswith("$GPRMC")` only (no `$GN`-prefixed check at all — modern multi-constellation modules commonly prefix `$GN`, not just `$GP`). Fixed: baud rate now `DRIVEWISE_GPS_BAUD_RATE` env var (default `38400`, overridable per-unit, same pattern as `DRIVEWISE_VIBE_MOTOR_PIN`), and `parse_gps_nmea` now also accepts `$GNRMC` alongside `$GPRMC`. Deployed to the physical Pi remotely (over `nano`+heredoc paste, since the unit was with a classmate on a different network entirely — `python3 -m py_compile` used to verify the paste landed cleanly before restarting the service).
  - **Confirmed working end-to-end after both fixes**: `[GPS] receiving NMEA data from module` → `[GPS] satellite fix acquired: ...` → `[GPS] upload confirmed working (...)` all observed live in `journalctl`, and the Driver portal's Live Navigation map (`DR-0021`) showed the real live position updating from actual `gps_logs` Realtime inserts — the first real (non-simulated, non-`service_role`-inserted) GPS coordinate this project has gotten all the way from a physical satellite fix through to the map.
- **Phase 9 gap #1 (Paused-trip Start Trip guard) built, deployed, and verified 2026-08-12** — `driver-trip`'s `start-trip` now also rejects when the driver has a _different_ delivery sitting Paused (past `ASSIGNED`, no open Active session), not just an Active one (`409`, curl-confirmed against the deployed function using `D002`'s real session token, cross-delivery). `DriverDeliveries.jsx`'s Start Trip failure alert now surfaces the real backend error message instead of a generic one. See `09_EDGE_CASES.md`'s gap #1/Progress section for the full two-round verification writeup (this had previously only been logged there, not here — folded in 2026-08-14 to keep the two docs in sync).
- **Phase 9 gap #2 (device reassignment while Active) fixed and click-tested live 2026-08-12** — the Admin "Edit Device" UI (`EditDeviceModal.jsx`) previously wrote a device's `plate_number` directly with no check for whether that device currently had an Active session, violating `PROJECT_CONSTRAINTS.md`'s "one active device assignment per truck" rule. Decision: block outright, not warn-and-allow — `performUpdate` now queries `sessions` for an Active row on `device_id` before any reassignment away from the device's current plate, rejecting with "This device is powering an active trip — end or pause the trip before reassigning it." Also sidesteps a related display-corruption case: `08_REALTIME_DASHBOARD.md`'s Supervisor Dashboard resolves Device/Trip state per truck via a live `devices.plate_number` join, so a live reassignment away from a truck with an Active session would have made that truck's dashboard entry read "no device" even though the original Pi was still reporting telemetry against its still-open session — blocking reassignment outright makes that unreachable too. Click-tested live 2026-08-12 against a real Active session (`DR-0011`/`D002`/device `DV-1114`, disposable fixture, fully reverted afterward): reassigning `DV-1114` to a different truck while Active was correctly rejected with the expected error. See `09_EDGE_CASES.md`'s gap #2 for full detail (this had previously only been logged there, not here — folded in 2026-08-14).
- **Stuck-delivery-status gap (found while building gap #1) fixed and curl-verified 2026-08-12** — `DriverDeliveries.jsx`'s "Start Pickup" flow called `update-driver-delivery` (advances `delivery_requests.status` to `OUT_FOR_PICKUP`) _before_ `start-trip`; if `start-trip` then rejected for any reason (including gap #1's new guard, which fires far more often than the pre-existing case it originally guarded against), the delivery was left showing an advanced status with no Session behind it — the same stuck-delivery shape as the `D002`/`DR-0015` incident, but reachable on a normal, expected rejection instead of a rare fluke. Fixed by reordering `advanceStage`'s `FOR_PICKUP` branch: `start-trip` is now called first, while status is still `ASSIGNED` (which it already accepts), and `update-driver-delivery` only runs after `start-trip` succeeds. Curl-verified end-to-end 2026-08-12 (`DR-0011`/`DR-0014`, `D002`): the second delivery's status was confirmed to have stayed `ASSIGNED`, never advanced, when its `start-trip` call was correctly rejected. See `09_EDGE_CASES.md`'s Progress section for full detail (this had previously only been logged there, not here — folded in 2026-08-14).
- **Phase 9 gap #3 (camera-fault isolation) fixed 2026-08-14** — `pi/drowsiness_monitor.py`'s `Picamera2()` init (previously no try/except, ahead of the heartbeat/GPS thread starts — the root cause of the 84+-cycle crash-loop found on physical hardware 2026-08-13, see that entry above) is now wrapped in try/except: on failure it sets `camera_available = False`, logs the error, and lets the process continue instead of crashing before the threads ever start. Heartbeat/GPS/alert threads now start unconditionally regardless of camera outcome. The main loop's detection block checks `camera_available` (alongside the existing Paused-session check) and skips frame capture/detection entirely when the camera is down, rather than calling `picam2.capture_array()` against a `None` camera. See `09_EDGE_CASES.md`'s gap #3 for full detail. Build-verified only (`python -m py_compile pi/drowsiness_monitor.py`) — **not yet re-deployed to or re-tested on physical hardware. This is now the only remaining open item in Phase 9.**
- **Traffic-aware live navigation, 2026-08-13** (`11_ROUTE_COMPARISON.md`'s "Traffic-Aware Suggested Routes" section, decided the same day but previously unbuilt). Added `drivingOptions: { departureTime: new Date(), trafficModel: 'bestguess' }` to `LiveNavigationMap`'s single `computeRoute()` request (`DriverDeliveries.jsx`) — the one function backing both the initial route and every reroute-on-deviation recompute, so `DirectionsService` now actually routes around currently-predicted congestion (not just distance/time) on every call, not only at mount. Scoped to live Driver navigation only, per explicit user decision — the separate not-yet-built `suggested_route` (Phase 11 persistence) is untouched by this change. `TrafficLayer` (visual-only congestion overlay, no routing effect) was considered and not added, since the ask was specifically about the route itself avoiding congestion, not displaying it. Build/lint-verified (`npm run build`, `eslint`); not yet live-verified against real traffic conditions (would require comparing a `drivingOptions`-on vs -off route through genuinely congested roads at request time, not something a disposable test fixture can force).
- **Phase 12 — Rest Stop Recommendations, built and live-verified 2026-08-13/14** (`IMPLEMENTATION/12_REST_STOP_RECOMMENDATIONS.md`, all design decisions already locked the same day this doc was written — see that file). Ephemeral, client-side only, `DriverDeliveries.jsx`: recommends a rest stop once either 200mi (321.9km) driven or 2 continuous driving hours is crossed since Trip start, summed across all of the Trip's Sessions (not reset per-Session, not reset on Pause/Resume — only a genuinely new Trip, i.e. a different `active.id`, clears it), one-shot (never re-fires once shown or dismissed), no persistence (no new table/column, `alerts` untouched), no location lookup (pure threshold, per the doc's explicit scope decision).
  - **Distance**: new `distanceMeters` haversine helper (plain JS, no `google.maps` dependency — mirrors `SupDashboard.jsx`'s existing helper of the same name/shape exactly, deliberately not reused directly since it lives in a different file with no shared utils module). Prior (already-closed) Sessions' distance is summed once per Trip via a `sessions`+`gps_logs` query (`status = 'Completed'`, grouped by `session_id`, consecutive-point haversine within each session only — mirrors the existing per-Session mileage precedent from `03B_PAUSE_AND_RESUME_TRIP.md`/`07_END_TRIP.md`, deliberately not summing across a Pause gap as driven distance). The current Active Session's distance accumulates incrementally off the same `gps_logs` Realtime subscription `LiveNavigationMap`'s own live-position marker already uses, piggybacking on it rather than a second subscription.
  - **Time**: current Session's own `start_time` (fetched fresh per Session, not trusted from `get-driver-deliveries`' response shape) plus prior Sessions' `session_duration` sum — matches the doc's explicit reasoning that driving time should track actual driving, not wall-clock Trip duration, since Pause already closes a Session.
  - **Check cadence**: fires on every `gps_logs` Realtime tick (piggybacked, no extra polling) plus a 60s backup `setInterval` purely for the time half of the threshold, since a GPS dropout (`09_EDGE_CASES.md`'s "GPS unavailable" — Trip/heartbeat keep going regardless) would otherwise leave the tick-driven check with nothing to fire on even after 2 hours genuinely elapse.
  - **One real ordering bug found and fixed before it shipped**: the threshold-check function was originally declared _after_ the `gps_logs` position effect that calls it — valid JS (closures over a later-declared `const` in the same function body work fine at runtime once effects actually fire, since effects don't run until after the full render completes), but `eslint-plugin-react-hooks`'s `react-hooks/immutability` rule correctly flagged it anyway: that earlier effect's closure would have captured whichever version of the check function existed at ITS setup time, never seeing later-reassigned reads correctly across the effect's own re-runs. Fixed by moving the threshold-check function and its two setup effects (prior-session totals, current-session start time) above the `gps_logs` position effect instead of relying on it working by accident.
  - **Live-verified 2026-08-13/14** via a disposable fixture (`DR-0023`, driver `D002`, truck `AAA 1111` — cancelled/cleaned up afterward, same pattern as `DR-0020`-`22`): rather than waiting 2 real hours, inserted one fake `Completed` prior Session with `session_duration: 7560` (2.1hr, already past threshold) directly via `service_role`, started a real trip via `driver-trip`, inserted one `gps_logs` row to trigger the seed-fetch check path, and loaded the Driver portal (`scripts/repro-reststop-fix.mjs`, kept as reusable disposable tooling). **Confirmed**: banner appeared immediately on load (screenshot-verified — sky-blue banner, coffee icon, distinct from the amber status banner above it), and clicking its dismiss button correctly hid it. Also confirmed via the same script that the seed-fetch path genuinely gates on `gps_logs` existing for the session — a session with zero GPS rows and no elapsed time on the 60s backup timer yet does not show the banner (this was actually hit first, as a false failure in the test script before adding the seed GPS row — not a product bug, just clarified the real check-cadence behavior described above).
- **Dynamic Nearest-Dropoff Ordering, built 2026-08-14** (`02B_MULTI_STOP_DELIVERIES.md`'s "Dynamic Nearest-Dropoff Ordering" — decided and built same day). Replaces the previous fixed customer-entered visiting order: after pickup, the driver is now routed to whichever remaining dropoff (`dropoff_location` or a `stops` entry) is nearest their current position, re-evaluated fresh each time a completion changes what's still remaining — confirmed with the user as greedy nearest-neighbor re-evaluation, not a one-time reorder computed once at pickup.
  - **`DriverDeliveries.jsx`**: new module-level `nearestDropoffOrder(referencePos, candidates)` helper (greedy nearest-neighbor). `activeNavTarget`/`activeNavStops` (feeding `LiveNavigationMap`) and the status banner's current-dropoff text now both derive from this, computed off `livePosition || activeNavOrigin` against the not-yet-completed candidates on every render — no extra effect needed, since it naturally recomputes whenever the existing `delivery_requests` Realtime subscription updates `active.stops`/`active.dropoffCompletedAt`.
  - **`admin-users`' `complete-dropoff`/`complete-stop`**: `isFinal` was positional (`stops.length === 0`, `stopIndex === stops.length - 1`), which silently assumed array order was visiting order — no longer true once routing is dynamic. Fixed to "are all OTHER chain items already completed." Deployed.
  - **`HelperDeliveries.jsx`**: new `livePosition` state + `gps_logs` Realtime subscription (Helper has no GPS device of their own, reads the truck's stream — same one the backend's proof-location check and the Driver's own nav already use). Delivery Chain list now shows a "Next" badge on whichever item matches this ordering — purely informational, does not change which items are `actionable` (completion order is still unenforced server-side, unchanged).
  - Build/lint clean on all three files; `deno check` on `admin-users/index.ts` shows only its pre-existing systemic `SupabaseClient` generic-mismatch noise (confirmed via line-by-line cross-check, not present near the new code).
  - **Live-verified 2026-08-14** via new `scripts/repro-nearest-dropoff.mjs` (kept in the repo, reusing `DR-0020`): driver nav's actual `DirectionsService` destination confirmed tracking live position (Stop 2 when nearest, Dropoff when nearest, not customer-entered order); backend `isFinal` confirmed correct across all three completion orders, including the specific case (completing the positionally-last stop first) that would have wrongly finalized under the old bug — real `DELIVERED` status reached only once every item was genuinely done, via a Helper session obtained through Supabase's magic-link admin API rather than a password reset. Fixture fully reverted, test photos deleted from Storage.
- **Chain-completion success toast added to `HelperDeliveries.jsx`, 2026-08-14** — `submitChainAction` previously closed the confirm modal silently on a successful photo upload with no confirmation it actually went through. New `chainSuccessNotice` state renders a floating, auto-dismissing (5s) toast on success, mirroring `DriverDeliveries.jsx`'s existing `completionNotice` toast exactly (same styling/z-index pattern): "`<Item> Confirmed` / Photo uploaded successfully." normally, or "Delivery Completed / ...trip has been closed out." when that action's `isFinal` came back true. **Live-verified 2026-08-14** via new `scripts/repro-chain-success-toast.mjs`: injected a real Helper browser session (magic-link technique, H003's password left untouched), clicked through a real Complete Dropoff with an actual file-picker photo upload, and screenshot-confirmed both the toast and the Delivery Chain list's "Next" badge (now on Dropoff 2) updated together correctly. This also serves as the first live click-test of the "Next" badge itself (previously build/lint-verified only) — both features confirmed working together in the same pass.
- **Supervisor's Completed-Delivery report ("Trip Details"/"DriveWise Report" tabs) wired to real data, 2026-08-14** — a groupmate flagged that alert timestamps, interpretation/analysis, and trip start/arrival timing "got lost" compared to the mock design. Root cause: `CompletedDeliveryReport` (in `SupDeliveries.jsx`, the panel a Supervisor sees opening a completed delivery) sourced both tabs entirely from `completed_delivery_reports`, a mock lookup keyed by a handful of fake delivery ids (`DEL-004`, etc.) — every real DB-backed completed delivery got `report === undefined` and those two tabs simply never rendered, falling back to Details + Quotation only.
  - New `buildRealTripAndBehaviorReport(delivery, sessions, alerts, gpsLogs)` builds the same shape from real `sessions`/`alerts`/`gps_logs`, fetched per delivery in `CompletedDeliveryReport` (mirrors `DriverPerformance.jsx`/`SupCrewProfile.jsx`'s established fetch pattern). Falls back to the legacy mock lookup only for the handful of ids that still have one (old demo fixtures); a real delivery with genuinely no sessions gets neither, same fallback as before.
  - **Real, not fabricated**: distance (haversine-summed from `gps_logs`, same per-session mileage calc `driver-trip`'s `pause-trip`/`end-trip` already does server-side, just recomputed for display since that result is never stored per-delivery), duration (summed `session_duration`), departed/arrived timestamps (`sessions.start_time`/`end_time`), a real per-item completion timeline sorted by actual `completedAt` (not a fixed dropoff-then-stops order, since that's no longer guaranteed — see the Dynamic Nearest-Dropoff Ordering entry above), alert counts/types/timestamps/history, per-session log, and risk/drowsiness tiers (reusing this file's existing `DB_ALERT_TYPE_LABELS`/`DB_ALERT_TYPE_SEVERITY` constants, already established elsewhere in this same file for live in-transit monitoring). The narrative "DriveWise Report" paragraph is a real computed summary templated from the actual numbers (mirrors `DriverPerformance.jsx`'s Insight box), not an invented AI narrative.
  - **Deliberately omitted, not fabricated** (per explicit user decision — no real data source exists anywhere in the schema for these): rest stops actually taken, waiting/idle time per leg, live camera/vibration/audio device-state history, and exact on-time/late-by-N-minutes (schedule fields exist but comparing them reliably against actual arrival was judged too fragile to present as precise). ~~The Route Deviation Report tab (planned-vs-actual map) is explicitly left out of this pass — needs the separate, schema-gated Phase 11 work (`11_ROUTE_COMPARISON.md`'s `suggested_route` persistence), not approved yet; it still shows only for the legacy mock reports that already carry a `routeDeviation` field.~~ Phase 11 work approved and built 2026-08-14, see below — this tab now shows real data too, for any delivery with a saved `suggested_route`.
  - **Real bug found and fixed while building this**: `Map` is imported from `lucide-react` for `REPORT_TABS`' route-tab icon, shadowing the global `Map` constructor within this file's module scope — `new Map()` inside the new builder was silently invoking the icon component as a constructor and throwing `"Map is not a constructor"`, which made `realReport` stay `null` forever with no visible error (the tabs just silently stayed on Details/Quotation). Fixed by using a plain object instead of `Map` for the gps_logs-by-session grouping.
  - Build/lint clean (one pre-existing, unrelated `setState`-in-effect error elsewhere in this same large file, confirmed via lint diff to predate this change). **Live-verified 2026-08-14** by temporarily flipping `DR-0021` (a real hardware-GPS test fixture from 2026-08-13 with 3 real sessions, 37 real alerts, 71 real `gps_logs` rows) to `COMPLETED` status, viewing it as a Supervisor (magic-link session, no password reset), and reverting the status back to `CANCELLED` afterward. Screenshot-confirmed both tabs render real data end-to-end — correct alert counts/types/history/session log/analysis paragraph on DriveWise Report, correct real timestamps/crew/timeline on Trip Details, and the on-time badge/waiting-idle rows/device-status row all correctly absent (no fabricated data).
  - **Follow-up round, same day, three more fixes from live feedback while reviewing this on a real screen:**
    1. **"Trip Timeline" added to the Delivery Request Details tab itself** (the screen a Supervisor actually opens by default, not just the separate Trip Details tab) — shows the same real, per-event-sorted completion timeline (Assigned to Crew, Pickup Trip Started off the driver's real `sessions.start_time` the moment Start Pickup was clicked, each Dropoff/Stop Completed off its real `completedAt`, Delivery Completed) directly under the Location section. Extracted the existing timeline JSX into a shared `TripTimelineList` component so both tabs render identically from the same data, and threaded `report?.trip?.timeline` into `DeliveryRequestDetails` as a new prop (only populated inside `CompletedDeliveryReport`, not `CancelledDeliveryDetails`/`IssueDetailView`'s other two call sites of the same component).
    2. **Raw "lat, lng" coordinates showing instead of resolved addresses** in the new Trip Details tab's Route line and Trip Stops list (`buildRealTripAndBehaviorReport` used `delivery.pickupAddress`/`deliveryAddress`/`stop.location` directly, unlike the pre-existing Location section which already reverse-geocodes via `useResolvedAddress`). Fixed by changing the builder to return raw `pickupLocation`/`dropoffLocation` instead of a pre-joined `route` string, and adding a new `ResolvedText` component (wraps `useResolvedAddress` so it can be called once per list row without violating the rules of hooks inside a `.map()`) used for the Route line and each Trip Stop. Legacy mock reports (which already set a pre-formatted `t.route` string) are unaffected — `t.route ? t.route : <ResolvedText .../>` keeps backward compat.
    3. **`face_not_detected` no longer counts toward the Driver Risk Level/Drowsiness Level classification** — per explicit user feedback, it's not a real drowsiness signal (same reasoning `06_DROWSINESS_ALERT_PIPELINE.md` already used to exclude it from the driver-facing audio alert). `riskLevel`'s count threshold and the analysis paragraph's "most frequently X" callout now both compute off a new `drowsinessAlertCount` (all alert types except `face_not_detected`), while the `Total Alerts` metric tile still honestly shows the full count including it — verified live: DR-0021's 37 total alerts (32 of them `face_not_detected`) now correctly reads "5 drowsiness alerts this trip / High Risk" (the 5 real `prolonged_eye_closure` alerts alone still legitimately warrant High Risk) instead of the previous "37 alerts this trip" figure that was mostly detection noise.
    4. All three re-verified live in the same DR-0021 pass (screenshot-confirmed) — reused the earlier temporary `COMPLETED`-then-revert-to-`CANCELLED` flip, no new fixture needed. Build/lint clean throughout.
- **Visual traffic layer added to Live Navigation, 2026-08-30** — reverses the "not added" call logged above under "Traffic-aware live navigation, 2026-08-13": `<TrafficLayer />` (from `@react-google-maps/api`) is now rendered inside `LiveNavigationMap`'s `<GoogleMap>` (`DriverDeliveries.jsx`, right after the `GoogleMap` opening tag, before the route `Polyline`), showing Google's live congestion coloring directly on the map. The traffic-aware *routing* behavior (`drivingOptions.trafficModel: 'bestguess'` on `computeRoute()`) already added 2026-08-13 is unchanged — this is purely the additional visual overlay that entry deliberately skipped. Build/lint-verified (`npm run build`, `eslint` — no new errors/warnings in `DriverDeliveries.jsx`). **Live-verified 2026-08-30** via new `scripts/repro-traffic-layer.mjs` (kept as reusable disposable tooling, reuses the DR-0020 fixture): patched `google.maps.TrafficLayer` the same "replace the whole class once it appears on `window.google.maps`" way `gps-route-capture.mjs` already does for `DirectionsService`, confirmed it's actually instantiated and `setMap()`-attached to the real map instance, zero page/console errors (one unrelated "Vector Map falling back to Raster" warning, a headless-Chromium/no-GPU artifact of the `mapId` option, present regardless of this change). Can't assert real congestion coloring renders correctly, since that depends on Google's live traffic data for real roads at request time — not something a script can force.
- **13-hour max travel-time cap added to Customer delivery request submission, 2026-08-30** — per explicit user decision. Previously, nothing prevented a Customer from booking a delivery whose route would take longer than 13 hours; only the existing Luzon service-area check (`src/lib/serviceArea.js`, unchanged) constrained pickup/dropoff locations, and Luzon alone spans routes over 20 hours end-to-end (confirmed via a real Directions API call, Pagudpud to Sorsogon: ~20h25m in traffic, 1,093km). `CustomerRequestDelivery.jsx`'s `handleSubmit` now computes the direct pickup->dropoff drive time via the same traffic-aware `DirectionsService` request shape every other route computation in the app already uses (`drivingOptions: { departureTime: new Date(), trafficModel: 'bestguess' }`, `11_ROUTE_COMPARISON.md`) — added via a new `useJsApiLoader(GOOGLE_MAPS_LOADER_OPTIONS)` call, since this page previously only used Leaflet, never the Google Maps JS SDK. Runs after the existing Luzon check, before the `delivery_requests` insert; rejects with a clear error message if the estimate exceeds `MAX_TRAVEL_HOURS` (13). Scoped to the direct leg only — `formData.stops` are saved as plain text (never geocoded at submit time), so multi-stop routes aren't factored into the estimate. **Confirmed this is the only delivery-creation path in the codebase** (grepped all of `src/pages` for `.from("delivery_requests").insert(`) — Admin/Supervisor never insert new `delivery_requests` rows anywhere, only update/quote existing ones, so extending the Luzon check to a "Sup/Admin creation flow" (raised as a possible gap during this same session) turned out to be a non-issue: no such flow exists to extend. Build/lint-verified; not yet live-UI-verified with a full Customer form submission (would need a scripted walkthrough of the whole multi-field booking form, not just this check in isolation) — the underlying `DirectionsService` call shape is the same one already live-verified working error-free via `repro-traffic-layer.mjs` above.
- **Per-stop dropoff time added to Customer booking form, 2026-08-30** — per explicit user decision, see `02_BOOKING_AND_TRIP_CREATION.md`'s "Per-Stop Dropoff Time" for the full writeup. `formData.stops` changed shape from an array of plain strings to an array of `{location, dropoffTime}` objects (`CustomerRequestDelivery.jsx`); each additional dropoff in the form now has its own optional Time input next to its location field. Validated with a new `getStopTimeError` (`lib/deliveryOptions.js`) reusing the exact rule `getScheduleErrors` already applies to the main dropoff time (must be later than the pickup window) — deliberately no cross-stop chronological check and no travel-time estimate between stops, per explicit user scoping. Persisted on `delivery_requests.stops` (`jsonb`) as `{"location": "...", "dropoffTime": "HH:MM"}`, `dropoffTime` omitted when left blank. **Confirmed additive, not breaking**: grepped every other reader of `stops` (`DriverDeliveries.jsx`, `SupDeliveries.jsx`, `HelperDeliveries.jsx`, `CustomerDeliveries.jsx`) — all already treat each entry as an object via `stop.location`, none would choke on an extra field; `stops` is `jsonb` with no fixed shape at the DB level, and this is the second time it's gained a new per-entry field this way (`completed`/`completedAt`/`photoUrl` first, 2026-08-12). Build/lint-verified; not yet live-UI-verified.
  - **Follow-up, same day**: main Drop Off Time moved from the Schedule section to sit beside Drop Off Location in the Location section, matching the per-stop Time-beside-location layout above (Drop Off Date stays in Schedule, unchanged) — purely a layout move, `formData.dropoffTime`'s handling is otherwise unchanged.
  - **Follow-up, same day, real gap found and confirmed, not yet acted on**: user asked whether per-stop dropoff times could create driver-routing confusion (e.g. a farther dropoff requested for an urgent time, a nearer one for later) — confirmed real: `nearestDropoffOrder` (`DriverDeliveries.jsx:2649`, powers both `PlannedRouteMap` and `LiveNavigationMap`) ranks purely by physical distance from the driver's current position and never reads any time field, and per-stop `dropoffTime` isn't even surfaced to Driver/Supervisor anywhere yet (wired into the Customer form and DB storage only). Discussed three options (surface time to Driver/Supervisor without changing routing; make routing time-aware; leave purely informational) — recommended surfacing-only as the low-cost first step, since real time-aware routing is nontrivial algorithmic work not yet justified by confirmed need. **Not implemented** — no decision made yet, flagged here so it isn't lost.
- **13-hour cap rejection changed from inline text to a modal, 2026-08-30** — per explicit user request ("dont use alert window ... use a modal"). Previously `setSubmitError` rendered the rejection as the same small red inline-text banner every other form error uses; now a dedicated `TravelTimeExceededModal` (new component in `CustomerRequestDelivery.jsx`, same `createPortal`/scroll-lock pattern `LocationPickerModal` already uses) interrupts with "Travel time exceeds our 13-hour limit... Try splitting this into separate delivery requests instead," dismissed via a single "Got it" button. Reasoning logged inline: this rejection means "restructure into a separate delivery," not "fix a typo and resubmit," so it reads better as an interrupting dialog than a skimmable text line. New `travelTimeModalHours` state (`null` = hidden, a number = shown, holding the estimated hours for the message) replaces the old `setSubmitError(...)` call in this one rejection branch only — every other validator in this form (Luzon, budget, schedule, per-stop time, etc.) still uses the existing inline-banner pattern, unchanged. Build/lint-verified; not yet live-UI-verified.
  - **Superseded later the same day** by the Delivery Time-Planning Calculation entry below — `travelTimeModalHours` (a single number) became `travelTimeModalSchedule` (a full `simulateSchedule()` result), and the modal's message now shows the Day1/Day2/Total breakdown instead of one "about X hours" line. The interrupting-modal design itself (vs. inline banner) is unchanged.
- **Delivery Time-Planning Calculation, 2026-08-30** — per explicit user decision, full spec (Same-Day/Two-Day toggle, sequential schedule simulation with rest/meal breaks, combined 13-hour total). **Replaces** (not extends) the single-leg-only 13-hour cap above — two different 13-hour gates with different math would contradict each other. See `02_BOOKING_AND_TRIP_CREATION.md`'s "Delivery Time-Planning Calculation" section for the full writeup. Summary:
  - New `src/lib/scheduleSimulator.js` — pure logic, single source of truth for `PICKUP_LOADING_MINUTES`/`UNLOAD_MINUTES`/`REST_INTERVAL_HOURS`/`REST_MINUTES`/`MEAL_MINUTES`/`MAX_TOTAL_DELIVERY_HOURS` (13), plus `LUNCH_THRESHOLD_MINUTES`(12:00)/`DINNER_THRESHOLD_MINUTES`(18:00) — single crossing instants per explicit user clarification ("no specific time... just add it to be calculated"), not fixed windows; a day's start time already past a threshold means that meal is never added that day. `simulateSchedule({mode, day1StartTime, day2StartTime, legTravelSeconds})` returns only `{day1TotalSeconds, day2TotalSeconds, totalSeconds, exceedsLimit}` — no itemized event list surfaced, per explicit user scope ("just the 3 totals... no itemized breakdown"). Two-Day's Day 1 is a fixed 30-minute pickup/loading block (no depot/origin in the data model, so nothing to drive on Day 1) per explicit user decision; Day 2 runs the full per-leg walk. **Verified**: hand-traced 5 scenarios (rest-boundary crossing, lunch triggering mid-unload, a start time already past lunch correctly not re-adding it, a >13h two-day total correctly flagged, an exact-2-hour leg) via a throwaway Node script — all matched independent by-hand arithmetic before wiring into the page.
  - New `delivery_mode` column (`text null`, `supabase/migrations/20260830150000_delivery_requests_delivery_mode.sql`) — the `"SAME_DAY"`/`"TWO_DAY"` toggle choice only, not the computed breakdown (stays client-side, recomputed on demand). **Live-verified** via direct REST select immediately after push.
  - Day 2's start time reuses `dropoffDate`/`dropoffTime` (no new field) — Two-Day mode's dropoff-time field relabels to "Day 2 Start Time" and hides its "to" (window-end) input.
  - `getScheduleErrors` (`lib/deliveryOptions.js`) gained an optional `deliveryMode` param: Same-Day requires `dropoffDate === pickupDate`, Two-Day requires it strictly later. Confirmed via grep this function has exactly one caller in the whole codebase (`CustomerRequestDelivery.jsx`), so no other page needed updating.
  - **Real gap fixed**: stops were never geocoded before this (`handleStopChange` discarded the lat/lng `LocationInput` already emitted) — fixed by capturing it live plus reusing the already-existing `useResolvedStopCoords` (`lib/forwardGeocode.js`) as the Photon fallback, the same hook `DriverDeliveries.jsx`/`HelperDeliveries.jsx` already use for their own stop-coordinate resolution — not rebuilt. Stop `lat`/`lng` stays client-side only, never persisted.
  - New `estimateLegDurations` (replaces `estimateTravelSeconds`) — one multi-waypoint `DirectionsService` call for the whole `[pickup, dropoff, ...stops]` chain, `optimizeWaypoints` left at default `false` so leg order matches the customer-entered stop order (each leg maps 1:1 to an unload event in the simulator).
  - Debounced (~600ms) live-preview effect shows a "Schedule Summary" card (Day 1 / Day 2 if applicable / Total only) after the stops section. Every `setState` call inside it is deferred past a microtask or the debounce's `setTimeout` (never synchronous in the effect body) — same established pattern `useResolvedStopCoords` itself already uses for the `react-hooks/set-state-in-effect` rule.
  - **App-wide impact check, done before writing code, not assumed**: grepped all of `src/` confirming `getScheduleErrors`/`estimateTravelSeconds`/`MAX_TRAVEL_HOURS`/`TravelTimeExceededModal` are used exclusively inside `CustomerRequestDelivery.jsx` (nothing else could break from changing/removing them); confirmed (again) this is the only delivery-creation path in the app; confirmed stop `lat`/`lng` never reaches the persisted `stops[]` shape so nothing reading `stops[]` elsewhere is affected; grepped `scripts/` for anything depending on what was removed (nothing found).
  - Build/lint-verified (`npm run build`, `eslint` — zero new errors; the one pre-existing unrelated `CustomerRequestDelivery.jsx` lint error, predating this session, is untouched). Not live-UI-verified end-to-end (no Customer test account exists in this repo's scripts).
  - **Same-day follow-up: mode made fully automatic, not a customer-clicked toggle** — per explicit user request ("it should be automatic depending on the pickup date and drop off date"). `deliveryMode` changed from `useState` to a derived value: `"TWO_DAY"` whenever `dropoffDate > pickupDate`, `"SAME_DAY"` otherwise (including before dates are filled in, matching the pre-existing pickup→dropoff date auto-fill default). `handleDeliveryModeChange` removed entirely — the two Same-Day/Two-Day boxes in the form lost their `onClick` and became plain visual indicators of whichever mode the current dates imply. `getScheduleErrors`'s `deliveryMode` param (added earlier the same day) was removed again, not just left unused — mode can no longer disagree with the dates by construction, so the mode-aware date-relationship checks it added were dead code the moment mode became derived. Build/lint-verified, zero new errors.
    - **Further follow-up, same day**: the two Same-Day/Two-Day indicator boxes still visually read as clickable buttons even with `onClick` removed (large filled/bordered pill shapes, per user screenshot feedback: "it's still a button") — replaced with a single small status badge ("Delivery Mode: ● Same-Day — pick a later Drop Off Date to switch to Two-Day"), which can't be mistaken for a control regardless of interactivity. Build-verified.
  - **UI bug fixed, same day, two attempts** — the Drop Off Time window ("Open From – To") overflowed its card. First attempt (below) was wrong: assumed the outer row (`LocationInput` beside the time block) wasn't wrapping, and changed it to `sm:flex-row`/`sm:w-52`. User reported it was **still overflowing on a wide desktop card**, which disproved that theory (`sm:flex-row` is active well above mobile widths, so nothing had actually changed there). Re-diagnosed properly: the real cause was the fixed `w-52` (208px) time-window box being physically too narrow to fit two native `<input type="time">` fields (each has a real rendered minimum width from the browser's own clock-icon/spinner chrome) plus a "to" label — the two time inputs overflowed *their own* 208px box regardless of how the outer row was laid out, which is exactly what the follow-up screenshot showed (Drop Off Location rendering fine, only the time fields spilling past the card edge).
    - Fix: outer row is `flex flex-wrap items-end gap-2` (wraps based on actual available space, not a fixed breakpoint) with `LocationInput`'s wrapper at `min-w-[220px] flex-1`. The time-window box dropped its fixed `w-52` entirely — now `w-full sm:w-auto sm:shrink-0`, sized by its own content — and each time `<input>` got an explicit `sm:w-[8.5rem]` (136px, comfortably fits a native time picker) instead of `w-full`. The inner time-input row also gained `flex-wrap` as a safety net. Applied identically to both the main Drop Off block and the per-stop block.
    - **Verified this time, not just asserted**: built a standalone Tailwind reproduction of the exact structure/classes and screenshotted it via Playwright at three widths — 1435px (matching the reported card width), 600px, and 375px (mobile) — confirmed zero horizontal overflow (`scrollWidth === clientWidth`) at all three, with the time block sitting inline at the two wider sizes and wrapping to its own line at mobile width. Build-verified; still not live-UI-verified inside the real app (no Customer test account in this repo's scripts) — the standalone reproduction is a structural proxy, not the actual page.
- **Two real bugs found and fixed during a requested review pass, 2026-08-30** — user asked "check if there is any errors in what we implemented" for the delivery time-planning feature. Both were traced by hand through the actual field logic, not guessed:
  1. **Stale `dropoffTimeEnd` could permanently block Two-Day submission.** `dropoffTimeEnd` is only rendered/editable in `SAME_DAY` mode, but nothing cleared its state when mode auto-switched to `TWO_DAY` (e.g. customer fills a same-day window, then picks a later Drop Off Date). `getDropoffWindowError` doesn't know about mode, so it kept comparing the leftover value against the repurposed "Day 2 Start Time" field — easily produced a "window end must be later than start" error pointing at a field no longer present in the UI, a genuine dead end with no way to fix it, and would also have silently written the stale value to `dropoff_time_end` on any submission that slipped past (its `|| null` fallback only catches an *empty* string, not a stale non-empty one). Fixed two ways: (a) a new effect (`CustomerRequestDelivery.jsx`) clears `dropoffTimeEnd` to `""` the moment `deliveryMode` becomes `"TWO_DAY"`; (b) defense in depth at both `getDropoffWindowError` call sites (`handleChange`, `handleSubmit`) — skip the check entirely in Two-Day mode rather than relying on the effect alone to have already run.
  2. **Per-stop time validation broke across the Two-Day boundary.** `getStopTimeError` unconditionally compared a stop's `dropoffTime` against the pickup window's end — correct in Same-Day mode, but in Two-Day mode stops happen on Day 2, a different calendar day, so comparing clock-time-only strings across days is meaningless (a legitimate 09:00 Day-2 stop time would wrongly compare as "before" a 17:00 Day-1 pickup window and get rejected). Fixed by adding an `isTwoDay` param to `getStopTimeError` (`lib/deliveryOptions.js`) that skips just that specific sub-check in Two-Day mode — the half-filled-window and end-after-start checks still apply regardless of day, since neither depends on which day it is. All three call sites updated to pass it.
  - Both fixes build/lint-verified, zero new errors. **Not live-UI-verified** (no Customer test account in this repo's scripts) — confirmed by tracing the exact field/state logic by hand (documented above) rather than clicking through the real form.
  - **Also verified as part of this review**: all local Supabase migrations are applied on the linked remote project (`npx supabase migration list` — every `local`/`remote` pair matches, nothing pending) and the new `dropoff_time_end`/`delivery_mode` columns are confirmed live via a direct REST select — **nothing further needs to be run manually**; every migration this session was pushed directly rather than left for the user to apply.
- **PWA "Add to Home Screen" support added, 2026-08-30** — user asked whether a driver could add the site as a mobile home-screen shortcut. Answer: yes, already possible with zero changes (any mobile browser can shortcut any URL) — but the project had no `manifest.json`, no properly-sized icons, and no standalone-display config, so the out-of-the-box result would have used the tiny favicon and opened with browser chrome still visible rather than feeling like a real app. Added, per explicit user request to build the proper version:
  - New icon set generated from the existing brand mark (`public/MLOGO.png`, 251×247, the same "mono logo" already used as the favicon and on `LandingPage.jsx` — not `public/favicon.svg`, which turned out to be an unused leftover asset, referenced nowhere in the app). No image-processing library available (`sharp` not installed) — rendered via a throwaway Playwright HTML reproduction instead (render the source PNG centered on a sized canvas, screenshot at the exact target resolution), same technique already used earlier this session to verify the dropoff-time-window overflow fix: `public/icon-192.png`, `public/icon-512.png` (both transparent background, logo at ~82% fill), `public/icon-maskable-512.png` (white background, logo at ~62% fill for Android's adaptive-icon safe zone), `public/apple-touch-icon.png` (180×180, white background, iOS ignores alpha transparency on touch icons). Real transparency on the first two confirmed by compositing over a magenta test background via Playwright, not just eyeballed (an image-viewer preview alone would show white regardless, since viewers render transparency against their own default backdrop).
  - New `public/manifest.json` — `name`/`short_name: "DriveWise"`, `start_url: "/"` (the app's real root route, confirmed via `App.jsx` — renders `Login` directly), `display: "standalone"`, `background_color: "#ffffff"`, `theme_color: "#059669"` (the emerald shade used throughout the actual UI, not the logo mark's own red/orange, since the theme color tints browser chrome to match the *app's* look, not one static asset), and the three icons above (`any`/`any`/`maskable` purposes).
  - `index.html` gained `<link rel="manifest">`, `<link rel="apple-touch-icon">`, a `theme-color` meta tag, and iOS-specific `apple-mobile-web-app-*` meta tags — iOS Safari ignores `manifest.json`'s `display`/`theme_color` on "Add to Home Screen" entirely and only respects these dedicated meta tags for standalone/full-screen behavior; Android Chrome reads the manifest instead but also honors `theme-color` for its own toolbar tint.
  - No service worker added — out of scope for what was asked (a proper installed-app *icon and standalone window*, not offline support); would be a separate, larger addition if ever wanted.
  - Build-verified: `npm run build` correctly copies every new `public/` asset into `dist/` (confirmed via directory listing), `dist/index.html`'s head matches source exactly, `manifest.json` parses as valid JSON. `eslint` unaffected (no JS files touched) — problem count unchanged from the pre-existing baseline. **Not live-device-verified** (no way to test an actual iOS/Android "Add to Home Screen" flow from this environment) — the icons/manifest are correct by inspection and the build pipeline is confirmed to ship them, but the on-device install experience itself wasn't clicked through.
  - **Real gap found on user device-testing, same day**: user tested on Android Chrome against the local dev server reached over WiFi (LAN IP, not `localhost`) — address bar still showed after "Add to Home Screen." Root cause confirmed, not a bug in the manifest/icons themselves: **Chrome only grants installable-PWA (standalone, no address bar) treatment over a secure context** — exactly `https://` or the literal origin `localhost`. A LAN IP reached over plain HTTP (`http://192.168.x.x:5173`, the only way a phone can reach a laptop's dev server over WiFi) is neither, so Chrome silently falls back to a plain bookmark-style shortcut that still opens with browser chrome, regardless of how correct `manifest.json` is.
  - **Attempted fix for local testing**: added `@vitejs/plugin-basic-ssl` (dev dependency) and updated `vite.config.js` to (a) apply it only when `command === 'serve'` (production builds, which get real HTTPS from actual hosting, are untouched) and (b) set `server.host: true` so the dev server binds to all network interfaces, not just `localhost`, so a phone on the same WiFi can reach it at all. `npm run dev` now serves over `https://<LAN-IP>:5173` (or next free port) with a locally-trusted self-signed certificate. Verified server-side: dev server startup log confirms both `Local: https://localhost:PORT/` and `Network: https://<LAN-IP>:PORT/`; `npm run build` still succeeds unaffected; `curl -k` confirmed `manifest.json`/`icon-192.png`/`icon-512.png` all serve `200 OK` with correct `Content-Type`. Killed a stale non-HTTPS dev server instance left running from earlier the same session so the user doesn't accidentally test the wrong (HTTP) port.
  - **This did NOT fully fix it, and the original claim above was wrong** — user tested it live: entered the HTTPS LAN URL, accepted the certificate warning, ran "Add to Home Screen," address bar still showed. Diagnosed further by asking what exact menu wording Chrome used: it said **"Add to Home screen," not "Install app"** — confirming Chrome never recognized the site as an installable PWA at all, self-signed cert or not. Root cause: **Chrome's PWA-installability check requires a certificate from a real trusted CA, not merely an encrypted connection the user manually approved** — clicking through the "not private" warning makes the connection usable, but does not make Chrome treat the origin as trustworthy enough to grant standalone/no-address-bar install treatment. This is a hard limitation of local self-signed-cert testing, not something fixable in app config.
  - **Next step identified, not yet taken by user choice**: a real trusted-CA HTTPS origin is required to see the actual standalone install behavior — either a quick tunnel (`npx localtunnel`, real cert, temporary public URL, but ephemeral and shows a one-time interstitial page) or actual deployment (Vercel/Netlify, zero-config for this static Vite/React SPA since Supabase is already hosted separately). Recommended deployment over a tunnel, since a driver installing a real shortcut only ever makes sense against a URL reachable from their own phone in the field, not this laptop's WiFi — a tunnel only answers "does the manifest technically work," not the real goal. **User explicitly deferred deployment for now** ("I dont want to deploy yet") — no hosting has been set up. The manifest/icons/meta-tag work above is already correct and complete; it will only be *visibly* verifiable as true standalone install behavior once real hosting exists, whenever that happens.
  - Local dev server's HTTPS config is left in place regardless (harmless — dev-only, doesn't affect `npm run build`) in case it's useful for other local-network testing later.
- **Drop Off Time changed from a single instant to a window (start + end), 2026-08-30** — per explicit user request ("it should be a window because it should show up as like 'Open from -- to --'"), same day as the per-stop dropoff time feature above, which this extends rather than replaces. See `02_BOOKING_AND_TRIP_CREATION.md`'s "Per-Stop Dropoff Time" section (its same-day follow-up) for full detail. Summary:
  - New `dropoff_time_end` column (nullable, mirrors `pickup_time_end`'s exact shape) — **deployed** via `supabase/migrations/20260830140000_delivery_requests_dropoff_time_window.sql`. New `getDropoffWindowError` in `lib/deliveryOptions.js` mirrors `getPickupWindowError`. Both fields required in the form now, UI label "Open From – To" beside Drop Off Location.
  - Each stop's `dropoffTime` also became a window (`dropoffTime`+`dropoffTimeEnd`) — `getStopTimeError`'s signature changed to take the whole stop object, now also rejecting a half-filled window and a window whose end isn't later than its start, alongside the pre-existing "must start after the pickup window" rule.
  - **Migration deploy required navigating two unrelated pre-existing pending migrations** (`20260830120000_harden_permissive_rls_policies.sql`, `20260830130000_harden_driver_default_assignments.sql`, real Supabase Advisor security fixes from earlier the same day, not written this session) — `supabase db push` applies all pending migrations in order, so pushing the new dropoff-window one would have silently pushed those two as well. Asked the user first rather than assuming; confirmed to push all three. Mid-push, discovered both security migrations were **already live** (their target policies existed, dropped ones didn't) but untracked in the CLI's migration history — same situation as the `suggested_route` migration once before (`STATUS.md`'s 2026-08-14 entry). Fixed via `npx supabase migration repair --status applied 20260830120000 20260830130000` (syncs the tracking table only, no SQL re-run), confirmed via `db push --dry-run` that only the new migration remained pending, then pushed it for real. **Live-verified** via a direct REST `select` on `delivery_requests.dropoff_time_end` immediately after (`{"dropoff_time_end": null}` on an existing row, as expected for a pre-existing row with the new nullable column).
  - Build/lint-verified (`npm run build`, `eslint` — no new errors in `CustomerRequestDelivery.jsx`/`lib/deliveryOptions.js`); not yet live-UI-verified with a full form walkthrough.
- **`DR-0021` repurposed as a permanent "real data" demo fixture for the Completed-Delivery report, 2026-08-14** — previously `CANCELLED`/retired (see the 2026-08-13 entries below); at the user's request (to show groupmates what the real Trip Details/DriveWise Report tabs look like), permanently set to `status: COMPLETED` with a real `completed_at` (`2026-08-13T15:01:24.659+00:00`, matching its last real session's `end_time`) and its stale `cancelled_by`/`cancel_reason`/`cancelled_at`/`cancelled_from_status` fields cleared. **This is a deliberate, permanent state change, not a temporary test flip** — unlike every other `DR-0021`-flipped-then-reverted mention in this file (all from testing earlier the same day), this one should stay `COMPLETED` going forward. It has 3 real sessions, 37 real alerts, 71 real `gps_logs` rows, and a full real Trip Timeline (Assigned → Pickup Trip Started → Delivery Completed) — no `stops`/multi-dropoff data, but Trip Details and DriveWise Report both render fully real data end-to-end. Screenshot-confirmed 2026-08-14 in the Supervisor portal's Completed Deliveries tab.
  - **Proof-of-delivery photos added the same day**, per follow-up user request: `src/layout/images/MarvelTrucks1.png`/`MarvelTrucks2.jpg` (real project assets, not fabricated content — stand-ins for a real driver-taken photo, chosen for a visually convincing demo rather than a blank placeholder) uploaded directly to the `delivery-proof-photos` bucket at `DR-0021/pickup.png`/`DR-0021/dropoff.jpg` via the Storage REST API (`service_role`, same pattern as this project's other manual fixture uploads). `pickup_photo_url`/`dropoff_photo_url` set to their public URLs, `dropoff_completed_at` set to match `completed_at`. This also means the Trip Timeline now shows two more real entries it couldn't before (`pickup_photo_url` presence drives the "Pickup Confirmed" row, `dropoff_completed_at` drives "Dropoff Completed"): Assigned → Pickup Trip Started → Pickup Confirmed → Dropoff Completed → Delivery Completed. Screenshot-confirmed the Proof of Delivery section renders both photos correctly.
  - **Real quotation added the same day**, per a further follow-up request (Quotation tab showed "No quotation was submitted"): inserted a real `delivery_quotations` row (`quotation_type: 'initial'`, `delivery_id: 'DR-0021'`) via the REST API with a fully internally-consistent breakdown (`distanceKm: 24.5` matching the Trip Details tab's own real computed distance; diesel/direct/indirect/operating totals and the 15% income all cross-check correctly, ₱17,968.75 proposed rate) — `id` deliberately omitted from the insert so Postgres's own `nextval()`-backed default generates it (`QTN-0021`), avoiding a future primary-key collision that manually picking the next id could have caused. `delivery_requests.approved_amount` (which the code reads for a "Final Approved Amount" banner) turned out not to actually exist on the live table — a pre-existing, unrelated schema gap, not something added or fixed here — so that banner doesn't show, but the Quotation tab itself renders the full real breakdown correctly regardless, sourced entirely from `delivery_quotations`. Screenshot-confirmed.
- **App-wide timezone display bug fixed, 2026-08-14** — a Supervisor asked why timestamps on the Completed-Delivery report "looked converted"/wrong. Root cause: every timestamp column is stored as UTC (`timestamptz`, per `DATABASE.md`'s "Timestamps use UTC" convention), but display formatting across the app was never actually converting to the business's real timezone (Asia/Manila, UTC+8) — a full codebase survey found **28 formatter functions/call sites across 19 files** doing one of two wrong things: (a) 8 functions regex-extracted the raw digit characters straight out of the UTC ISO string with no conversion at all, displaying the UTC clock reading mislabeled as local time (8 hours behind real Manila time), or (b) ~20 more used `new Date(...).getHours()`/`toLocaleString(...)` without an explicit `timeZone` option, which only happened to look correct because the dev/testing machines used so far are already set to Asia/Manila — not guaranteed for every viewer (e.g. a grader on a different machine).
  - New shared `src/lib/manilaTime.js` — the one place in the app that does real, explicit Asia/Manila conversion (`getManilaFields`, `formatManilaTimestamp`, `formatManilaShortDate`, `formatManilaShortTime`, `formatManilaDateTime`, `getManilaHour`, `getManilaWeekday`, `manilaTodayISO`), all built on `Intl.DateTimeFormat` with `timeZone: 'Asia/Manila'`. Every file's own formatter functions now either delegate to this module or have `timeZone: MANILA_TIMEZONE`/`timeZone: 'Asia/Manila'` added to their existing `toLocaleString`/`toLocaleDateString`/`toLocaleTimeString` calls — chosen over silently duplicating the same fix 28 times, since duplicated near-identical formatters disagreeing with each other is exactly how this bug happened unnoticed in the first place.
  - **Files touched**: `SupDeliveries.jsx`, `SupCrewProfile.jsx`, `DriverPerformance.jsx`, `HelperDeliveries.jsx`, `DriverDeliveries.jsx` (the 8 regex-based formatters, incl. two `localTodayISO` duplicates now routed through `manilaTodayISO()`), `SupDashboard.jsx`, `AdminDevices.jsx`, `SupAnalysisIndiv.jsx`, `AdminAnalysis.jsx`, `CustomerDeliveries.jsx` (incl. its own `localTodayISO`), `SupDeliveryCrew.jsx`, `SupTruckProfile.jsx`, `AdminTruckProfile.jsx`, `AdminHome.jsx`, `src/lib/deactivation.js`.
  - **Birthdate/date-acquired fields** (`SupDeliveryCrew.jsx`'s `getAgeFromBirthday`, `SupTruckProfile.jsx`/`AdminTruckProfile.jsx`'s `formatMonthYear`, `AdminHome.jsx`'s `calculateAge`) were switched to **UTC-anchored** getters instead of Manila-pinned — these are date-only values with no real time component, so UTC-anchoring (matching the pattern the five `*Profile.jsx` pages' `calculateAge`/`formatBirthdate` already used correctly) is the genuinely correct, timezone-independent approach, not just "another timezone to pin to."
  - **`SupDeliveries.jsx`'s `autoCompleteDelivered`** (mock-data-only auto-completion helper) was storing a pre-formatted locale string into `completedAt` instead of a real ISO timestamp — harmless today since it only runs against mock data, but a landmine if ever wired to real data (that field gets read back through `formatAlertTimestamp` elsewhere, which expects a parseable timestamp). Normalized to `new Date().toISOString()`.
  - Deliberately **not** touched: the five `*Profile.jsx` pages' (`DriverProfile.jsx` etc.) `calculateAge`/`formatBirthdate` — already correctly UTC-anchored, this was the reference pattern the other date-only fixes above were aligned to, not something that needed fixing itself.
  - Build clean project-wide. Full-project lint pass shows only pre-existing, unrelated errors (confirmed via diff on every touched file — none of my changes introduced a new lint error). **Live-verified 2026-08-14** two ways: (1) `DriverPerformance.jsx`'s "Last Completed Trip" against a real session (`start_time: 2026-08-14T08:34:44.976+00:00`) now correctly shows "4:34pm" (08:34 UTC + 8h), not the old "8:34am" (raw UTC digits); the hour-bucketing chart's "Peak Drowsiness Time" also shifted to a Manila-correct hour. (2) Re-verified on the exact screen the report came in on — `SupDeliveries.jsx`'s Completed-Delivery report (`DR-0021`, temporarily flipped to `COMPLETED` and reverted after, same fixture used for the report-wiring work above): "Pickup Trip Started" now reads "August 13, 9:18 pm" and "Delivery Completed" reads "August 13, 11:01 pm" — previously "1:18 pm"/"3:01 pm", an exact 8-hour shift confirming the fix.
- **Booking/Supervisor UI updated to stop implying stop order is a route, 2026-08-14** — closes the last open item from the Dynamic Nearest-Dropoff Ordering work above. `CustomerRequestDelivery.jsx`'s stops section gained a clarifying caption and its add-button text dropped "after Drop Off" (implied sequence); `SupDeliveries.jsx`'s `LocationSwitcher` (the request detail panel's location chip/map switcher) got the same caption when stops exist. Build clean; the two lint errors surfaced in these files are pre-existing and unrelated (confirmed via diff — nowhere near the touched lines).
- **`DriverPerformance.jsx` wired to real data, 2026-08-14** — was mock-data-only since it was first built (`MOCK_ALERTS`/`MOCK_SESSIONS`, no Supabase call), flagged 2026-08-11. Per user request, kept a small (2-session/3-alert) sample fallback rather than removing mock data outright, but real data now takes priority whenever it exists.
  - New `useDriverPerformanceData()` hook queries `sessions` (last 7 days) directly client-side — correctly scoped to the calling driver's own rows via the existing `assigned crew can read own sessions` RLS policy (`session_visible_to_caller`, see `RLS.md`), no new Edge Function needed, contrary to the earlier assumption in this file that one would be required. `alerts` has no equivalent per-driver RLS (blanket `authenticated: select`, see `DATABASE.md`), so it's explicitly filtered to `.in('session_id', <this driver's own session ids>)` afterward rather than trusted to scope itself — deliberately not the same unscoped-query pattern `SupCrewProfile.jsx`'s own Performance tab uses (that page queries both tables with only a date filter, no driver scoping at all; left alone since fixing it wasn't in scope here, but worth knowing it's a latent gap on that page).
  - Sample fallback (`SAMPLE_SESSIONS`/`SAMPLE_ALERTS`, generated relative to `Date.now()` so they never go stale) only renders when the real `sessions` query returns zero rows for the window (a driver with no trips at all in the last 7 days) — deliberately not triggered when real sessions exist but have zero alerts, since a genuinely clean week is real data, not something to paper over with fake alerts. A visible "Showing sample data — you haven't completed any trips in the last 7 days yet" banner (new `Info` icon) makes clear it isn't the driver's real history, given the domain (a driver's own safety record) makes silently-fabricated-looking stats a real trust problem, not just a cosmetic one.
  - Build/lint clean. **Live-verified 2026-08-14** via two new disposable scripts (`scripts/repro-driver-performance.mjs`, `scripts/repro-driver-performance-sample.mjs`, kept as reusable tooling): logged in as `D002` (real sessions from this same day's own testing, 22 trips/42 alerts) — confirmed real data rendered, no sample banner, and the numbers matched a direct DB query; then, via a magic-link-injected session for `D004` (zero sessions in the window, confirmed by direct query first), confirmed the sample banner appeared with the small 2-trip dataset instead of a blank page. Screenshot-confirmed both.
- **`SupCrewProfile.jsx`'s Performance tab scoping bug fixed, 2026-08-14** — found while wiring `DriverPerformance.jsx` above (see that entry): this tab's `loadPerformance` query had no per-driver scoping at all, only a `created_at >= since` date filter, so a Supervisor viewing _any_ Driver's profile got every driver's `sessions`/`alerts` mixed together for that date window (not caught earlier since it was invisible until more than one driver had recent data at the same time). Fixed the same way `DriverPerformance.jsx`'s new `useDriverPerformanceData()` does: `sessions` now filtered by `.eq('driver_id', crew.employeeId)` (the specific driver being viewed, from `SupDeliveryCrew.jsx`'s `mapCrewRow`), then `alerts` filtered by `.in('session_id', <that driver's own session ids>)` — two sequential queries instead of one unscoped `Promise.all`, since `alerts` has no per-driver RLS to lean on either (see `RLS.md`/`DATABASE.md`). This tab only ever renders for a Driver (`driversOnly: true`), so `driver_id` is always the right column.
  - Build/lint clean. **Live-verified 2026-08-14**: viewed `D004`'s (zero real sessions in the window) profile Performance tab — correctly showed "No trip data"/0 alerts/0 trips instead of leaking `D002`'s real 22-trip/42-alert history the way the unscoped query would have; then confirmed `D002`'s own profile still correctly showed their real 22/42 numbers, matching the Driver-side page exactly. Screenshot-confirmed both, via a Supervisor session obtained through Supabase's magic-link admin API (no password on file for the test Supervisor account either).
- **Phase 11 — Route Comparison (planned vs. actual), built 2026-08-14** (`IMPLEMENTATION/11_ROUTE_COMPARISON.md`; trigger-point decision corrected the same day per explicit user instruction — see that doc's "Decision" section). Driver requested a whole-trip planned-route guide shown pre-trip, which also became the persisted `suggested_route` this doc had been waiting on since 2026-08-13.
  - **Schema, approved explicitly**: `delivery_requests.suggested_route` (jsonb, nullable), `supabase/migrations/20260814131307_delivery_requests_suggested_route.sql`.
  - **`driver-trip`'s new `save-suggested-route` action** persists it once (ownership-checked like `start-trip`; a second call for an already-saved delivery no-ops instead of overwriting).
  - **`DriverDeliveries.jsx`'s new `PlannedRouteMap`** replaces the old single-point static Google Maps iframe embed on the pre-trip screen (before Start Pickup) with a real Google Maps guide of the whole Pickup → Dropoffs/Stops chain in one view — stop order via the same `nearestDropoffOrder` heuristic `LiveNavigationMap` already uses for live dynamic ordering, seeded from Pickup; one colored polyline per leg (`NAV_LEG_COLORS`) plus the pickup/dropoff/stop marker icons already built earlier this session. Computes via `DirectionsService` only the first time (no `suggestedRoute` saved yet); renders the frozen saved route directly on every later view, no repeat API calls. Falls back to the original static embed when pickup/dropoff aren't parseable coordinates (a real street address, not "lat,lng"), so no delivery's pre-trip screen regresses.
  - **`SupDeliveries.jsx`'s `buildRealTripAndBehaviorReport`** gained a `routeDeviation` field (real planned legs, real GPS-reconstructed actual route via the same `sessions`/`gps_logs` fetch that function's other tabs already use, honestly-computed Planned/Actual/Deviation distance — no fabricated AI narrative/deviation-segments/schedule-fuel-impact fields, matching this same function's existing "don't fabricate" convention). `RouteDeviationMap`/`RouteDeviationTab` extended from a single flat planned/actual pair to `plannedLegs` (multi-leg, colored) — the Supervisor's "Route Deviation Report" tab now shows real data for any delivery with a saved `suggested_route`, not just the legacy `completed_delivery_reports` mock fixtures (which still work, normalized to the same shape).
  - **Known, accepted gap, not a bug**: `suggested_route` covers Pickup → Dropoffs only (per the corrected decision's scope), while the real GPS actual-route trace still includes whatever the driver drove before reaching pickup — no reliable "arrived at pickup" timestamp exists to clip on, so this shows as an expected extra segment near the start of the actual route on the comparison map.
  - Build/lint-verified (`npm run build`, `eslint` — one pre-existing, unrelated `setState`-in-effect error in `SupDeliveries.jsx`, confirmed via a stash diff to predate this change). **Not yet live-verified end-to-end** against a real Supabase project — would need a live delivery assignment, a Driver walkthrough of the pre-trip screen, and a completed trip to exercise the full path from generation through to the Supervisor's comparison view.
- **Driver's own past-trip report wired to real data too, 2026-08-14, same-day follow-up** — the entry above initially left `DriverDeliveries.jsx`'s own independent `RouteDeviationMap`/`CompletedDeliveryReport` duplicate (the Driver's own past-trip report view) mock-only, flagged as a follow-up; user asked for it to be wired up next. Turned out to be a bigger gap than the Supervisor side: that file had _no_ real-data wiring at all (all three tabs — Trip, Behavior, Route — were 100% the two `COMPLETED_REPORT_DATA` mock fixtures), unlike `SupDeliveries.jsx`'s equivalent, which already had Trip/Behavior done in an earlier pass and only Route was missing.
  - New `buildRealDriverTripReport(delivery, sessions, alerts, gpsLogs)` mirrors `SupDeliveries.jsx`'s `buildRealTripAndBehaviorReport` (same real sources — haversine distance, real per-item completion timeline sorted by actual `completedAt`, alert counts/types/peak-hour, session log, risk tier via `getRiskLevel`, same `routeDeviation` shape/logic), reshaped into this file's own simpler report structure. Same "don't fabricate" rule: no AI narrative, no precise pickup-confirmed timestamp (only `pickup_photo_url` exists, a boolean flag with no time attached).
  - `DriverDeliveries.jsx`'s own `RouteDeviationMap` extended from a single flat planned/actual pair to `plannedLegs` (multi-leg, colored via the same `NAV_LEG_COLORS` already used by `LiveNavigationMap`/`PlannedRouteMap` in this same file), mirroring the Supervisor-side extension exactly. New `ResolvedText` component (mirrors `SupDeliveries.jsx`'s identical helper) resolves any "lat, lng"-shaped stop location to a real address inline.
  - Fetched lazily in `DeliveryDetailView` (per selected delivery, same `sessions`/`alerts`/`gps_logs` query shape as the Supervisor's version), replacing the old unconditional `COMPLETED_REPORT_DATA[delivery.id]` lookup — falls back to that mock lookup only for the handful of ids that still have an entry, same "real data if present, otherwise the honest empty state" precedent used everywhere else in this app.
  - Route tab is hidden (not shown broken) for a delivery with no saved `suggested_route`, same as the Supervisor's version — tab grid switches from 3 to 2 columns when it's hidden.
  - Build/lint-verified (`npm run build`, `eslint` clean on this file). Not yet live-verified end-to-end.
- **Real bug found and fixed in Phase 11 (above), same day, caught by the user on a real screen** — `PlannedRouteMap` gated on `active.pickupCoords && active.destinationCoords` (both only non-null when `parseCoords` matches a literal `"lat, lng"` string), so it silently never rendered for a real, human-entered street address — the normal case for an actual booking, not the exception. Every real delivery fell through to the old static single-point embed with no indication anything was wrong. Root cause: the whole component was built around requiring pre-parsed coordinates, when `DirectionsService` already geocodes a plain address string directly (same as `LiveNavigationMap`'s own waypoints, `parseCoords(x) || x`) — no pre-parsing was ever actually necessary.
  - Fixed: `PlannedRouteMap` now takes `pickupAddress`/`dropoffAddress` (raw strings) and passes them straight to `DirectionsService`; the call site's gate is now just "do these addresses exist" (effectively always true), not "did they happen to parse as coordinates." Marker positions (pickup/dropoff/stop pins) are now derived from the resulting route legs' own real geocoded path points instead of `parseCoords`, so they work regardless of address format. Map bounds are fit from the real `DirectionsResult` bounds (freshly computed) or the saved legs' own points (already-frozen `suggestedRoute`).
  - Same root-cause bug also existed one level downstream, in both `buildRealTripAndBehaviorReport` (`SupDeliveries.jsx`) and `buildRealDriverTripReport` (`DriverDeliveries.jsx`)'s `routeDeviation.pickupCoords`/`dropoffCoords` (fed `getPickupCoords`/`getDropoffCoords`/`delivery.pickupCoords`/`destinationCoords`, all `parseCoords`-based) — fixed the same way, derived from `suggestedRoute`'s own first/last path points instead, so the Route Deviation Report's pickup/dropoff pins now also work for a real address, not just lat/lng fixture data.
  - Build/lint-verified (`npm run build`, `eslint` clean on both files). Not yet live-verified against a real booking with a real (non-fixture) address.
  - **Second, separate spot with the identical static-embed gap found while confirming the fix with the user** — the user's screenshot turned out to be a _different_ screen than the one just fixed: `DeliveryDetailView`'s own "Route Overview" section (reached by opening a specific delivery from the Upcoming tab), not the active delivery's workspace view. That section still had its own old static single-point iframe embed, untouched by the fix above since it's a separate code path. Swapped it for the same `PlannedRouteMap` (whole-trip guide), matching the active view's layout exactly — `PlannedRouteMap` already renders its own titled section, so it isn't re-wrapped in a second "Route Overview" header/border here. New `onSuggestedRouteSaved` prop threaded through both `DeliveryDetailView` call sites (Upcoming/Past tabs) so a freshly-saved `suggestedRoute` updates `selectedDelivery` state directly, same reasoning as the active view's `onSaved`. Build/lint-verified.
  - **Map zoomed out too far, found live by the user** — `fitBounds`'s 48px padding was eating a large share of the map's already-short (144-176px) container height, forcing an aggressive zoom-out even for a short route. Padding dropped to 16px on both `fitBounds` calls (fresh-compute and already-saved-route paths) and the container itself grown to 192-224px for more natural breathing room.
  - **Warehouse → Pickup leg added, per explicit user instruction, same day** — `suggested_route`/`PlannedRouteMap` now lead with a fixed **Warehouse → Pickup** leg before the existing Pickup → Dropoffs/Stops chain, using a new constant `WAREHOUSE_ADDRESS` (`"140 M. Suarez Avenue, Brgy. San Miguel, Pasig, Metro Manila"`) as `DirectionsService`'s origin — this reverses the "Pickup-only scope" decision from earlier the same day (see `11_ROUTE_COMPARISON.md`'s "Decision" section, corrected again). New `warehouseMarkerIcon()` (slate warehouse/building glyph, matching the pickup/dropoff icon style) marks the leg's start on the map.
    - Leg indexing shifted: leg 0 is now genuinely the to-pickup leg, matching `NAV_LEG_COLORS`' own documented convention (index 0 reserved for it) — the `(i + 1) % NAV_LEG_COLORS.length` offset used everywhere this feature colors legs (`PlannedRouteMap`, both `routeDeviation` builders) dropped back to a plain `i % NAV_LEG_COLORS.length`.
    - `pickupCoords` derivation in both `buildRealTripAndBehaviorReport` (`SupDeliveries.jsx`) and `buildRealDriverTripReport` (`DriverDeliveries.jsx`) fixed to match — was `suggestedRoute[0]?.path?.[0]` (correct back when leg 0 started at Pickup), now finds the leg whose `to === 'pickup'` and takes its _last_ point instead, since leg 0's start is now the Warehouse.
    - Side benefit: the Route Deviation Report's "Actual Distance" and "Planned Distance" (and the visually-obvious extra actual-route segment near the map's start) are now much closer to an apples-to-apples comparison, since both figures cover roughly the same real-world starting point instead of the actual trace having a large unmatched head-start — see `11_ROUTE_COMPARISON.md`'s updated caveat.
    - `LiveNavigationMap`'s own pre-existing, separate depot→Pickup leg (`activeNavOrigin`'s hardcoded `{lat: 14.5506, lng: 121.0471}`) was **not** touched — different code path, not reconciled with the new `WAREHOUSE_ADDRESS` constant even though they likely represent the same physical location; flagged as a possible follow-up, not done here.
    - Build/lint-verified (`npm run build`, `eslint` clean on both files). Not yet live-verified against a real booking.
- **Real, live-project gap found and fixed the same day: the `suggested_route` migration was never actually applied to the live database, and `driver-trip`/`admin-users` were never redeployed with this session's code — the whole feature had been running against a local file only, not the real project, since it was first built.** Caught while trying to demo the feature with real data: `delivery_requests.suggested_route` didn't exist live, so every `save-suggested-route` call and the map's whole-trip guide had been silently failing (or falling back) since Phase 11's first pass. Root cause echoes `DATABASE.md`'s existing gotcha ("a decision documented as resolved here previously meant decided, not necessarily applied to the live database") — writing the migration _file_ was mistaken for actually running it.
  - Also found: 8 earlier migrations (`delivery_quotations` through `proof_photos`) were already live but untracked in the CLI's own migration history table (applied via some other manual method previously, not `supabase db push`) — `supabase migration repair --status applied` used to sync the tracking table to reality before pushing only the genuinely-missing one, rather than blindly re-running all 9 and risking a "column already exists" failure on the ones already live.
  - Fixed: `npx supabase migration repair` (8 versions) + `npx supabase db push` (the new migration only) + `npx supabase functions deploy driver-trip` + `npx supabase functions deploy admin-users`. Verified live via a direct REST `select` on `suggested_route` immediately after.
  - **Then actually demoed end-to-end against real data** (`scripts/repro-route-comparison-demo.mjs`, kept as reusable tooling): temporarily reopened `DR-0021` (the permanent real-data demo fixture) so the Driver app would generate+save a real `suggested_route`, confirmed the save via polling, restored `DR-0021`'s status back to `COMPLETED` (a `finally`-guarded safety check re-confirms this even on script failure), then viewed the real Route Deviation Report as both Supervisor and (separately, read-only, no status change needed) the Driver's own Past tab. Both rendered correctly, zero page errors, screenshot-confirmed (screenshots since replaced, see the follow-up entry below). One honest finding at the time, not a bug: DR-0021's real GPS trace showed ~16 meters of movement (a stationary hardware bench test, not an actual drive), so its "Actual Distance"/"Deviation %" read as near-zero/100% — an accurate reflection of that specific fixture's limited real data, not fabricated or wrong. **Superseded same day** — DR-0021's `gps_logs` were later deliberately replaced with fabricated demo data, see below; this finding describes the original real trace only.
  - This is the first time Phase 11 has been confirmed actually working against the live project — everything before this entry in the Phase 11 write-ups above was build/lint-verified only.
- **Two follow-up fixes to Phase 11, same day, after showing the user the real-data demo above:**
  1. **Actual route was too visually similar to a planned leg.** The actual-route polyline reused `#2563eb` (the same blue `NAV_LEG_COLORS` assigns to the second planned leg), distinguished only by dashed-vs-solid — read as "basically the same line" at a glance, per user feedback. Fixed in both `RouteDeviationMap` copies (`SupDeliveries.jsx`, `DriverDeliveries.jsx`): actual route is now a new `ACTUAL_ROUTE_COLOR` (`#0F172A`, near-black/neutral — deliberately not another hue from `NAV_LEG_COLORS`) rendered as two stacked polylines, a wide white/light halo underneath (`weight: 8`) and the dark line on top (`weight: 4`) — a standard cartographic contrast technique. Legend swatches in both `RouteDeviationTab`s updated to match.
  2. **DR-0021's real actual route was too small to demo.** Its real `gps_logs` (71 points, all from one session) covered only ~16 meters — a stationary hardware bench test, not a drive (see the entry above). Per explicit user request for a visually substantial "route taken" to demo, `scripts/seed-dr0021-fake-actual-route.mjs` (new, kept as reusable one-time-seed tooling, clearly marked as **fake demo data, not a diagnostic** — distinct from every `repro-*.mjs` script in this folder) replaces DR-0021's `gps_logs` with a real, road-following alternate path between the same Pickup/Dropoff (fetched via the app's own browser-side `DirectionsService`, not synthetic noise — a REST Directions API call was tried first and correctly rejected with `REQUEST_DENIED` since the Maps key is browser-referrer-restricted, so the script instead drives the real app in Playwright and calls `window.google.maps.DirectionsService` from inside the page, same as every other script in this repo), with a detour waypoint injected so it visibly diverges from the planned route. Timestamps spread evenly across DR-0021's longest real session window. Old real (bench-test) points deleted first, not layered alongside the fake ones.
  - **Re-verified live**: fresh screenshots (`scripts/repro-route-comparison-sup-3-fake-route.png`, refreshed `-driver-past.png`) confirm the new near-black haloed line is unambiguously distinct from both dashed planned legs, and the fake route now shows a real, visible deviation (Planned 23.1km / Actual 11.1km / Deviation 12.0km, 52%) instead of the previous near-zero figures. Published documentation artifact updated with the new screenshots and corrected copy (was describing the near-zero-actual-distance case as expected; now describes DR-0021's route as explicitly fabricated demo data).
- **Pre-real-device-test triage, 2026-08-14** — asked "is this ready to test on a real device tomorrow," identified several gaps that were fixable without physical hardware, fixed the three that were, and ran the closest-to-real-device test possible without one.
  1. **Reconciled the two different depot locations.** `PlannedRouteMap`'s new `WAREHOUSE_ADDRESS` (`DriverDeliveries.jsx`) and `LiveNavigationMap`'s pre-existing hardcoded `activeNavOrigin` (`{lat: 14.5506, lng: 121.0471}`) were two independent guesses at the same real place, never reconciled when the first was added. Fixed: new `WAREHOUSE_COORDS` constant (`{lat: 14.57147, lng: 121.08762}`, the actual geocoded result for `WAREHOUSE_ADDRESS`, captured from a real `DirectionsService` response) replaces the old hardcoded literal in `activeNavOrigin`. Both views now agree on where the driver's Trip actually starts.
  2. **Real device-pipeline test, closest possible without physical hardware** (`scripts/repro-real-gps-pipeline.mjs`, new, kept as reusable tooling). Everything about Phase 11 up to this point had only ever gone through browser-side `DirectionsService` or direct `service_role` DB inserts — never the actual `gps-upload` Edge Function's device-auth path a real Pi uses. This script: registers a real test device (`DV-PIPETEST`, same hashing `register-device` produces) on DR-0020's truck, resets DR-0020 and lets `PlannedRouteMap` save a real `suggested_route`, starts a real Trip (`driver-trip` start-trip), uploads all 559 points of DR-0020's already-captured real route through the **actual `gps-upload` endpoint** (device_id/device_secret auth, not a shortcut), ends the Trip (real mileage calc off those same rows), and views the resulting Route Deviation Report as Supervisor.
     - **First run found a real script bug, not a product bug**: all 559 uploads failed with `401 UNAUTHORIZED_NO_AUTH_HEADER`. Root cause: `gps-upload`'s Edge Function gateway enforces `verify_jwt` before the function's own device-auth code ever runs — the real Pi (`pi/drowsiness_monitor.py`) sends the anon key as `Authorization`/`apikey` headers to satisfy that gateway check, separately from `device_id`/`device_secret` in the body for the app-level check. The test script was missing the gateway headers entirely. Fixed to match `drowsiness_monitor.py` exactly; re-run: **0/559 upload failures**, real mileage computed (14.66 km) from genuine `gps-upload` rows.
     - **Second, separate real gap found via this same script's cleanup step**: `service_role` has no `DELETE` grant on `sessions` at all (every existing Trip-lifecycle action only ever inserts/updates a session, so this was never hit before) — confirmed via Postgres's own error message, not assumed. Not fixed (a grants change needs explicit approval per `00_IMPLEMENTATION_RULES.md`, flagged here instead); worked around in the script by `UPDATE`-ing the leftover session's `device_id` to `null` (which is granted) instead of deleting the row, freeing the FK reference so the test device can still be removed.
     - **Confirmed correct, not a bug**: `driver-trip`'s `end-trip` correctly leaves `delivery_requests.status` at `DELIVERED`, not `COMPLETED` — advancing to `COMPLETED` today only happens via the Customer's own "Confirm Receive" action (`CustomerDeliveries.jsx`) or a Supervisor resolving a reported issue (`SupDeliveries.jsx`'s `resolveIssue`), neither of which this script exercises, so it advances status directly as a documented test-only shortcut purely to reach Supervisor's Completed list.
     - **Per user clarification, same day: neither of those is supposed to be a required gate** — once a delivery is actually done, it should just be `COMPLETED`, not stuck at `DELIVERED` awaiting a separate confirmation step. This is a real, open product-behavior question about what `end-trip` (or something downstream of it) should do — **not implemented**, flagged here for a decision before it's built, not assumed.
     - **Full pass, real pipeline, zero errors**: `scripts/repro-real-gps-pipeline-2-live-position.png`/`-3-sup-route-tab.png` confirm the Driver's Live Navigation view renders real-time position from genuine `gps-upload` inserts, and the Supervisor's Route Deviation Report renders a real 4-leg planned route (Warehouse → Pickup → Dropoff → Stop → Stop) against a real actual trace built entirely from those same rows — Planned 26.1km / Actual 14.7km / Deviation 44%.
  3. **Multi-stop, real-address `suggested_route` stress test** (`scripts/repro-multistop-suggested-route-stress.mjs`, new, reusable tooling) — everything tested so far used either DR-0020's parseable `"lat, lng"` fixture addresses or DR-0021's single real address with no stops; nothing had exercised several genuinely different real street addresses across a wide multi-leg chain (the actual production shape). Temporarily overrode DR-0020 with 6 real Metro Manila addresses (Pickup: Rizal Park; 4 Stops spanning Pasay/Taguig/Quezon City; Dropoff: Ortigas Center), ran the pre-trip screen for real. **Result**: 6 legs, 2,468 total path points, 82.3 KB payload, ~1.3s save latency, zero page errors — no size/reliability problem found. DR-0020 restored to its exact original fixture values afterward (verified byte-for-byte against a pre-change backup), since other scripts (`simulate-dr0020.sh`) depend on those exact coordinates matching the already-captured route fixture.
  - Build/lint clean throughout (`npm run build`, `eslint`). This is the most real-world-representative testing this feature has had — still not a physical device, but every layer that can be exercised without one (real Edge Function auth, real Directions geocoding, real multi-leg real-address chains) now has been.

- **Resolved the open DELIVERED → COMPLETED product question from 2026-08-14 (line above), 2026-09-05** — triggered by a real user-reported bug: Supervisor's "In Transit Deliveries" tab was showing DELIVERED requests, because `SupDeliveries.jsx`'s `ongoingDeliveries` filter explicitly included `"DELIVERED"` alongside the genuinely-in-transit statuses. Decision made: keep DELIVERED as a real, visible milestone (so the Customer's already-built "Confirm Received" card, `CustomerDeliveries.jsx`, stays reachable) rather than collapsing straight to COMPLETED, but stop it from being bucketed as "in transit". Fixed: `ongoingDeliveries` no longer includes `DELIVERED`; `completedDeliveries` now includes both `DELIVERED` and `COMPLETED` (each row's own status badge still shows "Delivered" vs "Completed" distinctly).
  - **Auto-complete-after-7-days was briefly implemented the same day, then explicitly reverted per user decision, same day**: a `src/lib/autoCompleteDelivered.js` (`autoCompleteOverdueDeliveries()`) was added and wired into both `SupDeliveries.jsx` and `CustomerDeliveries.jsx` right after each fetches its rows, auto-flipping any unconfirmed `DELIVERED` request 7+ days old to `COMPLETED`. **Reverted**: user explicitly asked to make customer confirmation *required* instead — DELIVERED now stays DELIVERED indefinitely with no auto-advance; `persistReceivedConfirmation` (`CustomerDeliveries.jsx`) is the only path to COMPLETED. `src/lib/autoCompleteDelivered.js` was deleted; the wiring in both pages was removed. The pre-existing in-file `autoCompleteDelivered`/`mockRequests`/unused `requests` state in `SupDeliveries.jsx` remains dead code either way (only ever ran against unused legacy mock state, never real data) — left in place, not cleaned up, out of scope for this fix.
  - Also fixed the same day: the Supervisor's "Submit Quotation" button/form (`SupDeliveries.jsx`) had no upper status bound — gated only on `hasApproved` (true for anything past `PENDING_REQUEST`) and whether a `quotation` object was attached, so a request with a data gap (missing quotation join) could still show "Submit Quotation" even at `DELIVERED`/`COMPLETED`. Fixed by additionally requiring `stageStatus[status] < stageStatus.ASSIGNED`, since quotation only makes sense pre-dispatch.
  - Build/lint-verified throughout (`npm run build`, `eslint`); not click-tested live in the browser.

- **Supervisor Route Review & Approval feature, 2026-09-06** — per explicit user request: a route (Warehouse → Pickup → Dropoff → Stops) is now generated for every delivery request at customer-submission time (`CustomerRequestDelivery.jsx`), not only later by the assigned Driver's pre-trip screen. A Supervisor reviewing a `PENDING_REQUEST` can now view it on a new map (`SupDeliveries.jsx`'s `EditableRouteMap`, via the new shared `src/components/EditableRouteMap.jsx`), drag pickup/dropoff/stop pins to correct it (position-correction only, no stop-reordering, per explicit decision), and click "Approve Route" to freeze it — writing `route_approved_at`/`route_approved_by` (new migration `20260906000000_delivery_requests_route_approval.sql`; RLS-clean, since a Supervisor already has blanket `UPDATE` and neither column is in `enforce_supervisor_status_only_update`'s guarded list). The approved route is then shown read-only alongside the quotation on the Customer's side (`CustomerDeliveries.jsx`'s `ApprovedRouteMap`, only once `route_approved_at` is set — an unreviewed draft is never shown to the customer as if final).
  - This is the second correction to `IMPLEMENTATION/11_ROUTE_COMPARISON.md`'s route-generation timing decision (see that doc for the dated history) — generation moves back to request-creation time, but this time reviewable/editable, not invisible.
  - Extracted the route-computation logic (`WAREHOUSE_ADDRESS`/`WAREHOUSE_COORDS`/`distanceMeters`/`nearestDropoffOrder`/a new `computeSuggestedRoute`) out of `DriverDeliveries.jsx`'s `PlannedRouteMap` into shared `src/lib/suggestedRoute.js`, since it's now needed identically in three places (Customer creation, Supervisor edit, Driver fallback) — extracted rather than duplicated per-portal (unlike `RouteDeviationMap`) since it's a nontrivial multi-step DirectionsService/nearest-neighbor algorithm, not a simple read-only renderer.
  - **Confirmed decision, changes Driver-side behavior**: since every request now gets an auto-generated `suggested_route` from creation, `PlannedRouteMap`'s skip-recompute check moved from "does `suggested_route` exist" to "is `route_approved_at` set" — an auto-generated-but-never-reviewed route still gets a fresh recompute at Driver pre-trip time, same as before this feature existed. `driver-trip`'s `save-suggested-route` Edge Function action guard was updated to match (previously no-op'd on `suggested_route` presence, now on `route_approved_at`), otherwise the Driver's fresh recompute would silently fail to persist.
  - Route generation on submission is **non-fatal** — unlike `estimateLegDurations` (blocks submission, since a wrong schedule is a correctness problem), a failed route-geometry call just leaves `suggested_route: null`; the Supervisor can still generate one later from `EditableRouteMap`'s "Generate Route" button.
  - **Cost note, accepted trade-off**: this adds a second Directions API call per submission (on top of `estimateLegDurations`'s existing duration-only call), for every request including ones a Supervisor declines without ever reviewing the route.
  - **Deployed and live-tested end-to-end 2026-09-06** (migration + both Edge Functions pushed by the user). New `scripts/verify-route-approval-feature.mjs` (kept as reusable tooling, same backup/restore-a-real-fixture pattern as `verify-customer-progress-details.mjs`) reuses `DR-0048` (real customer `jmsoho01@marveltrucking.local`): resets it to `PENDING_REQUEST` with no route, logs in as Supervisor (`atdrilon01@marveltrucking.local`), confirms `EditableRouteMap` renders, clicks "Generate Route", clicks "Approve Route", confirms `suggested_route`/`route_approved_at`/`route_approved_by` all land correctly in the DB, then sets the row to `PROCESSING` with a disposable test quotation and confirms the Customer sees the approved route read-only (no edit buttons) next to the quotation — all via screenshots plus DB assertions, restoring the row exactly afterward.
    - **Two real bugs found and fixed during this live test:**
      1. **`EditableRouteMap`'s map area rendered as a blank box after Generate/Regenerate/a drag-triggered recompute**, even though the route had genuinely computed (buttons correctly switched to "Regenerate Route"/"Approve Route", and the DB later confirmed real leg data was saved). Root cause: the map's camera was only fit to the route's bounds once, inside `onLoad` — but in editable mode the map mounts *before* any route exists (Supervisor starts from nothing, unlike the Customer's read-only view where `suggestedRoute` is already populated on first render), so `onLoad`'s one-time `fitBounds` call ran with nothing to fit yet and the camera stayed parked on Google's default view. Fixed by adding a separate effect that re-fits whenever `bounds` becomes available or changes, not just at mount.
      2. **On mobile, the Customer's approved-route map was invisible for the common single-round-quotation case.** The map had only been wired into two of `CustomerDeliveries.jsx`'s several quotation-display call sites (the "Round 1/Round 2" counter-offer card, which only renders once `request.previousQuotation` exists). A first-round quotation on mobile actually renders through a third, separate compact "Your Quotation" card (`hasRightContent`'s `md:hidden` block) that hadn't been touched — confirmed live via screenshot (quotation and Approve/Reject buttons showing, but no route map). Fixed by adding `ApprovedRouteMap` there too.
    - Drag-to-recompute confirmed working via a follow-up manual script: dragging the pickup marker changed its coordinates, triggered a fresh `computeSuggestedRoute` call, and the map re-fit to the new (in this test, intentionally nonsensical -- dragged into Manila Bay) bounds, with `suggested_route` correctly staying unwritten in the DB until "Approve Route" was clicked.
    - Not yet re-verified: the Driver's `PlannedRouteMap` skip-recompute behavior itself (gating on `route_approved_at`) was confirmed correct by code review and the Edge Function guard update, but not separately click-tested live end-to-end as a Driver — lower priority since it's a straightforward boolean-gate change to code that was already working before this feature (see the rest of this entry).

- **Real crash fixed, reported live by the user 2026-09-06: typing a dropoff-window time for any stop crashed the whole booking form** (`Uncaught TypeError: Cannot destructure property 'dropoffTime' of 'undefined'`, `deliveryOptions.js`'s `getStopTimeError`) whenever a request had multiple stops. Root cause in `CustomerRequestDelivery.jsx`'s `handleStopTimeChange`: `updatedStop` was declared with `let updatedStop;` (no value) and only assigned inside the `setFormData` state-updater callback's `.map()` — then read on the very next line, outside that callback, assuming the updater had already run synchronously. That assumption doesn't hold reliably; when it didn't, `updatedStop` was still `undefined` when passed into `getStopTimeError`, which destructures its first argument directly in the function signature with no guard. Fixed by computing `updatedStop` directly from `formData.stops[index]` (already available in the handler's own closure) merged with the new field value, independent of the `setFormData` call's timing — matching the pattern every other `getStopTimeError` call site in this file already used safely. Live-verified via a disposable Playwright script (`scripts/tmp-verify-stop-time-fix.mjs`, not committed): added two stops, typed a start time into a stop's window field, confirmed zero page errors and the expected "Enter both a start and end time for this dropoff's window" validation message rendering normally instead of crashing. Build/lint-verified (`npm run build`, `eslint`).

- **Real crash fixed, reported live by the user 2026-09-06: dragging the dropoff pin in `EditableRouteMap` (Supervisor Route Review & Approval feature, same day) threw `InvalidValueError: in property destination: not a string; and not a LatLng or LatLngLiteral: in property lat: not a number; and unknown property 0`.** Root cause: `EditableRouteMap`'s initial `waypointCoords` state fell back to `legEndpoint(suggestedRoute, "pickup"/"dropoff")`/`stopLegEndpoint(...)` when no `pickupCoords`/`dropoffCoords` prop was given — but those helpers return a raw `[lat, lng]` array (the shape `suggested_route.path` stores in the DB), while every other coordinate in this component (the props, drag events, `computeSuggestedRoute`'s own params) uses `{lat, lng}` object shape. Dragging any *other* pin (pickup, a stop) triggered a recompute that passed the still-array-shaped, never-yet-touched dropoff coordinate straight into `computeSuggestedRoute`'s `destination`/waypoint `location` — Google's `DirectionsService` rejects a plain array there, since it reads `.lat`/`.lng` properties that don't exist on an array (only numeric-indexed `0`/`1`, hence "unknown property 0"). Fixed by wrapping both `legEndpoint`/`stopLegEndpoint` fallbacks with the component's existing `toLatLng()` normalizer at the point `waypointCoords` is initialized. Live-verified via a disposable Playwright script: reset DR-0048 to `PENDING_REQUEST`, generated a route, dragged the "D" (dropoff) marker specifically (the exact pin the user reported), confirmed zero console/page errors and the route correctly recomputing to the dragged position. Build/lint-verified (`npm run build`, `eslint`).

- **Real bug fixed, reported live by the user 2026-09-06: dragging the pin in `CustomerRequestDelivery.jsx`'s booking-form `LocationPickerModal` (pickup/dropoff/stops) appeared to "revert" to the pre-drag location.** Not a crash — the drag itself worked and the correct precise coordinate was genuinely saved to `formData` at confirm time — but reopening the picker afterward (e.g. to double-check the pin) silently discarded that precision. Root cause: the modal's `initialValue` effect always re-forward-geocodes the field's current address *text* via Photon on every open, regardless of whether a precise coordinate already existed — and reverse-geocoding is coarse enough that a manually dragged point and its surrounding street commonly share the exact same display address, so Photon's forward-geocode of that shared text returns its own canonical point for the street, not the dragged one, snapping the visual pin to a different (sometimes near-identical-looking, sometimes visibly different) location than what was actually confirmed. User-visible even without ever explicitly reopening the modal a second time, since re-rendering ordering could trigger it too. Fixed by threading the already-known precise coordinate through: `LocationInput` now accepts `lat`/`lng` props (pickup passes `formData.pickupLat/Lng`, dropoff `formData.dropoffLat/Lng`, each stop its own `stop.lat/lng` — all three already tracked in state, just not previously passed down), forwarded into `LocationPickerModal` as `initialLat`/`initialLng`; the modal's effect now uses that exact coordinate directly when present, only falling back to forward-geocoding the text when no precise coordinate exists yet (a manually-typed address that was never picked/dragged). Live-verified via a disposable Playwright script: searched an address, opened the picker, dragged the pin, confirmed, reopened the picker — before the fix the reopened coordinate drifted (`14.723836, 121.043351` → `14.724429, 121.042999`); after the fix it matches exactly. Build/lint-verified (`npm run build`, `eslint`).

- **Lint cleanup + Driver Today/Upcoming/Past tab fixes, 2026-09-08** — triggered by the user pulling a groupmate's merge (`aee3a48`, the COCO-SSD dropoff-photo-verification feature) and being told by a groupmate lint had errors.
  - **Real build failure found and fixed first**: `@tensorflow-models/coco-ssd`/`@tensorflow/tfjs` were added to `package.json` by that merge but never actually installed (`npm install` wasn't run after pulling) — `npm run build` failed outright with `Rolldown failed to resolve import "@tensorflow-models/coco-ssd"`. Fixed by running `npm install`.
  - **Lint: 74 errors/8 warnings → 28 errors/8 warnings.** Removed all 45 `no-unused-vars` and the 1 `no-undef` (`AdminTruckProfile.jsx`'s dead `UrgencyChip` component called `getMaintenanceUrgency`, a function never imported anywhere — harmless only because `UrgencyChip` itself was never rendered). Left the remaining 28 (`react-hooks/set-state-in-effect`, `exhaustive-deps`, `static-components`, `react-refresh/only-export-components`) untouched, per explicit user decision — those can change render timing/behavior and need individual judgment, not a mechanical pass. Most of the volume was `AdminTruckProfile.jsx`/`SupTruckProfile.jsx` (18 and 17 errors respectively): a large leftover block of pre-real-data mock generators (`CLIENT_SPECIALTIES`, `TRIP_ROUTES`, fake maintenance shops/types, a seeded RNG, `NOW`/`DAY_MS`) that both pages' own comments already called "frontend only, no backend/API/database" — fully dead now that both use real Supabase data via `components/trucks/utils/maintenance.js`. Deleting `overallCondition`/`nextService`/`lastService` in both files cascaded into three more now-orphaned `useMemo` blocks (`upcomingMaintenance`/`completedMaintenance`/`overdueMaintenance`) and their now-unused icon imports (`AlertTriangle`/`CheckCircle2`/`Clock`) and the `useMemo` import itself — all removed in the same pass once confirmed genuinely unused. Build/lint-verified (`npm run build`, `eslint`) after every file.
  - **Real bug fixed: `DriverDeliveries.jsx`'s Today/Upcoming/Past tabs made CANCELLED deliveries disappear from every tab.** `nonArchived` (Today/Upcoming's pool) already excluded `CANCELLED` correctly (needed, so a same-day cancelled delivery could never be picked as the active "Today" delivery), but `completed` (Past's pool) only ever matched `DELIVERED`/`COMPLETED` — and `DB_TO_DRIVER_STATUS`/`statusConfig` had no `CANCELLED` entry at all, so even attempting to show one would have crashed (`statusCfg.banner` reading off `undefined`). The original exclusion comment admitted this was only ever done to dodge that crash, not a real design decision about where cancelled jobs belong. Fixed: added a `CANCELLED` entry to both `DB_TO_DRIVER_STATUS` and `statusConfig` (rose "Cancelled" badge, matching `DeliveryRow`'s existing optional-chained badge lookup — confirmed `DeliveryDetailView` never touches `statusConfig` at all, so opening a cancelled delivery from Past was never at risk), and added `CANCELLED` to `completed`'s filter. Live-verified against real data: driver `D002` has 4 real cancelled deliveries (`DR-0020`, `DR-0022`, `DR-0023`, `DR-0028`) that were invisible everywhere before this fix — confirmed all 4 now show under Past with the new badge, zero console errors across all three tabs.
  - **Second real bug found while double-checking the tab logic explanation given to the user, not from a user report — a Paused trip left unresumed past midnight would silently lose its place on the Today tab.** `activeDelivery`'s selection prioritized `hasOpenSession` (a truly Active, currently-driving session) over `pickupDate === today`, fixed 2026-08-12 for a stale *Active* session (see that date's entry above) — but a **Paused** trip has no open Session at all (`pause-trip` closes it entirely; per `03B_PAUSE_AND_RESUME_TRIP.md`, "Paused isn't a stored value," it's inferred from "past ASSIGNED, no open Session"). So a Paused trip only ever got the Today slot via the `pickupDate === today` fallback — once midnight passed without a Resume, neither condition matched anymore, and the delivery fell out of Today into Upcoming (likely flagged Overdue), with `isPausedTrip`/Resume Trip's UI unreachable from there since it only renders inside Today's single-delivery view. Not documented anywhere as a known gap (checked `STATUS.md` and `09_EDGE_CASES.md`). Fixed by adding a second priority tier between the two existing ones: `pausedDelivery = nonArchived.find(d => !d.hasOpenSession && (d.status === "FOR_PICKUP" || d.status === "OUT_FOR_DELIVERY"))`, matching `isPausedTrip`'s own definition exactly (excludes `ASSIGNED`, since a delivery that never started a trip at all isn't "paused," just not-yet-started). Live-verified: set a real assigned delivery (`DR-0053`, pickup_date 2026-09-05, "today" being 2026-09-08 — 3 days stale) to `OUT_FOR_PICKUP` with no session row (simulating paused-and-abandoned), confirmed it now renders under Today with the "Trip paused" banner and a working Resume Trip button despite the stale date, zero console errors; reverted the fixture back to its original `ASSIGNED`/2026-09-05 state afterward. Build/lint-verified throughout.
  - **Tab labels renamed same day, per a groupmate's UX report the user agreed with after independently confirming it against the code**: "Today"/"Upcoming"/"Past" never actually were a date partition, and the names overpromised one. Renamed (`DELIVERY_TABS` in `DriverDeliveries.jsx`) to **Active** / Upcoming / **History** — no logic change, display-only. "Active" replaces "Today" since the tab always shows exactly one delivery chosen by status priority (open session, then paused, then `pickupDate === today` only as a last fallback — see the two entries above), not "whatever's scheduled today." "History" replaces "Past" since that bucket is a pure status filter (`DELIVERED`/`COMPLETED`/`CANCELLED`), not a date one — an `ASSIGNED` delivery from a month ago that's never finished stays in Upcoming (flagged Overdue) forever, it doesn't migrate here just because its date has passed. "Upcoming" keeps its name — it already visually separates an "Overdue" sub-section from "Scheduled" inside its own content, and a fully accurate combined label ("Upcoming & Overdue") doesn't fit this 3-up flex tab bar on a mobile viewport without wrapping. Updated the two empty-state copy strings to match ("Nothing active right now" / "No delivery in progress, and nothing scheduled for today." for Active; "No delivery history yet." for History). Live-verified at a 375px mobile viewport: all three labels (with count badges) fit on one line with no wrapping, both empty states read correctly. Build/lint-verified.
  - **Superseded the same day by an actual date-based restructure** — the user asked directly "is it possible that it should be date based instead of like a status," and after scoping the safety implications (this page also runs live GPS navigation and drowsiness-alert monitoring), decided to build it rather than just rename labels a second time. `DriverDeliveries.jsx`'s `loadDeliveries` now computes genuinely date-based buckets (Today = `pickupDate === today`, as a real list; Upcoming = `pickupDate > today`; History = terminal status OR `pickupDate < today`, the latter kept in its own "Overdue" sub-section so it doesn't read as finished) — while keeping the pre-existing status-priority pick (open session → paused → `pickupDate === today`) **completely unchanged** under a new name, `workspaceDelivery`, since that logic is what makes "Start Pickup" reachable at all (it only exists inside the single inline live workspace — deleting it, as an initial planning pass proposed, would have made it impossible to ever start a delivery once nothing was already in progress, caught before implementing). `workspaceDelivery` is unconditionally pinned at the top of Today (even on a stale date — the exact stale-session-recovery case from the two entries above) and excluded from every other bucket so it's never listed twice; every other `pickupDate === today` delivery shows as a plain row underneath it labeled "Also today," fixing the actual complaint (a same-day delivery no longer vanishes into Upcoming just because a different delivery — possibly stale-dated — occupies the workspace). Backend invariant this whole design leans on: `driver-trip`'s `start-trip` action already guarantees at most one delivery per driver can be genuinely in-progress at a time (`409` if another session is Active or another delivery is sitting Paused), so the five live/session-dependent features (drowsiness alerts, rest-stop recommendation, phone-GPS broadcast, Pi-GPS fallback, the live-nav map swap) only ever needed a rename (`active` → `workspaceDelivery`), no structural change. Scope: Driver only — `HelperDeliveries.jsx` mirrors similar logic but wasn't confirmed to have the same Paused-tier fix, deliberately deferred rather than silently ported. Live-verified end-to-end against real/synthetic fixtures on driver `D002`: a stale Paused trip (`DR-0053`, dated 3 days past) still pins the full live workspace with a working Resume Trip button, and is correctly excluded from Upcoming/History's Overdue section (a real double-listing bug caught and fixed during this same verification pass, before it shipped); two same-day `ASSIGNED` deliveries with no in-progress trip both show under Today, one as the pinned workspace with "Start Pickup" reachable, the other under "Also today"; a future-dated delivery appears only in Upcoming; a non-terminal overdue delivery shows under History's own "Overdue" section, separate from the "Finished" list; the real cancelled deliveries (`DR-0020`/`DR-0022`/`DR-0028`) still show correctly in History (re-confirming the earlier same-day fix survived this restructure). Zero console/page errors across all scenarios. Build/lint clean throughout.
  - **Ported to `HelperDeliveries.jsx`, same day, per explicit user request ("apply it to helper too... not everything but what helper needs only")** — scoped deliberately: Helper has no live-nav/GPS-watch/rest-stop machinery at all (it's read-only with respect to trip control, completing the chain via photo-required actions only), so only the bucketing logic, tab shape, and the rename convention were ported, not any Driver-only feature. Applied: the identical `loadDeliveries` restructure (`workspaceDelivery` selection unchanged in spirit -- open session → paused → `pickupDate === today` -- now including the paused-tier Helper was missing entirely, since it had only ever had Driver's original two-tier version); the same date-based Today/Upcoming/History buckets, "Also today" list, and History's Overdue/Finished split; tab label `"past"` renamed to "History" to match Driver. Helper's three delivery-scoped Realtime subscriptions (`helper-sessions-<id>`, `helper-alerts-session-<sessionId>`, `helper-gps-session-<sessionId>`) and the photo-chain completion flow (`submitChainAction`) needed only the same `active` → `workspaceDelivery` rename, no structural change -- confirmed via the same backend one-Active-or-Paused-trip-per-driver invariant Driver's restructure already leaned on. Also found and fixed the identical latent crash Driver had: `statusConfig`/`DB_TO_HELPER_STATUS` had no `CANCELLED` entry either, so a same-day cancelled delivery could have been picked as the workspace and crashed on `statusCfg.banner`; added the same rose "Cancelled" badge entry. One extra fix specific to this port: `loadDeliveries`'s `sessionStorage` cache-read (`"helperDeliveries"`) now checks for the new shape (`"today" in parsed`) before trusting a cached read, so a stale cache from before this restructure can't leave `data.today`/`overdue`/`history` undefined on first paint. Live-verified via Helper `H003` (Tyler Durden, magic-link auth): a stale Paused trip (`DR-0053`, same fixture used for Driver's verification) still pins the full read-only workspace with the Proof of Delivery chain and Complete button reachable, correctly excluded from History's Overdue section; History correctly splits a real stuck `ASSIGNED` delivery (`DR-0038`) under "Overdue" from the finished list below it; real cancelled deliveries (`DR-0020`/`DR-0022`/`DR-0023`/`DR-0028`) show correctly with the new badge instead of being invisible/crashing. Zero console errors. Build/lint clean.

- Web application development
- Trip management: Start Trip, Pause/Resume Trip, and End Trip are all done — backend and UI, tested end-to-end including click-testing in the browser (see Completed). Phases 1-3B and 5-7 are complete (backend/UI; Pi-side sender fully wired, boot-automated, and hardware-confirmed for each — see Completed). Phase 4 is now also fully confirmed (backend + `session_active` gating bench-tested 2026-08-12, see Completed). The ad-hoc live in-app navigation feature (capstone requirement, not a numbered phase) is also now done and polished (2026-08-12, see Completed). **Phase 8 (Supervisor Realtime Dashboard) is now also done and live-verified (2026-08-12, see Completed)** — Phase 9 (`09_EDGE_CASES.md`) is the next actual unstarted phase, see Next Steps below.
- Vibration motor (GPIO 4) reliability under investigation: missed one physical actuation out of two `prolonged_eye_closure` alarms during the 2026-08-12 bench test, despite identical, correctly-executed code both times (see Completed for the full analysis — leading theory is motor start-torque margin or a loose connection, not a software bug). Not yet enough data to call this a real pattern. Next step: repeat with ~5 consecutive closures to establish a real hit rate before investigating the driver transistor/MOSFET further.
- The repo-root prototypes (`drowsines.py`, `renz_test.py`) are now superseded by `pi/drowsiness_monitor.py` (see Completed, 2026-08-11) but were deliberately left in place, not deleted — kept as reference for the detection logic they were ported from. `renz_test.py`'s serial-GPS reading and dlib-based EAR/MAR formulas were the two things carried forward; its direct-Postgres-write pattern and hardcoded anon key were not.
- Helper has no equivalent performance page at all (`/helper` only routes to `trips`/`profile`) — unchanged, still true (see the `DriverPerformance.jsx` entry below for what Driver now has).
- **Open investigation, started 2026-08-13, not yet resolved: possible reroute-triggered rendering gap in `LiveNavigationMap` during the DR-0020 simulation.** User reported both the blue (leg 0, pickup→dropoff) and green (leg 1, dropoff→Dropoff 2) polylines appearing to show "at the same time" near a leg transition, watching a real browser. A live Playwright repro (`scripts/repro-dual-route.mjs`, screenshots the Driver's Live Navigation frame-by-frame while driving the same GPS points `simulate-dr0020.sh` uses) did **not** reproduce a literal simultaneous dual-color overlap — instead found: blue renders normally, then the map goes fully blank (no polyline, no road labels, as if tiles were reloading) for roughly 4-5 seconds, then green appears along with what looks like a POI re-tile. This pattern — a full reset, not a partial color swap — points to `LiveNavigationMap`'s reroute-on-deviation effect (`isLocationOnEdge` check, `NAV_REROUTE_TOLERANCE_DEGREES` ~100m, `NAV_REROUTE_DEBOUNCE_MS` 12s) actually firing and calling `computeRoute()` again mid-simulation, which resets `currentStepIndex`/`currentLegIndex` to 0 and refetches from `DirectionsService`. Leading theory: the simulation replays a route captured _once_ (`scripts/fixtures/DR-0020-legs.json`), but the live page computes its **own fresh route** independently each mount — Google's `DirectionsService` doesn't always return an identical path for the same origin/destination on repeated calls, so the dense fixture's points can drift just far enough off the live page's freshly-computed path near a turn to spuriously trip the "you've left the route" check, even though nothing is actually wrong. **Not confirmed** whether the user's own observation (colors overlapping) matches this repro's finding (a blank gap, not an overlap) — that's the next thing to confirm before deciding whether this needs a real fix (loosen `NAV_REROUTE_TOLERANCE_DEGREES`/`NAV_REROUTE_DEBOUNCE_MS`, or suppress reroute checks specifically during the test simulation) or is a simulation-fidelity artifact not worth changing production logic for. `scripts/repro-dual-route.mjs` is a disposable diagnostic script (not documented in `scripts/README.md`'s main list, unlike the permanent DR-0020 tooling) — rerun it to get fresh screenshots (`scripts/repro-NN.png`, gitignored/not committed) if picking this investigation back up.
  - **Real root cause found and fixed 2026-08-13, on a second pass through this same investigation** (code review, not a fresh repro): independent of whichever visual artifact the original report/repro were each seeing, `computeRoute`'s reroute path (`waypoints.slice(currentLegIndex)`) always resets `currentLegIndex` back to `0` on success — necessary, since it has to index into the _new_, shorter `directions` object's own `legs[]`. But every value that keyed off `currentLegIndex` for _display_ (the polyline's `NAV_LEG_COLORS` index, the "Stop X of Y" text, `totalStopLegs` itself via `legs.length - 1`, `isOnFinalLeg`) read that same reset-to-0 value directly — so any reroute (which the entry above already established can fire spuriously off ordinary GPS noise/route drift, not just the simulation fixture) made the rendered leg's color jump backward to whatever "leg 0" renders as, the stop counter revert to "Stop 1," and the total count itself shrink (since a resliced `waypoints` list is shorter) — all while the driver hadn't actually regressed at all. This plausibly explains a driver perceiving "a different/wrong route showing" independent of the repro's separate blank-tile-gap finding, since it's a color/label _identity_ bug, not a rendering-timing one. Fixed by adding `completedLegsOffset` state (`LiveNavigationMap`, holds how many real-world legs preceded the _current_ `directions` object's own leg-0), accumulated on each reroute (`completedLegsOffset + currentLegIndex`, captured right before the slice/recompute) and passed into a new `legOffset` param on `computeRoute`; every display-facing computation now uses `absoluteLegIndex = completedLegsOffset + currentLegIndex` instead of the raw, reroute-resettable value, and `totalStopLegs` now reads directly off the stable `stops` prop instead of the reroute-shrinkable `legs.length - 1`. `currentLegIndex` itself is untouched and still used exactly as before for anything that has to index into the current `directions` object's own arrays (step advance, `legEnd` pin-matching). Build/lint-verified (`npm run build`, `eslint`).
  - **Live-verified 2026-08-13** against a fresh disposable fixture, `DR-0022` (short 2-stop route, driver `D002`, truck `AAA 1111`/device `DV-1114` — retired/cancelled afterward, `gps_logs` cleared, same as `DR-0020`/`DR-0021`). New diagnostic script `scripts/repro-reroute-offset-fix.mjs` (kept in the repo as reusable disposable tooling, same precedent as `repro-dual-route.mjs` — not in `scripts/README.md`'s main list): captures the live page's own real `DirectionsService` route (`page.addInitScript`, not `page.evaluate` — the class-replace patch has to survive the post-login navigation, `page.evaluate` alone gets wiped by it), drives simulated GPS through leg 0 into leg 1 normally, then injects one point ~500m off the real route to deliberately force the reroute-on-deviation effect, and reads the "Stop X of Y" label before/during/after. **Confirmed PASS**: label read `Stop 2 of 2` before, immediately after, and following the forced reroute — unchanged throughout, not reverted to `Stop 1 of 2` the way it would have before this fix. Screenshots confirmed the reroute genuinely fired (visibly different polyline geometry post-reroute, marker off the old path) and stayed the correct green (leg-1 color), not reverted to red (leg-0 color).

- **GPS source split for driver live nav, decided and implemented 2026-08-31, not yet click-tested live:** `LiveNavigationMap`'s `livePosition` (marker, camera, rerouting, arrival detection, ETA, and the rest-stop distance accumulator) now sources primarily from the driver's own phone GPS (`navigator.geolocation.watchPosition`, client-side only, never written to `gps_logs`), falling back to the existing `gps_logs` Realtime subscription only when the phone has no reading (permission denied, no signal, unsupported, or non-HTTPS). Supervisor Dashboard, truck mileage, and Route Comparison are untouched — all still read `gps_logs` exclusively, still written only by the Pi via `gps-upload`. See `IMPLEMENTATION/01_SYSTEM_ARCHITECTURE.md`'s "Live position source changed" note and `IMPLEMENTATION/05_GPS_PIPELINE.md`'s "GPS Source Split" note for the reasoning. Build/lint-verified (`npm run build`, `eslint`); **not yet tested on a real phone in the browser** — needs a live check that the permission prompt fires correctly (requires HTTPS in production) and that the Pi fallback actually kicks in when location is denied.

- **Supervisor critical-alert popup + sound, decided and implemented 2026-09-02, not yet live-tested:** `CriticalAlertPopup` (new, `src/components/CriticalAlertPopup.jsx`), wired into `SupDashboard.jsx`'s existing `alerts`/`gps_logs` Realtime subscriptions. Deliberately narrow, per the user's own anti-spam requirement — only two named triggers reach the popup, everything else stays in the existing passive `DriverSafetyList`/`PausedMovementBanner` feeds: (1) **Very High Risk of Drowsiness** — a Session accumulating 5 alerts total, excluding `face_not_detected` (a detection/camera issue, not a drowsiness event) — cumulative count, not a rolling time window, queried directly against the DB rather than off the capped-at-50 `recentAlerts` feed; (2) **Very High Possibility of Route Deviation** — new logic, built from scratch (no real-time deviation detection existed for supervisors before this; only the Driver's own client-side check and a post-trip report existed) — on each `gps_logs` INSERT during an open Session, checks distance from the delivery's saved `suggested_route` via `isLocationOnEdge`, deliberately looser (`SUP_ROUTE_DEVIATION_TOLERANCE_DEGREES` ~500m) than `LiveNavigationMap`'s own ~100m reroute tolerance, since this is an attention-grabbing popup, not a routing decision — fires once per Session (not on every subsequent off-route tick). Both reuse `DriverDeliveries.jsx`'s existing audio-unlock pattern verbatim (mute-play-pause on first click to satisfy browser autoplay rules, plays the clip twice) and the Driver portal's existing "5+ Multiple Alert.mp3" clip rather than a new asset. The popup's "View Delivery" link navigates to `/supervisor/deliveries` with `state: { openRequestId }`; `SupDeliveries.jsx` now reads that on mount and auto-opens the matching request's detail view (new effect, fires once via `openedFromNotificationRef`). Scoped to `SupDashboard.jsx` only, not `SupDeliveries.jsx` (the latter has no Realtime `alerts`/`gps_logs` subscription of its own). Build/lint clean (one new `set-state-in-effect` lint error, same already-tolerated class as ~40 pre-existing instances elsewhere in this codebase, confirmed via lint diff). **Not yet live-tested** — needs a real session reaching 5 non-face alerts and a real truck moving >500m off a saved `suggested_route` to confirm both actually fire; neither is easily reproducible without a live driving simulation.
  - **Live-tested 2026-09-03** via a new disposable Playwright script, `scripts/verify-critical-alerts.mjs` (kept, reusable): signs in as Supervisor via a generated magic link (no password needed), inserts 4 then a 5th `prolonged_eye_closure` alert directly into `alerts` for a real Active session (`DR-0051`), and a deviating `gps_logs` point. **First run found both a real product bug and a real test-infra bug, neither previously known:**
    - The "exactly 5" trigger (`SupDashboard.jsx`, `if (count !== 5) return;`) had never actually fired in testing because the test session's `alerts` had silently accumulated 25-35+ stray rows across repeated runs — `scripts/verify-critical-alerts.mjs`'s own cleanup step (`admin.from('alerts').delete()...`) was failing every time with `permission denied for table alerts`, since `service_role` had no `DELETE` grant on `public.alerts` (only `select`/`insert` had ever been granted, going back to Phase 6). The cleanup's own log line printed "Cleaned up N alerts" regardless, since the delete's result was never checked — so this had been silently broken since the popup was built, not something new. Fixed via new migration `supabase/migrations/20260903035705_alerts_service_role_delete_grant.sql` (`grant select, insert, update, delete on public.alerts to service_role;`), pushed with `npx supabase db push --linked`. Confirmed fixed: re-run showed the popup correctly staying hidden at 4 alerts and firing exactly at 5 ("VERY HIGH RISK OF DROWSINESS — AAA 1111 (DR-0051) has logged 5 drowsiness alerts this trip."), "View Delivery →" navigating correctly, and the route-deviation popup firing on the off-route point — all confirmed via screenshots.
  - **Per the user, 2026-09-03: the popup's audio was wrong** — it was reusing the Driver portal's own `5+ Multiple Alert.mp3` clip verbatim, so a Supervisor and a Driver hearing an alert at the same moment couldn't tell them apart by sound. Replaced with a synthesized two-tone descending siren via the Web Audio API (`CriticalAlertPopup.jsx`'s new `playCriticalTone`, no new asset) — unique to this popup, unlock still fires on the Supervisor's first click anywhere on the page (`AudioContext.resume()` now, instead of the old `<audio>` mute-play-pause trick). `scripts/verify-critical-alerts.mjs` updated to match: an `AudioContext` spy (`page.addInitScript`, counts `oscillator.start()` calls) replaces the old `<audio>`-element playback check.
- **Two real bugs found and fixed in `SupDashboard.jsx`'s Driver Safety — Drowsiness Alerts list, 2026-09-03** (found while manually testing the critical-alert popup above, not part of its own scope):
  1. **"This Trip" alert count was a flat total, not a running tally** — `tripAlerts: countBySession[a.session_id] || 1` counted every occurrence of a session across the whole (capped-at-50) `recentAlerts` array and stamped that same final total onto *every* row of that session, so three consecutive alerts from the same trip all read "14 alerts" instead of progressing 12 → 13 → 14. Fixed: `alertFeed`'s `useMemo` now sorts `recentAlerts` oldest-first once, keeps a running per-session counter, and looks up each alert's own count-as-of-that-alert by id — verified live (inserted 3 fresh alerts, counts read 1, 2, 3 across the three new rows).
  2. **No pagination** — the list rendered every matching alert in one unbounded table. Added `DRIVER_SAFETY_PAGE_SIZE = 15` and a `DriverSafetyPaginationBar`, copied verbatim (same first/prev/numbered-with-ellipsis/next/last shape, sized down to fit inside a `Panel`) from `SupTrucks.jsx`'s existing `PaginationBar` — the user explicitly asked for this exact format to be reused rather than a new pattern.
- **PMS tagging made visible + a real cross-page staleness bug fixed, 2026-09-03** (`SupTrucks.jsx`, `AdminTrucks.jsx`, `SupDashboard.jsx`) — prompted by manually testing PMS Overdue (bumping a test truck's `current_mileage` past its interval) and finding the Trucks page's own Truck Profile "Maintenance Status" card is a **different, intentionally separate** computation (driven by the latest `maintenance_records` row's own status, not `getPmsStatus`) — not a bug, just two different concepts that look confusingly similar; documented as such in code comments, not merged (a truck can legitimately be `Available` and PMS-`Overdue` at once — those are independent axes, keeping them separate avoids inventing composite statuses that would ripple into every other place `status` is used for assignment eligibility).
  - Added a **PMS badge column** (`PmsTag`, red/amber/emerald pill matching `MaintenanceSummaryWidget`'s existing card colors) and a **PMS Status filter dropdown** to both `SupTrucks.jsx` and `AdminTrucks.jsx` — the underlying `getPmsStatus`-based filtering already existed (reachable only via the Dashboard's "PMS Overdue" KPI-tile deep-link or the summary cards), but there was previously no way to see *why* a truck was tagged Overdue, or to filter by it directly, from the Trucks page itself. `AdminTrucks.jsx` also gained the `location.state?.pmsFilter` read-once-on-mount pattern `SupTrucks.jsx` already had, so `AdminDashboard.jsx`'s own PMS Overdue tile (see below) can deep-link into it pre-filtered.
  - **Real bug found while testing this**: a truck (`MIT 300`) freshly serviced (`maintenance_records` completed 2026-08-28) still showed as Overdue on the Supervisor Dashboard's KPI tile (using stale raw `trucks.previous_maintenance_date`, 2026-01-10) while correctly showing as not-overdue on the Trucks page (which runs every fetch through `addMaintenanceBaselines`, correcting `previous_mileage`/`previous_maintenance_date` from the truck's actual latest completed maintenance record) — a genuine disagreement between two surfaces reading the same underlying fact differently. Root cause: **`refreshTrucks()` in both `SupTrucks.jsx` and `AdminTrucks.jsx`** (fired on manual refresh and after every truck edit) fetched `trucks` raw, skipping `addMaintenanceBaselines` entirely — unlike each file's own initial-load effect and Realtime subscription, which both already called it correctly. Fixed by making `refreshTrucks()` fetch `maintenance_records` alongside `trucks` and apply `addMaintenanceBaselines` the same way, in both files. `SupDashboard.jsx`'s own truck fetch (`loadRefs`, feeds the PMS Overdue KPI tile) had never applied baselines at all — fixed the same way. Confirmed live: Dashboard tile and Trucks list now agree (5, MIT 300 correctly excluded) after the fix.
- **"Admin Analysis" renamed and rebuilt into "Dashboard," 2026-09-03** (`AdminAnalysis.jsx` deleted, replaced by new `src/pages/AdminDashboard.jsx`; route `/admin/analysis` → `/admin/dashboard`; `AdminLayout.jsx` sidebar entry relabeled) — the old page was entirely mock data (hardcoded fictional driver "Juan D. Santos," fabricated KPIs/sessions/alerts), per explicit user request to redesign it as a real, functional page mirroring the Supervisor Dashboard's live-data panels. New page reuses `SupDashboard.jsx`'s `useFleetOps` pattern near-verbatim (same per-role-duplicate-page convention already established by `AdminTrucks.jsx`/`SupTrucks.jsx` and the two `*TruckProfile.jsx` pages, rather than sharing one parameterized component) — real KPI strip (Active Deliveries/Alerts Today/High-Risk Drivers/Fleet Available/PMS Overdue, computed from live `deliveries`/`alerts`/`trucks`, not mock), Active Deliveries table, Live Fleet status bars, Live GPS map (with fullscreen), paused-but-moving anomaly banner, paginated Driver Safety — Drowsiness Alerts (same running-count/pagination fix as above), Recent Activity and Weekly Safety Summary now driven by real alert data instead of hardcoded arrays. Two deliberate differences from the Supervisor version, both per explicit user direction:
  1. **No `CriticalAlertPopup`** (the siren/popup from the entry above) at all — per the user, that notification is strictly a Supervisor-only surface. `AdminDashboard.jsx`'s own copy of `useFleetOps` also omits the underlying detection work that only existed to feed that popup (the 5-alert-count check and the route-deviation check inside the `gps_logs` INSERT handler), not just the rendering.
  2. **Admin has no `/admin/deliveries` or `/admin/delivery-crew` routes at all** (only `user-management`, `device-management`, `trucks`, `profile`) — decided with the user to render the same real data non-interactively (no click-through) rather than invent new routes or point links at the wrong page. Only `/admin/trucks` links stay live (Fleet Available, PMS Overdue tiles; Live Fleet's "View all"), since it actually exists. `CriticalAlertPopup.jsx` itself gained a new `deliveryLinkTo` prop (default `/supervisor/deliveries`, pass `null` to omit) to support this without duplicating the component — unused here since the popup isn't rendered on this page at all, but kept for symmetry/future reuse.
  - **Live-verified 2026-09-03** via a new script, `scripts/verify-admin-dashboard.mjs` (magic-link sign-in as `alexisyvone@gmail.com`, same pattern as the critical-alerts script): confirmed all real panels render with no page errors, confirmed `CriticalAlertPopup` is genuinely absent from the DOM (not just visually hidden), and confirmed clicking the PMS Overdue tile correctly deep-links into `/admin/trucks` pre-filtered (5 trucks shown, all tagged Overdue, matching the tile's count).
  - **Correction, 2026-09-04**: that verification's account choice was silently broken and this went unnoticed until the 2026-09-04 phone-GPS-parity work below re-ran the same kind of test. `alexisyvone@gmail.com` (used because `admin_records`' `A002` row lists it) is a real, separate Supabase Auth user with **no corresponding `public.users` row at all** — `admin_records.A002.auth_id` actually points to a *different*, unrelated auth user (the Supervisor test account's own id, `atdrilon01@marveltrucking.local`'s), a pre-existing data inconsistency this didn't create. With no `public.users` row, `current_user_role()` resolves to nothing for that session, so every role-gated table (`devices`/`sessions`/`delivery_requests`) 403'd silently the whole time — masked because Active Deliveries' empty-state ("No deliveries in progress") and `trucks`' differently-gated PMS Overdue count both still rendered *something* plausible-looking. Re-verified 2026-09-04 against a genuinely working Admin account (`admin@drivewise.com`, real `users` row, `role = 'Admin'`, confirmed via `auth.admin.listUsers`) — this time Active Deliveries showed 3 real rows, Live Fleet showed real truck counts, and Driver Safety showed real alert rows, none of which the earlier (broken-account) run ever actually exercised. `scripts/verify-admin-dashboard.mjs` and the newer `scripts/verify-phone-gps-admin.mjs` (see below) now both use `admin@drivewise.com`. No code changes needed — `AdminDashboard.jsx`'s own logic was always correct and correctly Admin-gated; only the test account was wrong.
  - **Known limitation, not fixed**: `crewStatus` (Live Fleet's crew row) is a placeholder (`{ available: 0, onDelivery: 0, offDuty: 0 }`) — no Admin-reachable query returns crew duty status today (`list-crew` only returns names), same "mock until a real source exists" gap `SupDashboard.jsx`'s own crew row had before Phase 8 wired in real truck/PMS data. Not part of this task's scope.
- **Stale rest-stop docs corrected, 2026-09-03** — a docs-only audit (asked "what's missing across `IMPLEMENTATION/`") surfaced that `03_START_TRIP_AND_SESSION.md`'s "Rest Stop Recommendation" section had never been updated after `12_REST_STOP_RECOMMENDATIONS.md` superseded it (2026-08-13) with a 200-mile-OR-2-hour dual threshold — `03` still described the original 2-hour-only design as current. Also stale: `10_TESTING_CHECKLIST.md`'s Rest Stop section only covered the 2-hour half (no mention of the 200-mile leg at all), and `09_EDGE_CASES.md`'s Phase 9 pre-work notes (written 2026-08-12, before the feature was built 2026-08-13/14) still claimed "the underlying rest-stop notification itself isn't built at all yet." All three corrected to point at `12_REST_STOP_RECOMMENDATIONS.md` as the source of truth rather than duplicating full detail that could drift again. No code changed — docs only.
- **Supervisor Dashboard can now optionally track a truck via the driver's own phone GPS, live-verified 2026-09-03** — per explicit user request, specifically to make live-tracking easier to demo for panelists/testing without needing real Pi hardware physically present. Deliberately **not** implemented by writing phone GPS into `gps_logs` (would have undermined the exact rationale `05_GPS_PIPELINE.md`'s "GPS Source Split" note already established when this same phone/Pi split was built for the Driver's own nav view 2026-08-31 — `gps_logs` stays the sole authoritative, device-attributed record backing truck mileage and Route Comparison). Instead: a new ephemeral Supabase Realtime **broadcast** channel per Trip (`phone-gps-<deliveryId>`, no schema/grant/RLS changes needed — broadcast is independent of table Realtime publications entirely). `DriverDeliveries.jsx`'s existing phone-GPS `watchPosition` effect now also sends each reading over this channel (gated on `isMonitoring`, same as the effect itself); `SupDashboard.jsx` joins/leaves one channel per in-progress delivery as the `deliveries` list changes, and `fleetOps` now prefers a broadcast position over the `gps_logs`-sourced one when received within the last 20s (`PHONE_POSITION_STALE_MS`), falling back to the Pi feed otherwise — same fallback shape as the Driver's own phone/Pi split. `movementMeters`/`isAnomalous` (the paused-but-moving anti-theft check) deliberately stay `gps_logs`-only, since that check is keyed on the Pi's own `session_id is null` marker, which a phone broadcast has no equivalent for. Added a `positionSource` field (`"phone"`/`"pi"`) surfaced two ways for demo visibility: a small "📱 Phone GPS"/"📡 Pi GPS" label under each truck's Device column in `ActiveDeliveries`, and in the Live GPS map marker's hover title.
  - **One real race found and fixed while testing**: the channel-join effect assigned `phoneBroadcastChannelRef.current = channel` synchronously, right after calling `.subscribe()` — so if `watchPosition`'s first tick landed before the channel actually reached `SUBSCRIBED` (a real possibility, not just a test artifact), `channel.send()` silently no-ops before the channel is ready, and the very first reading was lost with no retry. Fixed: `.subscribe()`'s own status callback now resends the latest known position (`latestPhonePositionRef`, mirrored on every tick) once it actually reports `SUBSCRIBED`.
  - **Live-verified 2026-09-03** via a new script, `scripts/verify-phone-gps-supervisor.mjs`: two real browser contexts — a real Driver login (`D002`) with Playwright's mocked `geolocation` against DR-0051's real Active session, and a Supervisor via magic link. Confirmed end-to-end: the Supervisor's dashboard received the broadcast, showed "📱 Phone GPS" for the correct truck (AAA 1111), and the map marker's title read `"AAA 1111 — Alexis Yvone Roque (Phone GPS)"`; a second truck with no phone broadcast correctly still showed "📡 Pi GPS", confirming the fallback works side-by-side, not just in isolation. One test-only gotcha worth noting for future reruns, not a product bug: Playwright's mocked geolocation only fires `watchPosition`'s callback once for a static value (real GPS hardware re-fires continuously regardless of movement), so the script has to nudge the mocked coordinate (`context.setGeolocation`) after the Supervisor page connects to simulate that natural next real-GPS tick — broadcasts have no replay/queueing, so a message sent before a listener subscribes is gone for good (self-heals within ~1-2s in production against a real phone, not an issue there).
- **Same phone-GPS live-tracking parity added to the Admin Dashboard, 2026-09-04** (per user request, "also do it with admin map too") — `AdminDashboard.jsx`'s `useFleetOps`/`fleetOps`/`ActiveDeliveries`/`LiveFleetMap` all mirror `SupDashboard.jsx`'s phone-GPS-broadcast-with-Pi-fallback exactly (same `phone-gps-<deliveryId>` channel, same `PHONE_POSITION_STALE_MS` 20s freshness window, same `positionSource` field/labels/marker-title). No new design decisions — a straight port of the 2026-09-03 entry above, since `AdminDashboard.jsx` was built (2026-09-03) before that feature existed and had no way to inherit it automatically (separate per-role page, not a shared component, per this codebase's established convention).
  - **Real gap found while verifying this, unrelated to the phone-GPS work itself**: the Admin test account used for the *original* AdminDashboard verification (`alexisyvone@gmail.com`, `scripts/verify-admin-dashboard.mjs`, 2026-09-03) turned out to have no `public.users` row at all — see the correction note on that entry above. Discovered because this phone-GPS parity test, run against the same broken account first, silently 403'd on `devices`/`sessions`/`delivery_requests` with no visible error (Active Deliveries just rendered its normal empty state). Re-verified 2026-09-04 via a new script, `scripts/verify-phone-gps-admin.mjs` (same shape as the Supervisor version, against `admin@drivewise.com`): confirmed "📱 Phone GPS" showing for the correct truck, marker title reading `"AAA 1111 — Alexis Yvone Roque (Phone GPS)"`, and — unlike the earlier broken-account run — real populated data throughout (3 real Active Deliveries rows, real Live Fleet counts, real Driver Safety alert rows). Re-ran the original `verify-admin-dashboard.mjs` too (also switched to `admin@drivewise.com`) to confirm nothing regressed — all checks still pass.
- **Customer portal's "Progress Details" now shows real, sequential pickup/drop-off location updates under Out for Delivery, live-verified 2026-09-04** — per explicit user request: with no live truck navigation on the Customer side, this is how a customer knows the crew's current pickup/drop-off status. Found while scoping this that the existing `transit` stage substeps (`CustomerDeliveries.jsx`'s `buildCustomerTimeline`) were **dead code** — they read `request.trip?.actualPickup`/`actualDropoff`, but nothing anywhere on the customer side ever populates `request.trip` (a leftover reference from an old placeholder shape; same for the unrelated "Trip Details" panel's `request.trip.onTime`/`.distance`/etc., also always-undefined today). Replaced with a new `buildTransitSubsteps`, built from the same real fields `ProofOfDeliverySection` already uses successfully: `pickupPhotoUrl`/`dropoffPhotoUrl`/`stops[i].completed` drive which chain items (Pickup → primary Dropoff → each `stops[i]` in order, matching `02B_MULTI_STOP_DELIVERIES.md`'s real chain order and `ProofOfDeliverySection`'s existing "Dropoff N" labeling) are done vs. next; "on its way to X" is inferred client-side from chain progress (no live position/ETA involved) for the single next incomplete item only, never more than one at a time.
  - **Real schema gap found and fixed**: there was no dedicated timestamp for pickup completion — only `dropoff_completed_at` existed (the dropoff/each stop's own `completedAt`), pickup only ever got `pickup_photo_url` + the generic `updated_at`. Per the user's explicit choice, added `pickup_completed_at` (new migration `20260904091323_delivery_requests_pickup_completed_at.sql`, mirrors `dropoff_completed_at`'s own precedent exactly) and wired it into `admin-users`' Confirm Pickup handler (the `update-driver-delivery` branch that sets `pickup_photo_url`) to write it alongside the photo. Also added `pickupCompletedAt` to `get-driver-deliveries`/`get-helper-deliveries`' response shape for consistency with `dropoffCompletedAt` (unused by this task, but the same sibling field was already returned there for dropoff). `admin-users` redeployed.
  - **Live-verified 2026-09-04** via a new script, `scripts/verify-customer-progress-details.mjs`, against `DR-0048` (a real customer's real delivery with a real human-readable address for its one stop, not a coordinate-string test fixture) — backs up and restores the exact original row afterward. Confirmed all three stages render correctly and in order: "The delivery crew has completed the pickup from `<address>`." with a real timestamp, then "...is on its way to `<next address>` (First Drop-Off Location)."; once that's also done, "...arrived at `<address>` (First Drop-Off Location)." plus "...on its way to `<stop address>` (Second Drop-Off Location)."; once the stop completes too, its own "arrived at" line with no dangling "on its way" line left over. Screenshots confirmed the customer-facing Delivery Timeline panel renders this correctly end to end.
  - **Two real bugs found on a follow-up review (2026-09-04, asked "check if there are bugs"), both fixed and re-verified live:**
    1. **Premature substep disclosure** — `buildTransitSubsteps` computed the "next up" item unconditionally, including pickup itself, so a delivery still in "Scheduled for Pickup" (pickup not yet confirmed) would already show "The delivery crew is on its way to `<pickup location>`." under the still-greyed-out "Out for Delivery" heading — violating this same file's own stated rule ("a sub-event is only ever added to a stage's substeps once it has actually happened," in `buildCustomerTimeline`'s doc comment) and impossible to reach in practice anyway, since `CUSTOMER_STATUS_MAP` only ever surfaces "Out for Delivery" as the customer's current status once `dbStatus` already reached `OUT_FOR_DROPOFF` — by which point pickup is always already done. Fixed: the chain walk now stops (no substep pushed at all) the moment it reaches an incomplete `pickup` item, instead of treating it like any other not-yet-done chain item.
    2. **Misleading ordinal on a single-dropoff delivery** — the primary dropoff was unconditionally labeled "(First Drop-Off Location)", even for a delivery with zero `stops` (there will never be a "Second"). Fixed: the ordinal qualifier is now only added when `stops.length > 0` (a real second-or-later drop-off actually exists in the chain); a single-dropoff delivery now just reads "...on its way to `<location>`." with no ordinal.
    - Re-verified via the same script, extended with two more stages: confirmed zero substeps render before pickup is done, and confirmed a zero-stop delivery's dropoff message carries no "First Drop-Off" wording. All five stages (0, 0b, 1, 2, 3) pass.

- **Supervisor Route Review & Approval feature (2026-09-06) partially reverted, 2026-09-08, per explicit user decision: "no more approving, just him viewing and no more adjusting of the pins."** The route-generation-at-creation-time half of that feature stays (`CustomerRequestDelivery.jsx` still auto-generates `suggested_route` for every request); the review/edit/approve half is removed entirely — the Supervisor no longer drags pins or clicks Approve, only views the same route the Customer sees.
  - `src/components/EditableRouteMap.jsx` deleted; replaced by a new, genuinely read-only `src/components/SuggestedRouteMap.jsx` (no `editable` prop, no drag handlers, no Generate/Regenerate/Approve buttons, no draft state) — shared between `SupDeliveries.jsx` and `CustomerDeliveries.jsx`'s `ApprovedRouteMap`, same sharing precedent the old component already established.
  - `route_approved_at`/`route_approved_by` columns dropped (`20260908090000_delivery_requests_drop_route_approval.sql`) along with every read/write of them: `SupDeliveries.jsx`'s `approveRoute` function and its row-mapping fields, `CustomerDeliveries.jsx`'s `ApprovedRouteMap` gate (now shows the route as soon as `suggestedRoute` exists, no approval gate), `DriverDeliveries.jsx`'s `PlannedRouteMap` prop/skip-recompute check, `admin-users`' `get-driver-deliveries` row mapping, and `driver-trip`'s `save-suggested-route` freeze guard.
  - `suggested_route` is frozen by mere presence again — both `PlannedRouteMap`'s skip-recompute effect and `save-suggested-route`'s guard reverted from checking `route_approved_at` back to checking `suggested_route` itself, which was this column's original rule before the 2026-09-06 feature ever existed. Functionally this restores the Driver's pre-trip screen as the sole fallback if creation-time generation ever fails, same role it always had.
  - `scripts/verify-route-approval-feature.mjs` (the disposable Playwright script built for the old drag/approve flow) deleted, since it no longer reflects real app behavior.
  - **Side effect, worth noting**: this incidentally closes a related gap found the same day while investigating a separate "planned route doesn't show" report — `PlannedRouteMap`'s old `if (routeApprovedAt) return;` early-exit meant a delivery with `route_approved_at` set but `suggested_route` null/missing (unreachable through the Supervisor's own UI, since Approve was disabled until a route existed and the write was atomic, but reachable via direct DB edits) rendered a silently blank map with zero error feedback. That whole code path no longer exists.
  - Docs updated to match: `DATABASE.md`'s `suggested_route`/`route_approved_at`/`route_approved_by` entries, `IMPLEMENTATION/11_ROUTE_COMPARISON.md`'s 2026-09-06 correction note, and the route-generation comment in `CustomerRequestDelivery.jsx`. Not yet click-tested live in a browser after this change — build-verified only so far.

- **Critical live-breaking bug found and fixed, 2026-09-08, while verifying a teammate's merged `truck_status_sync` trigger** (`20260908000000_truck_status_sync.sql` — new `AFTER INSERT OR UPDATE OF status` trigger on `delivery_requests` that syncs the assigned truck's `trucks.status` to `'Active'` on entering any transit status, back to `'Available'` on `COMPLETED`/`CANCELLED` if no other in-progress delivery shares the truck). The trigger set `trucks.status = 'Active'`, but the live `trucks_status_check` CHECK constraint never allowed that value — confirmed directly (a raw `update trucks set status = 'Active'` failed the identical way, independent of the trigger). Since the trigger runs `AFTER UPDATE` on `delivery_requests`, its constraint violation rolled back the *entire* triggering transaction, including the `delivery_requests.status` write itself — meaning **"Start Pickup" (and every other transition into `OUT_FOR_PICKUP`/`ARRIVED_PICKUP`/`OUT_FOR_DROPOFF`/`ARRIVED_DROPOFF`) hard-failed live for any delivery with an assigned truck**, from the moment the trigger was deployed until this fix.
  - **Pre-existing, not introduced by the trigger**: `AddTruckModal.jsx`'s own status dropdown already offered `"Active"` (displayed as "On Delivery") as a manually-selectable option — it would have failed identically, just never exercised until this trigger started hitting it on every delivery status change. Confirmed via the app's own literal status strings (`SupTrucks.jsx`, `AdminTrucks.jsx`, `AddTruckModal.jsx`, `EditDeviceModal.jsx`), not guessed, that exactly four values are ever written to `trucks.status` anywhere: `Available`, `Active`, `Inactive`, `Maintenance` (`"On Delivery"` is only ever a display label for `Active`, never itself stored).
  - **Fix**: widened the constraint — `20260908100000_fix_trucks_status_check.sql`, `alter table public.trucks drop constraint if exists trucks_status_check; alter table public.trucks add constraint trucks_status_check check (status in ('Available', 'Active', 'Inactive', 'Maintenance'));`. Applied directly by the user via the SQL editor, migration file added afterward so the repo's migration history matches the live schema.
  - **Verified twice, both directions**: (1) direct DB-level test — flipped a delivery to `OUT_FOR_PICKUP` → truck correctly went `Active`; flipped to `COMPLETED` → truck correctly reverted to `Available`; both succeeded with no errors. (2) **Real browser UI test** (`scripts/tmp-verify-start-pickup.mjs`, disposable Playwright script, deleted after use) — logged in as driver `D002`, navigated to Driver Trips, clicked the actual "Start Pickup" button on `DR-0053` and its confirm modal, exactly as a real driver would: `delivery_requests.status` advanced `ASSIGNED → OUT_FOR_PICKUP` and `trucks.status` (`MIT 300`) advanced `Available → Active`, no errors. Fixture fully restored afterward (delivery row, truck status, and the session created by the real Start Pickup click — the latter via `UPDATE ... status = 'Completed'` rather than `DELETE`, see `SUPABASE_GOTCHAS.md` #11).
  - Also confirmed while investigating: the rest of the same merge (teammate's Supervisor Route Review & Approval partial revert, a delivery-requests id-sequence fast-forward, `SupCrewProfile.jsx` rewrite, new toast UI in `SupDeliveries.jsx`) introduced no other breakage — clean build, clean lint (one pre-existing unrelated unused-import style issue), no dangling references, live smoke-tested on Trucks/Crew pages.

- **`service_role` granted `delete` on `sessions`, 2026-09-08** — closes `SUPABASE_GOTCHAS.md` #11, which had recurred twice (2026-08-14, 2026-09-08) as the identical test-cleanup friction. `supabase/migrations/20260908110000_grant_sessions_delete_service_role.sql`.

- **Supervisor's completed-delivery report overhauled (2026-09-08)** — the "Delivery Request Details"/"Trip Details"/"DriveWise Report"/"Route Deviation Report" tabs in `SupDeliveries.jsx`'s `CompletedDeliveryReport`, per explicit user request. Supervisor portal only (Driver/Helper have a separate, differently-named parallel report, not touched beyond the one required fix below). AI-based Route Deviation analysis was scoped into this request but explicitly deferred by the user as a separate future item — not built.
  - **Restated 2026-09-09**, same original ask, still not built, but now fully spec'd and implementation-ready: integrate analysis that judges a detected route deviation as beneficial/reasonable/potentially-problematic, with supporting detail (planned vs. actual route, location, date/time, deviation duration). Direction confirmed: a **non-LLM, rule-based classifier**, not an actual AI/API call — computed purely from data already fetched for this tab, matching this report's own "derive from real data, never fabricate" convention (the `routeDeviation` field's no-fabrication comment, above) rather than introducing this repo's first LLM integration. Full concrete spec (exact thresholds, algorithm steps, output shape merged into the existing `routeDeviation` object, the one UI change needed in `RouteDeviationTab` for a third "Beneficial"/green tone, and a verification plan) written into the plan file — see plan section D for the ready-to-implement detail, not duplicated here to avoid the two documents drifting out of sync.
  - **Delivery Request Details is now request-form data only**: removed the "Trip Timeline" and "Proof of Delivery" blocks (trip-generated data, not booking-form data) and added a "Submitted" row (`delivery_requests.created_at`, previously fetched but never shown on this tab).
  - **Trip Details' "Distance"/"Duration" stats renamed** to "Total Distance"/"Total Trip Duration" — the latter's *meaning* also changed, from `sum(sessions.session_duration)` (excludes pauses) to true wall-clock elapsed time from trip start to the last real completion event (includes pauses), per explicit request. The old sum-of-sessions figure is kept internally for the DriveWise Report's own "time actually monitored" narrative, which is a different, intentionally-pause-excluding number.
  - **Route section rebuilt as one card per location** (Pickup, Drop-off, Drop-off 2...) instead of a single "Pickup → Dropoff" line — each shows Scheduled/Arrival/Departure time, that location's own Proof-of-Delivery photo (moved here from the old standalone section, also fixing a real pre-existing bug: Pickup's photo timestamp was hardcoded `null` even though `pickup_completed_at` has existed since 2026-09-04), and **trip pauses nested under whichever location was "current" when the pause happened**.
    - Pauses are *derived*, not stored — every gap between a delivery's adjacent `sessions` rows (`03B_PAUSE_AND_RESUME_TRIP.md`: "Paused" has no status of its own) is one pause window. Attributed to a location via a new shared `legForTimestamp` helper (also used by the DriveWise Report, see below) that buckets a timestamp against the delivery's own completion-event boundaries in chain order — best-effort, since there's no direct pause-to-leg link in the schema.
    - Each pause shows a **"Rest stop recommended" badge** (new `sessions.rest_stop_recommended` boolean, stamped by `driver-trip`'s `pause-trip` action from the Driver's already-tracked-but-previously-ephemeral rest-stop-banner state — `IMPLEMENTATION/12_REST_STOP_RECOMMENDATIONS.md` had deliberately kept this unpersisted; explicitly reversed here per this request) and an **"Unusually long — may warrant review" flag** (pure display-layer rule: `>3h` AND doesn't substantially overlap a lunch (11:30–13:30) or dinner (18:00–20:00) window, Asia/Manila — no persistence, no AI, matches the literal ask's own example).
    - **New real Arrival timestamps, going forward only**: a new, optional, **Driver-owned** "Arrived at Pickup"/"Arrived at Drop-off" button (`DriverDeliveries.jsx`) stamps `delivery_requests.pickup_arrived_at`/`dropoff_arrived_at` via `admin-users`'s `update-driver-delivery`, now extended to accept `OUT_FOR_PICKUP → ARRIVED_PICKUP` and `OUT_FOR_DROPOFF → ARRIVED_DROPOFF`. These two status values already existed in transition guards and the `truck_status_sync` trigger's IN-list but had never actually been set by any UI action before this. Deliberately skippable (Helper's Confirm Pickup/Complete Dropoff work identically either way) and Pickup + primary Drop-off only (no per-stop equivalent) — both explicit scope decisions. Older/skipped trips show "Arrival: not recorded for this trip" rather than a fabricated timestamp.
    - **Safety audit done before shipping** (since this is the first time these two statuses ever go live): every status-reading site in Driver/Helper/Supervisor/Customer/Sup+AdminDashboard/Sup+AdminTruckProfile/CustomerHome/the DB trigger was checked. Almost all of it already handled both values correctly. Two real gaps found and fixed as part of this same change: (1) `admin-users`'s `DRIVER_STATUS_TRANSITIONS` didn't allow either new transition at all (rejected outright); (2) **a real pre-existing bug in `HelperDeliveries.jsx`'s `chainItems`** — it gated Confirm Pickup/Complete Dropoff off the *mapped/collapsed* status, which folds `ARRIVED_PICKUP` into the same bucket as an already-departed pickup, so tapping the new Arrived button would have marked Pickup done and prematurely offered Confirm Dropoff before pickup was ever confirmed (the backend correctly rejects that specific call, so no data corruption, but the Helper would've been stuck with no working action). Fixed by keying `done`/`actionable` off `pickupCompletedAt` (ground truth) instead of the collapsed status, matching how dropoff/stop items already worked. Also fixed the mirrored risk on the Driver's own side: `activeNeedsPickup` (decides the live-nav map's target) was rekeyed the same way, so tapping "Arrived at Pickup" does **not** reroute the map to the dropoff before the Helper actually confirms pickup — confirmed as an explicit requirement, not just a bug fix.
    - **Live-verified 2026-09-08** through the real deployed pipeline (not just direct DB writes): a real Driver login (`ayroque01@drivewise.local`) ran `start-trip → OUT_FOR_PICKUP → ARRIVED_PICKUP` and separately `resume-trip → ARRIVED_DROPOFF`, both through the actual Edge Functions — `pickup_arrived_at`/`dropoff_arrived_at` stamped correctly, `truck_status_sync` kept the truck `Active` throughout, `pause-trip` with `restStopRecommended: true` correctly wrote `sessions.rest_stop_recommended = true`, and the backend transition guard correctly rejected an out-of-order status jump. Fixture (`DR-0053`) restored cleanly after each run.
    - **A second real bug found and fixed the same day, via an actual real-browser click-through** (not just API calls) of the new button: `DB_TO_DRIVER_STATUS` collapsed `ARRIVED_PICKUP` into the same `"OUT_FOR_DELIVERY"` bucket as an already-departed pickup (matching `OUT_FOR_DROPOFF`/`ARRIVED_DROPOFF`) — so tapping "Arrived at Pickup" made the Driver's own stage stepper jump straight from "PICKUP" to "DELIVERY" and the banner flip from "Waiting for the helper to confirm pickup." to "Waiting for the helper to complete the delivery chain.", before the Helper had confirmed anything. Same bug, independently, in `HelperDeliveries.jsx`'s own `DB_TO_HELPER_STATUS` (a different banner: "The driver is heading to the pickup location." → "...is delivering to the drop-off location."). **Fixed** by remapping `ARRIVED_PICKUP → "FOR_PICKUP"` (not `"OUT_FOR_DELIVERY"`) in both files' status maps — matches how `activeNeedsPickup`/`chainItems` were already keyed off `pickupCompletedAt` rather than the raw status, just extended to the stage-label layer too. Re-verified via the same real click-through afterward: stepper/banner/header now correctly stay on "Heading to Pickup" / "Waiting for the helper to confirm pickup." through the Arrived tap, only the intended toast ("Arrival recorded.") and button state change. Confirmed via screenshots before/after, not just log output.
    - **Full real two-actor end-to-end confirmation, 2026-09-09** — the one gap left after the fixes above: the `chainItems` fix itself had only ever been verified by manual logic tracing against real DB values, never an actual Helper click, since no safe Helper test credentials were available at the time. Closed once the user provided real Helper credentials (`tdtdurden01@marveltrucking.local`, helper `H003`, genuinely assigned to the test fixture `DR-0053`): a real Driver login ran Start Pickup → Arrived at Pickup for real in the browser, then a real, separate Helper login opened their Trips page and confirmed — exactly as the fix intended — **exactly one "Complete" button was visible, for Pickup only** (Dropoff correctly showed no button, not prematurely actionable), clicked it, the modal correctly read "Complete Pickup" (not Dropoff), uploaded a real photo via the file input, submitted successfully with zero console errors, and the delivery correctly advanced to `OUT_FOR_DROPOFF` with `pickup_completed_at`/`pickup_photo_url` both stamped — at which point Dropoff's own "Complete" button correctly appeared for the first time. Screenshots confirmed both the before state (Pickup actionable, Dropoff not) and after state ("Pickup Confirmed" toast, Dropoff now actionable). Fixture restored cleanly afterward.
    - **A third, unrelated, pre-existing bug found during this same follow-up sweep** — see the `crewStatus.js` entry below (`CREW_ACTIVE_STATUSES` bug), found by grepping for every other place the two new statuses (or the fake `"OUT_FOR_DELIVERY"` string) appear across the codebase, beyond just the files this feature directly touches.
  - **"Trip Timeline" → "Progress Timeline"**: relabeled, not merged with the separate shipment-status stepper (explicit decision — same content, new name only). Since it's also removed from the Details tab (above), there is now exactly one timeline in the whole report.
  - **"Trip Stops" section removed** — fully redundant with the new per-location Route cards.
  - **Delivery Crew now explicitly labeled**: "Driver" above the driver's name, "Helper" or "Helper/s" above the helper chips depending on count.
  - **DriveWise Report's alert history now shows each alert's location** — nearest-`gps_logs`-timestamp match within the same session (data already fetched for the Route Deviation tab, just not previously correlated per-alert) plus the same `legForTimestamp` leg-bucketing used for pauses above, rendered as e.g. "En route to / at Drop-off" under each alert row.
  - **Live-verified end-to-end in the browser** (real Supervisor login via a server-verified magic-link session — the sandboxed browser can't reach the external Supabase auth host directly for a full-page navigation, so the OTP was verified server-side in Node and the resulting session tokens injected into the page's own storage instead) against three real completed deliveries: `DR-0021` (Details/Trip tabs, 2 real pauses correctly nested under Drop-off, POD/timeline/crew labels all correct, zero console errors) and `DR-0040` (DriveWise Report, 31 real alerts, each correctly labeled "En route to / at Drop-off"). Screenshots confirmed, not just build/lint-verified.
  - New migrations: `20260908120000_delivery_requests_arrival_timestamps.sql`, `20260908120100_sessions_rest_stop_recommended.sql`.
  - **Full five-portal regression sweep, 2026-09-08** (Admin/Supervisor/Driver/Helper/Customer, real logins, all main pages plus a deep dive into the rewritten Completed Deliveries report's all 5 tabs and the In Transit live view) — zero real errors anywhere; the only console output anywhere was the known-benign Google Maps WebGL/Vector-Map-fallback notice on the two dashboard pages with live maps.
  - **A third, unrelated, pre-existing bug found during a follow-up "check for more bugs" pass, 2026-09-09** — `src/lib/crewStatus.js`'s `CREW_ACTIVE_STATUSES` (used by `SupCrewProfile.jsx`, `SupDeliveries.jsx`, and `SupDeliveryCrew.jsx` to flag a driver/helper as currently "Assigned"/busy on a real trip, feeding the crew-availability badges and double-booking prevention) listed `'OUT_FOR_DELIVERY'` — a string that **never appears as a real `delivery_requests.status` value** (it's only ever the Driver/Helper UI's own internal collapsed label, `DB_TO_DRIVER_STATUS`/`DB_TO_HELPER_STATUS`'s output, not a database value) — instead of the real value `'OUT_FOR_DROPOFF'`, and was missing `'ARRIVED_DROPOFF'` entirely. Net effect, confirmed live and unrelated to any of this session's other work: **a driver/helper actively out on the drop-off leg of a real delivery was silently shown as "Available"/"Unavailable" instead of "Assigned"** on all three of those pages — a genuine double-booking risk, for the entire dropoff portion of every single delivery (often the longer leg, especially multi-stop). Found via a real live example (`DR-0026`, driver D006/helper H004, genuinely `OUT_FOR_DROPOFF`) — though that particular delivery is itself a month-old stale test fixture (`pickup_date`/`dropoff_date` both `2026-08-15`), so it doesn't show up on the crew page's *today* view regardless; the bug was confirmed instead with a same-day fixture (`DR-0053` temporarily set to `OUT_FOR_DROPOFF`, today's date). **Fixed**: `CREW_ACTIVE_STATUSES` now reads `['ASSIGNED', 'OUT_FOR_PICKUP', 'ARRIVED_PICKUP', 'OUT_FOR_DROPOFF', 'ARRIVED_DROPOFF']`. **Live-verified**: before the fixture's date/status flip, the test helper showed "Available"; after, `SupDeliveryCrew.jsx` correctly showed them "Assigned" and the page's "Assigned on Trips" counter incremented from 0 to 2 (driver + helper). Fixture restored afterward.
  - **Full app-wide crawl, every route in the router, 2026-09-09** — not just the areas this session's changes touch: all Admin/Supervisor/Driver/Helper/Customer routes, every sub-tab, and every drill-down that needs a real row clicked into (truck profiles, crew profiles, a real in-transit delivery, a real completed delivery's all 5 report tabs, a real cancelled delivery, a Driver history delivery's own Trip/Behavior report). Split across two parallel sweeps (Admin+Supervisor; Driver+Helper+Customer). Zero JS exceptions anywhere. One real, pre-existing, **entirely unrelated** bug found and fixed:
    - **`₱NaN` rendered in the Quotation tab** (`SupDeliveries.jsx`'s `BreakdownRow`, used by `QuotationBreakdown` on the Completed Deliveries report) — any direct/indirect expense line ≥ ₱1,000 rendered as `₱NaN` instead of the real amount. Root cause: the quotation form saves each breakdown field as the *comma-formatted display string* it was typed as (e.g. `"1,000.00"`), and `BreakdownRow` re-parsed it with a plain `Number(value || 0)`, which chokes on the thousands separator (`Number("1,000.00")` is `NaN`) — while `parseMoney` (already defined in this same file, used everywhere else these same fields get summed during live editing) strips it correctly. Found live on a real delivery (`DR-0057`'s "a. Driver" salary line, stored as `"1,000.00"`). Two other places in this file render these same fields (the live in-progress "Progress Details" quotation preview, both Initial and Final) but echo the raw string directly with no reparsing, so they were never affected — confirmed by reading both before ruling them out, not assumed. **Fixed**: `BreakdownRow` now uses `parseMoney(value)` instead of `Number(value || 0)` for non-numeric input. **Live-verified**: DR-0057's Quotation tab now correctly shows "a. Driver ₱1,000.00".
- **Two more findings while the user walked through this session's changes live, 2026-09-09** (both real, both fixed, both live-verified):
  - **Route color preference**: `NAV_LEG_COLORS`/`LEG_COLORS` (`DriverDeliveries.jsx`, `HelperDeliveries.jsx`, `SupDeliveries.jsx`, `SuggestedRouteMap.jsx` — four independent copies of the same palette, per this codebase's per-portal duplication convention) had the to-pickup leg pinned to red (`#DC2626`), a deliberate 2026-08-13 design choice per `02C_ROUTE_STYLING_AND_PROOF_VISIBILITY.md` — not a bug, but the user asked for red to stop being used for routes at all (reserved for real alerts/warnings instead). **Changed**: index 0 is now teal (`#0D9488`) in all four copies; the rest of the 6-color palette (blue/green/purple/orange/pink) is unchanged. Google's own live-traffic road tinting (a separate `<TrafficLayer />`, not app-controlled) still shows red for real congestion — that's live Google data, unrelated to this palette, and can't be recolored.
  - **A real, pre-existing, unrelated bug — the Live Navigation map could get permanently stuck on the Warehouse-origin fallback route until a manual page reload.** `DriverDeliveries.jsx`'s phone-GPS `watchPosition` call (~line 4288) was only ever started once, with no retry — if that very first attempt failed for any reason (most plausibly: the browser's location permission prompt was still unanswered at the exact moment monitoring started), the driver was stuck showing the Warehouse→Pickup fallback for the rest of the trip, with nothing short of a full reload able to recover. Found because the user noticed exactly this live ("why did I need to refresh?") and asked whether it was a bug. **Reproduced directly**: a fresh session with geolocation permission granted only *after* the page had already loaded and drawn the fallback route stayed stuck on that same fallback indefinitely (confirmed still stuck 16s later) — matching the user's real experience exactly. **Fixed**: the watch now retries every 5s for as long as monitoring stays on, so a late-answered permission prompt (or a brief real signal loss) self-corrects live instead of requiring a reload. **Live-verified**: same reproduction, this time the map picked up the newly-granted position within 6s with zero page reload.

- **GPS source split extended again, 2026-09-09: phone GPS is now the primary *persisted* source, not just live-display** — explicit user decision, reopening the 2026-08-31/09-03 split (`project_gps_source_split` memory, `05_GPS_PIPELINE.md`'s "GPS Source Split" section). Until now, the phone's browser geolocation was deliberately never written to `gps_logs` — it only ever drove `LiveNavigationMap`'s own marker and an ephemeral Realtime broadcast to the Supervisor Dashboard, by design, specifically so `gps_logs` stayed Pi-only/authoritative. That's now reversed: phone GPS writes directly into `gps_logs`, and the Pi's own `gps-upload` path is unchanged and still writes there too, now acting purely as the *fallback* for whenever a phone isn't actively broadcasting (no permission granted, browser tab closed, etc.) rather than the primary source it used to be. Concretely this means mileage summation, Route Comparison, and the proof-of-location check (`checkProofLocation`, used by Confirm Pickup/Complete Dropoff/Complete Stop) now all see real phone-GPS data whenever a phone is broadcasting, not just Pi data — found relevant live, 2026-09-09, when the user asked why the location check had nothing to enforce against despite the map clearly showing their real position; answer at the time was correct for how the app worked *before* this change (phone position was broadcast-only, never in `gps_logs`) — this entry is what changed that.
  - New `driver-trip` Edge Function action, `log-position` (`{deliveryRequestId, lat, lng}`, Driver-JWT-authenticated, same pattern as `start-trip`/`pause-trip`/etc.) — validates the delivery belongs to the caller, resolves the current Active session (if any) for `session_id` attribution (mirrors `gps-upload`'s own GPS-during-Pause rule: `session_id` set only while genuinely Active, null while Paused), inserts one `gps_logs` row. A phone can't use `gps-upload`'s device-secret auth (that's for the physical Pi only), so this is a separate, JWT-authenticated write path into the same table rather than reusing that function. No new grant needed — `service_role` already had `insert` on `gps_logs` (shared with `gps-upload`'s own client, grants are per-table not per-function).
  - `DriverDeliveries.jsx`'s existing phone-GPS `watchPosition` callback now also calls this action, throttled to once per second (`lastPersistedPositionAtRef`) to match the Pi's own upload cadence (`GPS_UPLOAD_INTERVAL`, `PROJECT_CONSTRAINTS.md`) rather than writing on every tick (which can fire far more often than 1/s). Fire-and-forget — a failed persist only logs a console warning, never blocks or errors the live-nav UI itself.
  - Scope note: this write only happens while `isMonitoring` (matching the pre-existing gating on the whole phone-GPS effect) — i.e. only while a Session is genuinely Active, same as before. During a Pause, the phone-GPS effect still doesn't run at all (unchanged), so only the Pi (if present) would write rows during that window — this wasn't extended, since it wasn't asked for.
  - **Live-verified**: real Driver login, fake real geolocation via Playwright, confirmed real rows landed in `gps_logs` with the correct `delivery_request_id`/`session_id`/lat/lng, cleaned up afterward. Zero rows existed for the test delivery beforehand and zero remained after cleanup.
  - **Full real two-actor end-to-end re-run after all of the above fixes, 2026-09-09** — one continuous real trip (Driver → Helper → Supervisor) exercising every feature built this session together: Start Pickup → real-GPS-verified Arrived at Pickup → Helper Confirm Pickup (location check genuinely evaluated, passed) → real-GPS-verified Arrived at Drop-off → Helper Complete Dropoff (location check genuinely evaluated a *different* real position, passed) → Supervisor opens the finished report, all 5 tabs. Zero errors anywhere. Found one more real bug this run that hadn't surfaced in any prior isolated test:
    - **`SupDeliveries.jsx`'s own `mapDbRequest` never carried `pickup_completed_at`/`pickup_arrived_at`/`dropoff_arrived_at` through from the raw row at all** — despite `buildRealTripAndBehaviorReport` correctly reading `delivery.pickupCompletedAt`/`pickupArrivedAt`/`dropoffArrivedAt` (added earlier this session), those fields were always `undefined` on the object actually passed in, so Trip Details' per-location Arrival always showed "Not recorded for this trip" and the Progress Timeline's "Pickup Confirmed" row always showed a hardcoded `"—"` — even on a trip where the Driver genuinely tapped Arrived and the Helper genuinely confirmed Pickup, with the real columns correctly populated in the database. Only surfaced because this run checked the Supervisor report against data from an actual just-completed real trip, not directly-seeded timestamps. **Fixed**: added all three fields to `mapDbRequest`; also fixed "Pickup Confirmed"'s hardcoded `"—"` to read the real timestamp, matching how `dropoffEvents` already worked. **Live-verified twice**: once via directly-seeded timestamps on a synthetic DELIVERED state (confirmed the display renders correctly), then again via a full real trip re-run (confirmed real Arrival/Departure times, and a real "Pickup Confirmed" timestamp, all render correctly end to end).
  - **Supervisor Deliveries module performance fix, 2026-09-09** — user-reported ("it seems slow" loading deliveries), two real, independent causes found and fixed, in order of actual impact:
    1. **The dominant cause: an 8-hop sequential network waterfall in `loadInbox`.** Before touching the payload-size issue below, `loadInbox` ran 8 independent reads — `list-clients`, `list-specialized-clients`, `quotation_settings`, `trucks`, `list-crew` (all 5 Edge-Function/table calls), the crew-active-trips query, `driver_default_assignments`, and the main `delivery_requests` query itself — **one after another via sequential `await`**, even though none of them needs another's result first. Edge Function invocations in particular carry real network+execution latency each, so paying that 4 times in a row (plus 4 more DB round-trips) compounded into a visibly slow load, and — same as the payload issue below — this whole chain re-runs on every Realtime change to `delivery_requests` from anyone in the app. **Fixed**: all 8 now run together in one `Promise.all`, cutting total wait to roughly the single slowest call instead of the sum of all of them; only the genuinely dependent step (the quotations fetch, which needs this batch's resulting delivery ids) still runs afterward. The two reads that were individually wrapped in their own try/catch (crew-active-trips, `driver_default_assignments`) keep that same "a failure here can't abort the rest of the load" isolation via a `.then(onFulfilled, onRejected)` pair instead. **Measured**: time from navigating to `/supervisor/deliveries` to the "Loading delivery requests..." placeholder clearing, with this fix in place: **177ms** (real Supervisor login, real data).
    2. **The main list query also fetched `select("*")`, including `suggested_route`** — a dense GPS-path JSON blob (~34KB average per delivery that has one; ~400KB total measured across just the 43 rows that existed at the time, 12 of which had a route). `suggested_route` is only ever needed for whichever *one* delivery is currently open (the Route Deviation Report, or the live in-progress detail view's planned-route map) — never for the whole list. **Fixed** (lazy-loading / column-projection, not a behavior change): `loadInbox`'s query now explicitly lists every column it needs *except* `suggested_route`; that field is fetched separately, only for the one delivery actually being viewed, in two places — `CompletedDeliveryReport`'s existing per-delivery effect (one extra parallel query alongside its sessions/alerts/gps_logs fetch) and a new small effect keyed on `selectedRequest?.id` feeding the live in-progress detail view's `SuggestedRouteMap`.
    - **A real bug caught live while verifying fix #1, unrelated to the two causes above**: the first attempt wrapped the `quotation_settings` read in a `.catch(...)` to preserve its original isolated-failure behavior inside the `Promise.all` — this broke the *entire* list load with `supabase.from(...).select(...).eq(...).maybeSingle(...).catch is not a function`, confirming this Postgrest query builder doesn't reliably expose `.catch` as a chainable method the way a plain `Promise` does. Only found because this was actually opened in a browser afterward, not just read for correctness — an assumption about a third-party library's API surface that silently would have broken the whole page for every Supervisor otherwise. **Fixed**: switched to the same `.then(onFulfilled, onRejected)` two-argument pattern already used (and already confirmed working) for the other two isolated-failure reads.
    - **Live-verified end-to-end after all of the above**: all 5 tabs (Delivery Requests Inbox, Assign Vehicle, In Transit Deliveries, Completed Deliveries, Cancellations) load with correct real counts and zero console errors; the Assign Vehicle list's "Specialized Crew" flag and quotation amounts (both fed by two of the now-parallelized reads) render correctly; a real completed delivery's Route Deviation Report (`DR-0021`) still shows the same planned/actual distance numbers via the new lazy fetch; a real delivery (`DR-0038`) temporarily flipped to `PENDING_REQUEST` still shows a real "Planned Route" map via the other new lazy fetch, then restored.

Planned

- Fleet management
- GPS tracking
- Reports
- Analytics Dashboard
- ~~Distinguishable pickup vs. dropoff markers/route color on `LiveNavigationMap`~~ — done, see Completed 2026-08-13 (`Phase 2C`). Shipped as a per-leg color palette plus Pickup/Dropoff/Stop pins, a scoped-up version of option (1) discussed here.

---

## Next Steps (handoff notes, updated 2026-08-14)

**Current actual next step:** Phases 1-8 are complete (backend/UI, Pi-side pipeline, hardware-confirmed, and now the Supervisor Realtime Dashboard — see Completed above). The ad-hoc live in-app navigation feature and the Helper portal catch-up are also both done and live-verified. All disposable test fixtures from these rounds of testing (`DR-0001`, `DR-0015`, `DR-0018`) have been reverted/cleaned up. **Phase 9** (`IMPLEMENTATION/09_EDGE_CASES.md`) is the active phase, and is now down to a single open item: its pre-work status check (done 2026-08-12, before any Phase 9 code was written) found three concrete gaps, all three now have code fixes — gap #1 (Paused-trip Start Trip guard) and gap #2 (Admin device-reassignment guard) are both fixed, deployed/click-tested, and verified live (2026-08-12, see Completed above); gap #3 (Pi camera-fault isolation) is code-complete (2026-08-14) but **not yet re-deployed to or re-tested on physical hardware** — that hardware retest is the only thing left in this phase. A related stuck-delivery-status gap found while building gap #1 is also fixed and curl-verified (see Completed above). `08B_ANALYTICS_AND_REPORTING.md` was also looked at during the pre-work check (its `SupDashboard` scope overlaps with what Phase 8 already wired — `ActiveDeliveries`/`DriverSafetyList` — and its "Required Schema" note about `sessions.driver_id` missing is stale, that column has existed since 2026-08-08) — deferred in favor of Phase 9, not resolved as done.

**Note on doc sync (2026-08-14):** gap #2's fix and the stuck-delivery-status fix had, until this update, only ever been logged in `09_EDGE_CASES.md`'s own Progress section, never folded back into this file's Completed log or this Next Steps section — which briefly made this file read as if gap #2 were still open when it wasn't. Both docs should now agree; `09_EDGE_CASES.md` remains the source of truth for Phase 9's own gap-by-gap detail.

**Also current as of 2026-08-14, end of day:** Phase 11 (Route Comparison, `11_ROUTE_COMPARISON.md`) went from undocumented-as-not-yet-live to real-pipeline-verified in one session — schema deployed, Edge Functions deployed, and exercised through the actual `gps-upload` device-auth path (not just browser/DB shortcuts), see the Completed entries above. It is now, alongside Phase 9's hardware retest, one of the two things this project is genuinely ready to test on physical hardware next. Two things surfaced tonight still need a decision, not just code, before that: (1) whether `end-trip` (or something downstream of it) should advance `delivery_requests.status` straight to `COMPLETED` once a delivery is actually done, instead of requiring a separate Customer-confirmation or Supervisor-issue-resolution step first — real product-behavior question, not yet implemented; (2) `service_role` is missing a `delete` grant on `sessions` (`SUPABASE_GOTCHAS.md` #11) — low urgency (nothing real needs it yet), but flagged so it isn't rediscovered from scratch.

Older steps 1-4 (below) are done (see Completed above: `get-driver-deliveries`/`update-driver-delivery` actions added, "Start Pickup" wired to `start-trip`, tested end-to-end including guardrails). What's left, from that point:

Also found and parked (not resolved): `DriverDeliveries.jsx` renders its map with **Leaflet**/`react-leaflet`, not Google Maps — contradicts the Google Maps Platform Setup decision in `IMPLEMENTATION/02_BOOKING_AND_TRIP_CREATION.md`. Full note on the likely resolution (Routes API still generates route data, Leaflet still renders it, Google Maps stays only as the existing navigation deep-link) is in `IMPLEMENTATION/01_SYSTEM_ARCHITECTURE.md`'s Route Comparison section — check there before touching map rendering.

Also found and parked (not resolved), 2026-09-08, per explicit user decision to tackle later: **a never-started `ASSIGNED` delivery whose `pickupDate` has passed can never become reachable again** in `DriverDeliveries.jsx`'s date-based Today/Upcoming/History tabs — it falls into History's Overdue section, which is read-only (no Start Pickup/Resume anywhere in `DeliveryDetailView`), with no path back since `workspaceDelivery`'s last-resort tier only matches `pickupDate === today`, not `<=`. Real example in live data: `DR-0053` (`D002`, `ASSIGNED`, `pickup_date` 2026-09-05). Recommended fix and its tradeoff (only the oldest such stale delivery gets pinned per load) are written up in full in `IMPLEMENTATION/09_EDGE_CASES.md`'s "Also noticed, not a Phase 9 item" section — that doc is the source of truth for this gap's detail.

Decided 2026-08-08: implement remaining phases in documented order (Phase 3B before Phase 7), rather than by which existing UI is easiest to wire — see `00_IMPLEMENTATION_RULES.md`'s "Implement only the current phase" principle.

Pause/Resume Trip (Phase 3B) is now fully done — backend and UI both built, tested via curl/console, and click-tested end-to-end in the browser (2026-08-08). Also decided but explicitly NOT yet implemented, and NOT blocking Phase 7 — pick back up when their own dependencies (Phase 4/5/6 Pi pipelines) are in scope: the `session_active` heartbeat field (silences the Pi's vibration motor on Pause) and GPS-during-Pause (anti-theft — schema/mechanism now resolved, see `DATABASE.md`'s `gps_logs` entry). See the In Progress section above for details on both.

Phase 7 (End Trip) is now fully done — backend and UI both built, curl-tested, and click-tested end-to-end in the browser (2026-08-09). Phases 1-3B and 7 are complete.

**Remaining step-by-step plan, next action is step 1:**

1. Phase 4 backend is deployed and tested (2026-08-11): `admin-users`' `register-device` action and the `device-heartbeat` Edge Function are both live, curl-verified (correct secret authenticates + updates `last_ping` + returns `session_active`; wrong secret gets `401` and leaves `last_ping` untouched). Still open, not blocking Phase 5/6: wiring the Pi side (outside this repo) to auto-launch the heartbeat sender on boot and actually call `device-heartbeat` every 10s.
2. Phase 5 (`IMPLEMENTATION/05_GPS_PIPELINE.md`) is done — `gps-upload` Edge Function deployed and curl-verified end-to-end (2026-08-11): Active-session insert, Paused-but-not-ended insert, no-session reject, and ended-Trip reject all confirmed against a real device/trip cycle. Required a live schema fix along the way (see Completed) — `DATABASE.md` now matches the deployed table. Still open: the Pi-side GPS sender itself (outside this repo, same as Phase 4's heartbeat sender).
3. Phase 6 (`IMPLEMENTATION/06_DROWSINESS_ALERT_PIPELINE.md`) is done — backend, UI, and now the Pi-side sender, all confirmed working end-to-end (2026-08-11, see Completed for full detail): `alert-upload` deployed and verified against both disposable test sessions and real hardware; `face_not_detected` added everywhere `ALERT_TYPE_LABELS` was enumerated; `DriverDeliveries.jsx`'s mock live-alert generator replaced with a real Realtime subscription on `alerts` (uncovered and fixed a publication gap along the way, see Completed); driver-facing audio replaced with two real user-provided clips (`Usual Alert.mp3`/`5+ Multiple Alert.mp3`) with an escalation rule and autoplay-unlock. Still open: revoking `anon`'s unrestricted `alerts` access (recommended, not done — flagged, not executed pending confirmation).
4. Separately tracked, not blocking any phase: the stale-open-session UI bug (`DriverDeliveries.jsx`'s date-based "Active" selection — see In Progress). The Pi-side script rewrite to match Phases 4-6 is done and hardware-verified (2026-08-11, see Completed) using dlib (GPIO 4 for the vibration motor, confirmed against real hardware after two wrong guesses — see Completed for the full story) — MediaPipe was tried first but reverted after real hardware blockers on the actual Pi 4 used for testing. Only remaining gap: boot automation (systemd unit) is still unwritten.
5. Note: `MIN_SCHEDULING_DAYS` in `src/lib/deliveryOptions.js` is temporarily set to `0` (was `3`) to allow same-day test deliveries — revert before production, see the `TEMPORARY` comment at that line.
